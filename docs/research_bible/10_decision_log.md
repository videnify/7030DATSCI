# 10 — Decision Log

**Purpose:** Every non-trivial scope, design, or methodology decision made during the project, with the reasoning behind it and the date — so choices are defensible to an examiner as deliberate rather than accidental, and so future-you can recall *why* something was done a particular way.
**Owner:** Ibrahim Haroun.
**Dependencies:** Referenced by nearly every other document in this folder; this is the "why" companion to `09_results_log.md`'s "what."
**Update Frequency:** **Every time a real decision is made** — a decision is anything where a reasonable alternative existed and was rejected. Append-only, chronological.
**Relation to Dissertation:** Direct source for dissertation Chapter 3's methodology justifications ("we chose X over Y because...") and for pre-empting examiner questions in the Discussion/Limitations chapters.

---

## How to log a decision

```
### YYYY-MM-DD — <decision title>
**Decision:** <what was decided>
**Alternatives considered:** <what else was on the table>
**Reasoning:** <why this option won>
**Reversible:** Yes/No — under what condition would this be revisited
```

---

### 2026-05-XX — Narrow project scope from broad causal/microstructure design to three RQs

**Decision:** Adopt the three-RQ structure (RQ1 abnormal returns, RQ2 feature importance, RQ3 ML vs. baseline) as the fixed scope, replacing an earlier, broader design (per `01_data_collection.ipynb`'s "v2.0 narrowed scope" title cell).
**Alternatives considered:** Multi-asset causal microstructure analysis; broader macro-event universe beyond presidential communications and FOMC.
**Reasoning:** The brief's own principle — "depth is preferred over breadth" — combined with MSc-dissertation scope constraints (a single semester of implementation time).
**Reversible:** No, without supervisor re-approval — this is the frozen Phase 0 scope referenced throughout `01_research_questions.md`.

---

### 2026-07-XX — Use lexicon sentiment as primary signal, FinBERT as robustness check only

**Superseded 2026-07-06 by Sentiment Engine Freeze v1.0 (SEF)** — see the dedicated SEF v1.0 entry near the end of this log. FinBERT is now the project's official primary sentiment engine; this entry is retained unmodified for the historical record, not as current guidance.

**Decision:** The curated financial/political lexicon scorer is the primary sentiment signal feeding the causal model and feature set; FinBERT is retained only as a secondary/robustness check.
**Alternatives considered:** FinBERT as primary (originally planned, given it's a purpose-built financial-sentiment transformer); an ensemble of both.
**Reasoning:** FinBERT was trained on financial-news headlines and, applied to formal presidential/policy language, produces 95.3% neutral labels — too little discriminative signal for downstream causal/feature use. The lexicon method (73.1% neutral) retains materially more usable variation. See `09_results_log.md` (2026-07-04 entry) for the numbers.
**Reversible:** Yes — if full-text (not title-only) scoring is added in a future iteration, FinBERT's domain mismatch may reduce and this decision should be re-tested, not assumed to still hold.

---

### 2026-07-XX — Rule-based (not ML) event-type classification

**Decision:** Classify APP documents into event categories (monetary, geopolitical, regulatory, trade, energy, health, labour, other) via rule-based keyword matching rather than a trained classifier.
**Alternatives considered:** Supervised ML classifier (would require a labelled training set); unsupervised topic modelling (LDA/BERTopic).
**Reasoning:** No labelled training set exists for this domain-specific category scheme, and rule-based matching is fully inspectable/auditable — an examiner can verify the classification logic directly rather than trusting an opaque model.
**Reversible:** Yes — if mislabelling is found to materially affect RQ1/RQ2 results, revisit with either a small hand-labelled validation set (to measure the rule-based method's precision/recall) or a topic-modelling alternative.

---

### 2026-07-04 — Keep the 8-notebook pipeline rather than splitting to match the 10-phase governing spec

**Decision:** Retain the current 8-notebook structure (sentiment folded into `03_event_detection.ipynb`, explainability folded into `07_model_evaluation.ipynb`) rather than splitting into the full 10-notebook layout described in `docs/00_project_workflow.md`.
**Alternatives considered:** Split `03_event_detection.ipynb` into separate event-detection and sentiment-analysis notebooks; split `07_model_evaluation.ipynb` into separate evaluation and explainability notebooks.
**Reasoning:** Phases 1–8 (as currently structured) are already complete; splitting at this stage adds file-management churn without changing any result, and the brief itself instructs "avoid unnecessary complexity." Phase-to-RQ traceability is preserved regardless of notebook boundaries (`docs/00_project_workflow.md`).
**Reversible:** Yes, low-cost if a supervisor specifically wants the dissertation methodology chapter structured around exactly 10 phases — the underlying code doesn't need to change, only file boundaries. **Status: this decision is proposed here but not yet formally closed** — `docs/00_project_workflow.md` still lists it as an open decision as of 2026-07-04; treat this entry as the recommended resolution, not yet a ratified one.

---

### 2026-07-04 — QQQ / GLD / TLT collected but not yet used downstream

**Decision (interim, not yet finalised):** Continue collecting QQQ, GLD, TLT prices per `config.yaml`, but do not include them in any Phase 3–8 result until a specific, scoped use case is defined.
**Alternatives considered:** Drop them entirely now; use them immediately in a cross-asset generalisation check.
**Reasoning:** Collection cost is low and the data is already in `data/raw/prices.parquet`; but including them without a defined analysis would violate "only include optional datasets if they improve the analysis." No such analysis has been scoped yet.
**Reversible:** Yes — either scope a cross-asset comparison (promote to active use, log a follow-up decision) or drop from `config.yaml`/`data/raw/` and document the exclusion in `05_data_dictionary.md` and `11_limitations.md`. **This decision must be closed one way or the other before submission** — see `13_validation_checklist.md`.

---

### 2026-07-04 — GDELT limited to 5-day proof-of-concept sample

**Decision (interim, not yet finalised):** Keep GDELT wired through the pipeline (`gdelt_daily_risk.parquet` exists and is merged into `daily_sentiment.parquet`) but do not treat any GDELT-derived feature as evidentially final given only a 5-day sample currently exists against a 2015–2025 study period.
**Alternatives considered:** Backfill the full historical GDELT series now; drop GDELT entirely.
**Reasoning:** Backfilling the full series is a non-trivial data-engineering task (GDELT's full historical export is large); dropping entirely would lose a genuinely relevant geopolitical-risk confounder. Interim compromise: keep the plumbing, exclude the feature from any "final" claim until resolved.
**Reversible:** Yes — this is explicitly flagged as needing resolution before Phase 6/7 results are treated as final. See `11_limitations.md` and `13_validation_checklist.md`.

---

### 2026-07-04 — Materialise `master_dataset.parquet` as a new, distinct artefact rather than reusing `model_features.parquet`

**Decision:** Build and freeze `data/processed/master_dataset.parquet` (clean/merged base data, no engineered features) as a new file, rather than treating the existing `model_features.parquet` (91 engineered features) as if it already served this role.
**Alternatives considered:** Rename/repurpose `model_features.parquet` as the "master dataset"; skip a dedicated freeze step entirely and keep letting `05_feature_engineering.ipynb` re-derive the merge in-memory each run.
**Reasoning:** `model_features.parquet` is already a downstream, feature-engineered product (lags, rolling windows, technical indicators) — collapsing it with the upstream "clean, merged" concept would blur the Phase 2/Phase 5 boundary and make it harder to audit each stage independently (e.g. this freeze's look-ahead-bias check is only meaningful at the pre-feature-engineering stage, before rolling windows introduce their own boundary effects). Re-deriving the merge in-memory each run (the prior status quo) meant there was no single frozen, versioned, independently-validated base dataset to point to — a reproducibility and auditability gap. Creating one new, clearly-scoped file closes that gap without duplicating `model_features.parquet`'s content (different column set: 34 raw/merged columns vs. 95 engineered columns).
**Reversible:** Low-cost to reverse if this proves redundant in practice — but not expected to be, since it directly satisfies the original 10-phase spec's "Create master dataframe" step (`docs/00_project_workflow.md` Phase 3), which the 8-notebook implementation had otherwise dropped.

---

### 2026-07-04 — Statistical Analysis Plan v1.0 frozen (Mission 04)

**Decision:** Freeze the complete Statistical Analysis Plan across five new documents (`statistical_analysis_plan.md`, `statistical_decision_matrix.md`, `statistical_assumptions.md`, `statistical_reporting_guidelines.md`, `dataset_contract.md`) plus an expanded `04_statistics_plan.md`, covering: α/CI/one-sided-vs-two-sided policy, multiple-comparison corrections, missing-data/outlier/transformation/scaling/stationarity policy, the full assumption matrix, the master test matrix (run and frozen-not-yet-run), and the exact RQ3 model-comparison protocol.
**Alternatives considered:** (a) Treat this SAP as a strict pre-registration written before any analysis — not honest, since Phases 1–8 already ran; (b) skip the freeze and let each remaining notebook (Missions 05–08) choose its own statistical methods ad hoc.
**Reasoning:** Option (a) would misrepresent the project's actual sequencing to an examiner. Option (b) risks exactly the kind of undocumented methodological drift the Research Bible exists to prevent. The adopted approach — ratify already-used RQ1/RQ2 methodology as-is (no numbers changed), freeze RQ3's remaining protocol prospectively before the baseline is trained — is stated explicitly in `statistical_analysis_plan.md`'s "Status of this freeze" section so the partial-retrospective nature is disclosed, not hidden.
**Additional judgment calls made and logged here for traceability:**
- **One-sided testing adopted for RQ3 only** (model-vs-baseline "outperforms" comparisons), two-sided remains the default everywhere else — directional test matches the already-existing directional wording of H3 in `02_hypotheses.md`, not introduced to inflate significance.
- **No outlier removal/winsorisation policy** — extreme return days are treated as signal, not noise, in an event-driven study; removing them would bias RQ1 toward smaller, "safer" effects.
- **VIF (>10), correlation (>0.90), and variance (<1e-8) thresholds** frozen for Mission 05, prospectively — previously undocumented.
- **Random Forest is not added as a fourth RQ3 predictive model**, despite the mission brief's Part K wording naming it alongside LASSO/XGBoost/LightGBM — RF stays an RQ2 importance tool only, per the existing `07_model_plan.md` candidate set. Flagged as an open scope question in `statistical_decision_matrix.md`, not silently resolved either way; revisiting it requires a Version 2 SAP amendment.
**Reversible:** Yes, via a numbered Version 2+ SAP amendment logged here — this entry establishes that governance rule going forward (no untracked statistical-method changes after v1.0).

---

### 2026-07-04 — Market-only baseline model not yet added (RQ3 gap) — ✅ resolved 2026-07-05, see below

**Decision:** Defer training the market-only baseline model until a dedicated session, rather than rushing a poorly-tuned baseline into the pipeline.
**Alternatives considered:** Add a quick untuned baseline immediately to "have a number."
**Reasoning:** An untuned baseline would bias the comparison in favour of the full-feature models (they were tuned; the baseline wouldn't be), producing a misleading RQ3 answer that looks resolved but isn't methodologically sound. Better to log the gap explicitly (`07_model_plan.md`) than to close it superficially.
**Reversible:** N/A — this is a sequencing decision, not a permanent one; see `07_model_plan.md` for the full specification of what needs to happen next.
**Resolution:** Closed 2026-07-05 (Mission 06) — see the "Market-only baseline (`Baseline_LASSO` v1.0) trained" entry below.

---

### 2026-07-05 — Feature Matrix v1.0 (FES v1.0) frozen; `car_results.parquet` approved as a second Mission 05B input

**Decision:** Freeze `data/processed/feature_matrix.parquet` (95 engineered features, 2,511 rows) built from exactly two inputs: `master_dataset.parquet` (v1.0, frozen) and `data/processed/car_results.parquet` (Phase-4 event-study output). `car_results.parquet` was **not** in Mission 05B's originally stated input list — this entry formally approves it as a second, explicit input, subject to a hard restriction: CAR/event-derived features built from it may be used only in event-enhanced models, never in the market-only baseline.
**Alternatives considered:** (a) Strict `master_dataset.parquet`-only scope, dropping `mean_car` and all CAR-derived event counts from the new canonical feature matrix entirely, since `master_dataset.parquet` alone cannot reconstruct them; (b) the adopted option, joining `car_results.parquet`'s daily aggregates as a second, explicitly-documented input.
**Reasoning:** `mean_car` is the single highest-ranked feature in the pre-freeze RQ2 result (`09_results_log.md`, 16.1% RF importance) and is required to test RQ2 and RQ3 meaningfully — dropping it would silently weaken the project's strongest existing finding for the sake of a stricter reading of Mission 05B's input list. `car_results.parquet` is itself an already-frozen, versioned Phase-4 artefact (not a "raw" source), so joining it does not violate `dataset_contract.md` term 2 (that term restricts re-deriving the *raw* price/VIX/macro/sentiment merge from `data/raw/*`, not incorporating another already-processed artefact). The restriction — CAR/event/sentiment features excluded from the market-only baseline, permitted only in event-enhanced models — is enforced structurally in `feature_contract.md`'s Baseline Eligibility table, not left as a convention a notebook could forget.
**Additional judgment calls made and logged here for traceability:**
- **`events_tagged.parquet`/`high_impact_events.parquet` remain out of scope.** `n_high_impact_events`, `days_since_hi_event`, and `high_impact_day` (present in the legacy `model_features.parquet`) require `is_high_impact`, which lives only in those two files — not approved as inputs here. Replaced by `sig_event_x_momentum` (uses the in-scope `n_sig_events` flag) where an equivalent interaction was needed. Re-adding them is a defined v1.1 candidate.
- **Warm-up/boundary rows are trimmed strictly**, per SAP v1.0's "trim, never back-fill" policy — 254 rows dropped (vs. `model_features.parquet`'s 2, which pre-dates the SAP freeze and did not trim as strictly). Train split shrinks from 2,014 to 1,761 usable rows; test split is unaffected (750, unchanged) since the longest warm-up window (200 days) falls entirely within 2015–2016.
- **Two documentation gaps fixed during the rebuild:** `momentum_21d` and `monetary_x_rate_cut` existed as real columns in the legacy `model_features.parquet` but were missing from the pre-freeze `06_feature_dictionary.md` tables. Both are included and documented in FES v1.0, correcting the Market category count to 27 (not 26) and the Interaction category to 8 (not 7) — `statistical_decision_matrix.md`'s "26 features (price + technical only)" baseline description should be read as 27 until that document is itself updated at Mission 06.
- **Variance/correlation/VIF thresholds applied for flagging, not automatic feature removal**, per `04_statistics_plan.md`/`statistical_assumptions.md` — 0 near-zero-variance features (after fixing a build bug where `car_event_day` was initially computed as a constant 1.0 for all rows — caught by the validation script, not shipped), 6 correlation pairs flagged (\|r\|>0.90), 10 features flagged VIF>10, all structurally expected (overlapping rolling windows, an interaction term correlated with its own parent, momentum vs. cumulative-return near-identity) and documented per-feature in `06_feature_dictionary.md` rather than silently dropped.
- **RF-importance selection is explicitly out of scope for this freeze** — Mission 05B produces the full, validated 95-feature candidate matrix; selecting a subset by RF importance (0.001 threshold, already-frozen from Phase 5) is a separate, later step, consistent with `14_project_dashboard.md`'s framing of Mission 05B as running "ahead of the existing RF-importance selection step."
- **Scaling parameters (train-split-only mean/std) are persisted in `feature_profile.json`, not baked into `feature_matrix.parquet`.** The frozen file stores raw engineered values — mirrors `dataset_contract.md` term 7's "scaling happens downstream" principle exactly, applied one layer further down the pipeline.
**Reversible:** Yes, via a new numbered Feature Matrix version (`v1.1`, `v2.0`) and a corresponding decision-log entry — e.g. re-adding `events_tagged.parquet`/`high_impact_events.parquet` as a third approved input, or revisiting the momentum/cumulative-return near-duplication once RF-selection results are in.

---

### 2026-07-05 — Market-only baseline (`Baseline_LASSO` v1.0) trained; Model Contract Protocol (MCP v1.0) frozen (Mission 06)

**Decision:** Train and freeze `Baseline_LASSO` (LassoCV, 27 Market-category features from `feature_matrix.parquet`, `TimeSeriesSplit(5)`, seed 42) as the project's official RQ3 benchmark, and freeze `model_contract.md` (MCP v1.0) governing which models are approved, how they're evaluated, and what "beats the baseline" means. Keep the baseline's outputs in dedicated `reports/baseline/`/`models/baseline/` files rather than joining `data/processed/model_comparison.parquet` or running the DM/two-proportion z-test comparison in this mission.
**Alternatives considered:** (a) Join `Baseline_LASSO` into `model_comparison.parquet` and run the full comparison against the existing full-feature LASSO/XGBoost/LightGBM immediately, closing RQ3 in one mission; (b) the adopted option — train and document the baseline only, deferring the comparison to Mission 07.
**Reasoning:** Mission 06's brief explicitly restricts comparing against event-enhanced models ("do NOT compare against event models yet") and the existing full-feature models were trained on the now-superseded `model_features.parquet`, not on `feature_matrix.parquet` (FES v1.0) — a same-feature-matrix comparison is not yet possible without retraining them, which is explicitly Mission 07's job, not Mission 06's. Keeping "baseline exists" and "baseline has been compared" as two separable steps also makes each step independently auditable.
**Additional judgment calls made and logged here for traceability:**
- **A single model family (LASSO) is used for the baseline, not a tree-based alternative.** Keeps model architecture constant across the RQ3 comparison (only feature scope varies) — already the reasoning frozen in `07_model_plan.md` prior to this mission, ratified again here.
- **Legacy `TimeSeriesSplit`-vs-`KFold` bug fixed.** `src/models.py: _lasso_baseline()` passed `LassoCV(cv=5, ...)`, which defaults to a shuffled `KFold`, silently violating `statistical_analysis_plan.md`'s "TimeSeriesSplit, never a random/shuffled split" policy. `Baseline_LASSO`'s build passes an explicit `TimeSeriesSplit(n_splits=5)` object instead — a correction, not a new methodological choice.
- **All 27 Market coefficients shrunk to zero at the CV-selected alpha — reported exactly as obtained, not re-tuned or adjusted.** This makes `Baseline_LASSO` numerically identical to a trivial mean predictor on every metric. This was verified deliberately (coefficient vector inspected directly, confirmed reproducible across two independent re-runs) rather than assumed to be a fitting error. See `09_results_log.md` (2026-07-05 entry) and `baseline_evaluation.md` Part G for the full interpretation.
- **Random Guess and Persistence comparators computed for context only**, per Mission 06 Part D — neither is a candidate RQ3 model; both exist solely so `Baseline_LASSO`'s numbers are read against a floor, not in isolation.
- **Scaling reused from `feature_profile.json`'s persisted train-split parameters, not refit.** Keeps `Baseline_LASSO` and any future Mission-07 model numerically consistent on the same 27 columns, and satisfies the "no undocumented preprocessing" QA requirement.
**Reversible:** Yes, via a new Baseline model version (`Baseline_LASSO v1.1`) if, e.g., a supervisor requests a market-only tree-based baseline in addition, or if a future Feature Matrix version changes the Market category's column set — each such change requires a `10_decision_log.md` entry and a version bump, per `model_contract.md`'s Versioning section.

---

### 2026-07-05 — Event-enhanced models retrained on FES v1.0; RQ3 comparison complete (Mission 07)

**Decision:** Retrain Event_LASSO, XGBoost, and LightGBM on `feature_matrix.parquet` (FES v1.0, 95 features) — the legacy versions of these models (trained on the superseded `model_features.parquet`) are not valid comparators against `Baseline_LASSO` (Mission 06), since a fair RQ3 comparison requires every model to read the identical frozen feature matrix. Run the frozen Diebold-Mariano + two-proportion z-test + Bonferroni protocol (`statistical_decision_matrix.md` Part K) against `Baseline_LASSO`, and re-run Random Forest importance + SHAP on the new matrix, replacing all legacy `model_features.parquet`-derived importance values.
**Alternatives considered:** (a) Compare the legacy full-feature models (trained on `model_features.parquet`) directly against `Baseline_LASSO` (trained on `feature_matrix.parquet`) without retraining — rejected, since the two model sets would differ in both feature set *and* feature matrix provenance, confounding the comparison; (b) the adopted option — retrain all three on the identical frozen matrix first.
**Reasoning:** MCP v1.0's fairness requirement ("every model MUST use the identical frozen feature_matrix.parquet") is not satisfiable by reusing legacy model objects. Retraining is a mechanical, reproducible step (documented hyperparameters/tuning/seed in `models/event/event_model_metadata.json`) and does not constitute a dataset, feature-engineering, or statistical-methodology change — it is applying the already-frozen protocols to the already-frozen matrix, exactly as Mission 07's restrictions require.
**Additional judgment calls made and logged here for traceability:**
- **Result: H0₃ is not rejected.** None of the three event-enhanced models clears the Bonferroni-corrected threshold (α = 0.0167) on either the RMSE (Diebold-Mariano) or directional-accuracy (two-proportion z-test) leg against `Baseline_LASSO`. Reported plainly as a null finding, per `02_hypotheses.md`'s explicit discipline that a null result is a valid, reportable outcome — not reframed or downplayed.
- **XGBoost/LightGBM tuned via `RandomizedSearchCV` with `TimeSeriesSplit(5)` (n_iter=25, seed 42) — genuine, documented tuning**, not left at `config.yaml`'s fixed defaults, to satisfy MCP v1.0's "no model wins purely on unequal tuning effort" fairness rule. `n_jobs=1` set on the base estimator (with `n_jobs=-1` on the search) to avoid nested-parallelism slowdown — a performance fix, not a methodological choice.
- **Comparison table and statistical tests kept in `reports/model_comparison/`, not overwriting `data/processed/model_comparison.parquet`.** Same "new canonical artefact, old one stays as a legacy record" pattern already used for `master_dataset.parquet`→no successor-needed and `model_features.parquet`→`feature_matrix.parquet` — keeps the FES v1.0-based comparison auditable as a distinct, complete artefact rather than silently mutating a pre-existing file with mixed provenance.
- **BH-FDR correction confirmed not applicable to this mission** — it governs RQ1's event-type family only (`statistical_analysis_plan.md` Part A); RQ3's frozen correction is Bonferroni, applied as specified. Stated explicitly in `reports/model_comparison/statistical_tests.json` so a future reader does not mistake its absence for an oversight.
- **SHAP computed via each library's native contribution API (`xgboost`'s `pred_contribs=True`, `lightgbm`'s `pred_contrib=True`) and the closed-form exact linear-SHAP identity for Event_LASSO — no external `shap`/`numba` package used.** These give mathematically exact TreeSHAP/linear-SHAP values (verified: every model's SHAP values sum exactly to its own prediction), not an approximation — chosen after the `shap` package's `numba` dependency proved impractical to install in the working environment; this is a tooling substitution with an equivalent (in fact exact, not sampled) result, not a methodological compromise.
- **Random Forest confirmed, again, as an importance-only tool** — fit once with fixed hyperparameters (not tuned via CV, since it is not a predictive candidate), consistent with `statistical_decision_matrix.md` Part K's "Open scope question" resolution and `model_contract.md`'s Approved Models table.
- **Directional-accuracy comparison caveat logged:** `Baseline_LASSO`'s test Dir. Acc. (0.575) is a degenerate artefact of its constant-positive prediction matching the test period's base rate — the two-proportion z-test against it is run exactly as the frozen SAP protocol specifies, but its interpretive weight is lower than the RMSE leg for this specific baseline version. Logged here so a future baseline version's comparison is read correctly relative to this one.
**Reversible:** Yes — a future Feature Matrix version, Baseline version, or model retuning requires a new decision-log entry and version bump (`model_contract.md` Versioning), and the RQ3 verdict above must be re-tested, not assumed to still hold.

---

### 2026-07-06 — QQQ, GLD, TLT confirmed as supplementary (non-required) datasets, out of current RQ scope

**Decision:** QQQ, GLD, and TLT remain in `master_dataset.parquet`'s schema as supplementary/optional cross-asset series, per the project brief's own "Optional" dataset list. They are not scoped into any RQ1–RQ3 result in the current dissertation and are not to be treated as an oversight — `15_traceability_matrix.md`'s "orphaned" flag is acknowledged and resolved by this entry, not by adding a new analysis or removing the columns.
**Alternatives considered:** (a) Scope a cross-asset robustness check into RQ2/RQ3 (e.g., using QQQ/GLD/TLT as additional predictors or a correlation control) before submission; (b) drop the three columns from `master_dataset.parquet` in a new dataset version; (c) the adopted option — leave the columns in place, documented as explicitly out of scope, and revisit only if time permits after the three RQs are otherwise complete.
**Reasoning:** The brief's own principle — "depth is preferred over breadth" (`00_project_overview.md`, `01_research_questions.md`) — means adding a fourth analytical thread this late in the project would dilute focus from RQ1–RQ3 rather than strengthen them. Removing the columns would require a new `master_dataset.parquet` version (v1.1), which is disproportionate for data that costs nothing to leave in place and is already clearly labelled as unused (`dataset_version.md`'s schema table already carries this caveat forward). Leaving them in, explicitly documented as supplementary, preserves optionality for a future robustness appendix without reopening the frozen dataset.
**Reversible:** Yes — if a supervisor or examiner specifically requests a cross-asset comparison, this decision should be revisited and a new version of `master_dataset.parquet` considered; until then, QQQ/GLD/TLT must not be cited as evidence for any RQ1–RQ3 claim in the dissertation text.

---

### 2026-07-06 — Notebook 01 confirmed as a historical data-collection record; frozen datasets are authoritative

**Decision:** `notebooks/01_data_collection.ipynb` is treated as the historical record of how `data/raw/` was originally assembled, not as a live, re-runnable source of truth. Where its markdown or in-notebook column-naming intent differs from what is actually cached on disk and carried into `master_dataset.parquet` (v1.0, frozen 2026-07-04) and `feature_matrix.parquet` (FES v1.0, frozen 2026-07-05), the frozen datasets and their contracts (`dataset_version.md`, `dataset_contract.md`, `feature_contract.md`, `05_data_dictionary.md`) are authoritative, not the notebook.
**Alternatives considered:** (a) Rebuild Notebook 01's collection code so it exactly reproduces the cached `macro_indicators.parquet` schema (`cpi`, `unemployment`, `mfg_employment`, `yield_spread` instead of the notebook's current `cpi_index`/`unemployment_rate` naming, with no `cpi_yoy` anywhere); (b) the adopted option — correct only the notebook's markdown narrative (Mission RCS v1.1) and record this authority ordering here, leaving the executable cell logic untouched since the project architecture is frozen and Notebook 01 is not scheduled to be rerun.
**Reasoning:** The Mission 03 data-provenance audit (2026-07-06) established that Notebook 01's cache guard (`load_if_cached`) prevents any re-collection while the existing cache file is present, so the drift between the notebook's code and the frozen schema is a documentation-accuracy risk, not a live reproducibility failure. Rebuilding the cell logic now would touch a "frozen architecture" notebook for a cosmetic naming mismatch with no current functional consequence — disproportionate for this stage of the project. Recording the frozen-datasets-authoritative rule here means any future reader (including an examiner) resolves the discrepancy correctly without needing to re-derive this reasoning.
**Reversible:** Yes — if `data/raw/macro_indicators.parquet` is ever lost and must be regenerated from a live FRED pull, Notebook 01's `FRED_SERIES` dict and derived-column logic must be rebuilt to match the frozen schema names exactly (`cpi`, `unemployment`, `treasury_10y`, `treasury_2y`, `mfg_employment`, `yield_spread`, `fed_funds_rate`) before being trusted as a new source — at that point this decision should be revisited and Notebook 01 promoted back to "live" status.

---

### 2026-07-06 — Run RQ3 as a granular 07A/07B/07C experiment sequence rather than one combined Mission 07 pass

**Decision:** Re-derive the RQ3 event-enhanced comparison as three separate, single-model experiments (07A `Event_LASSO`, 07B, 07C — one per candidate model), each restricted to training its one model and comparing only against `Baseline_LASSO`, with XGBoost/LightGBM training, SHAP, Random Forest importance, Diebold-Mariano, and the two-proportion z-test all explicitly out of scope for 07A. This sits alongside, not in place of, the existing 2026-07-05 Mission 07 entry (`09_results_log.md`) which already ran all three models plus the full significance-testing protocol in a single pass.
**Alternatives considered:** (a) Treat the existing 2026-07-05 Mission 07 result as final and skip re-deriving it — rejected because the current mission brief explicitly calls for an isolated, model-by-model protocol with its own artefact trail (`models/event/event_lasso_07a.joblib`, `reports/event/`, `reports/statistics/07A_event_lasso_summary.md`); (b) overwrite the existing Mission 07 artefacts with the new 07A run — rejected, since both runs are legitimate, reproducible, and it is more defensible to an examiner to show independent reproducibility (07A's numbers match Mission 07's Event_LASSO numbers almost exactly) than to silently replace one with the other; (c) the adopted option — run 07A as a new, additively-named artefact set and cross-reference both in `09_results_log.md`.
**Reasoning:** Running Event_LASSO in isolation, without XGBoost/LightGBM/SHAP/RF/DM/z-test in the same pass, makes each experiment's scope and restrictions independently auditable (matches this Research Bible's general practice of keeping "the model exists" and "the model has been compared" as separable steps, already used for `Baseline_LASSO` in Mission 06). The near-exact reproduction of the 2026-07-05 numbers is treated as a positive reproducibility signal, not a reason to discard either run.
**Reversible:** Yes — if 07B/07C are never run and only 07A exists, RQ3 must continue to cite the 2026-07-05 Mission 07 entry as the authoritative full-comparison result (it already contains the DM/z-test verdict); 07A/07B/07C would only become the authoritative comparison once all three experiments plus a formal significance-testing pass are complete under this narrower protocol.

---

### 2026-07-06 — XGBoost (07B) hyperparameter search space reconstructed, not reused verbatim from Mission 07

**Decision:** Mission 07B's `RandomizedSearchCV` for XGBoost used a newly-defined `param_distributions` grid (documented in `models/event/xgboost_07b_metadata.json`), reconstructed to be a reasonable, standard XGBoost tuning grid consistent with the 2026-07-05 Mission 07 XGBoost run's winning point estimate (`event_model_metadata.json`'s `best_params`). The full search space used in that prior run was never persisted anywhere in the Research Bible — only its winning point was recorded — so an exact re-derivation was not possible.
**Alternatives considered:** (a) Attempt to reverse-engineer the exact prior search space from the single winning point alone — rejected as unreliable guesswork that could not be verified either way; (b) skip hyperparameter tuning and hard-code the prior run's exact `best_params` — rejected, since Mission 07B's brief calls for genuinely re-running `RandomizedSearchCV`, and hard-coding a previously-found optimum would not be a real, independently-verifiable tuning pass, undermining the "no model wins purely on unequal tuning effort" fairness principle (`statistical_analysis_plan.md` Part A) applied consistently across every model in this project; (c) the adopted option — define a new, reasonable, fully-documented search space and report whatever result it produces honestly, including if it differs from the prior run's numbers.
**Reasoning:** This experiment's result (test RMSE 0.009881, R² −0.054) is qualitatively consistent with the prior run (test RMSE 0.009533, R² 0.019) — both show XGBoost overfitting and failing to beat `Baseline_LASSO` — but is not numerically identical, unlike `Event_LASSO`'s deterministic `LassoCV` alpha-path search in 07A which reproduced the prior run to full floating-point precision. This divergence is expected and disclosed, not smoothed over: `RandomizedSearchCV`'s outcome is sensitive to the exact distributions it samples from, and no verbatim record of the original distributions exists to reproduce against. Reporting this run's own number honestly, with the divergence explained, is preferred over presenting a false sense of exact reproducibility.
**Additional judgment call:** This experiment's `param_distributions` are persisted alongside `best_params` in `xgboost_07b_metadata.json` — a documentation improvement over the original Mission 07 run, logged so this gap does not repeat for 07C (LightGBM) or any future re-tuning.
**Reversible:** Yes — if the original Mission 07 XGBoost run's exact search space is ever recovered (e.g. from an external notebook history or log not currently in this repository), this entry should be revisited and 07B re-run against the recovered space for a tighter cross-check.

---

### 2026-07-06 — 07A/07B/07C isolated experiment sequence complete; formal significance testing still deferred to a dedicated mission

**Decision:** With Event_LASSO (07A), XGBoost (07B), and LightGBM (07C) all now retrained in isolation under the identical protocol, the 07A/07B/07C sequence is complete on its own terms (each single-model experiment, compared descriptively only against `Baseline_LASSO`). No Diebold-Mariano or two-proportion z-test has been run *within this sequence* — the existing 2026-07-05 Mission 07 entry remains the only source of a formally-tested RQ3 verdict (H0₃ not rejected for all three models), and it is retained as authoritative pending a decision on whether to re-run the significance tests on this sequence's own (numerically close but not identical) predictions.
**Alternatives considered:** (a) Immediately run Diebold-Mariano/two-proportion z-tests on the 07A/07B/07C predictions now that all three exist — rejected for this entry, since doing so was not authorised by the 07C mission brief (which, like 07A/07B, explicitly excluded significance testing) and would exceed this session's scope without an explicit instruction to do so; (b) discard the 07A/07B/07C sequence and rely solely on the 2026-07-05 Mission 07 entry — rejected, since the granular sequence has independent value (per-model auditability, documented search spaces, the `mean_car`-concentration finding from 07C) that the original combined run does not surface as clearly; (c) the adopted option — leave the sequence's own numbers as descriptive evidence, explicitly flag that a formal test on them has not been run, and let a future mission decide whether to run one.
**Reasoning:** Both sets of numbers (2026-07-05 Mission 07 vs. 2026-07-06 07A/07B/07C) agree in direction and rough magnitude for all three models (Event_LASSO and LightGBM modestly beat baseline on RMSE/R², both underperform on raw Dir. Acc.; XGBoost underperforms outright and overfits severely) — this consistency is itself evidence that the qualitative RQ3 finding (no model clearly and robustly beats the baseline) is not an artefact of one specific run's random hyperparameter draw. Formally re-testing the new numbers is a reasonable next step but is a distinct, explicitly scoped task, not an automatic extension of 07C.
**Reversible:** Yes — if a future mission runs DM/two-proportion z-tests on the 07A/07B/07C predictions specifically, this entry should be updated to record whether that test's verdict agrees with the existing 2026-07-05 Mission 07 result, and `09_results_log.md` should gain a corresponding entry.

---

### 2026-07-06 — Mission 03-PRECHECK: `03_event_detection.ipynb` must not consume `master_dataset.parquet`

**Decision:** Confirm and document that `03_event_detection.ipynb` continues to read directly from `data/raw/{app_presidential_documents,fomc_dates,gdelt_sample,prices}.parquet`, and explicitly does **not** switch to `data/processed/master_dataset.parquet` as an input, despite the latter being the project's frozen canonical merged dataset.
**Alternatives considered:** Switching the notebook's price/calendar reference (currently `data/raw/prices.parquet`, used only for plotting and trading-day reindexing) to `master_dataset.parquet`'s `spy_close`/`date` columns, on the reasoning that `master_dataset.parquet` is the "preferred input" for downstream notebooks per `dataset_contract.md`.
**Reasoning:** `master_dataset.parquet` is built *from* `data/processed/daily_sentiment.parquet` — which is this notebook's own output. If `03_event_detection.ipynb` were to read `master_dataset.parquet`, the pipeline would contain a circular dependency (03 → daily_sentiment.parquet → master_dataset.parquet → 03), meaning the notebook could never be run from a clean `data/raw/` state without `data/processed/master_dataset.parquet` already existing from a prior run — silently breaking reproducibility from raw data. `dataset_contract.md`'s own "Consumers of this contract" table lists only `05_feature_engineering.ipynb` and Mission 06 as consumers; `03_event_detection.ipynb` is correctly excluded because it sits upstream, not downstream, of the freeze. This finding arose from a mission brief (Mission 03-PRECHECK) that listed `master_dataset.parquet` as a "preferred input" for this notebook — that instruction does not hold up against the actual dependency graph and is not applied.
**Reversible:** Only if the pipeline is restructured so `daily_sentiment.parquet` is no longer an input to `master_dataset.parquet` (i.e. a new dataset version that excludes event/sentiment columns, deferring their merge to a stage after Notebook 03) — not currently planned; would require a new `dataset_version.md` version and a corresponding update to `dataset_contract.md`'s consumer list.

---

### 2026-07-06 — Mission 03-PRECHECK: event-window construction stays in `04_causal_analysis.ipynb`, not moved into `03_event_detection.ipynb`

**Decision:** Confirm `03_event_detection.ipynb` continues to produce only a tagged event catalogue, daily sentiment index, and high-impact flag — it does not construct the ±5/+10 trading-day event windows or compute abnormal/cumulative abnormal returns. That logic remains solely in `04_causal_analysis.ipynb`'s `EventStudy` class.
**Alternatives considered:** The Mission 03-PRECHECK brief's suggested notebook structure included "10. Event window construction" and "11. Event calendar validation" as sections within `03_event_detection.ipynb`, and suggested output files `event_windows.parquet` / `event_calendar.parquet`.
**Reasoning:** Moving event-window construction into Notebook 03 would duplicate logic already implemented and validated in `src/causal_engine.py::EventStudy` / `04_causal_analysis.ipynb`, creating two places that could each define "the event window" and risk drifting apart — directly against the project's "never duplicate files"/"never duplicate logic" principle. No `event_windows.parquet` or `event_calendar.parquet` file exists or was created; the existing four outputs (`events_tagged.parquet`, `daily_sentiment.parquet`, `high_impact_events.parquet`, `gdelt_daily_risk.parquet`) remain the canonical, sole outputs of this notebook.
**Reversible:** Low priority to revisit — if a future refactor genuinely wants a single "event calendar" artefact shared between Notebooks 03 and 04, it should be built as a new, distinct file consumed by both, not by relocating logic that already works correctly in 04.

---

### 2026-07-06 — Mission 03-PRECHECK: notebook alignment scope — documentation only, no executable cell changed

**Decision:** The `03_event_detection.ipynb` alignment pass added documentation cells only (expanded purpose/inputs/outputs header, an EDA cross-reference section, an event-definitions reference table, a pre-save validation cell, a post-save verification cell, and a handoff section) — no existing executable cell's logic was modified, and neither `02_eda.ipynb` nor any frozen artefact (`master_dataset.parquet`, `feature_matrix.parquet`) was touched.
**Alternatives considered:** Physically reordering the notebook into the mission brief's suggested 13-section structure (imports → load canonical datasets → load event sources → ... → output export → section summary).
**Reasoning:** The existing §3.0–§3.11 structure already maps cleanly onto the suggested 13-part structure (documented in the new title-cell "Pipeline stages" table) — a physical cell reorder would be pure churn with no functional benefit, and risks introducing execution-order bugs that cannot be verified without a live kernel in this session. The two new code cells added (`validation-checks`, `post-save-verification`) are purely additive and read-only/diagnostic — they do not alter `catalogue`, `daily_sentiment`, `high_impact`, or any saved file.
**Reversible:** N/A — this is a completed, low-risk documentation pass. The new validation cells' output has not yet been executed as of this pass; the notebook should be run top-to-bottom before treating the validation checks as having actually passed (see the notebook's own handoff section).

---

### 2026-07-06 — Mission 03: full execution surfaced two upstream schema drifts, both patched minimally

**Decision:** After a live top-to-bottom execution of `03_event_detection.ipynb`, two cells failed or would have produced misleading numbers because two raw files' schemas had drifted since this notebook was last written: (1) `data/raw/fomc_dates.parquet`'s `decision` column is now named `rate_decision` (also gained `is_emergency`/`event_importance`, unused here) — cells `fomc-enrich` and `viz-sentiment-ts` were patched to reference `rate_decision`; (2) `data/raw/vix.parquet`'s single `vix` column is now `vix_close`/`vix_change`/`vix_high_regime` — no code change was needed since this notebook only consumes VIX's shape/date/null-count generically, but the fact is now documented in `05_data_dictionary.md` for the next notebook that assumes a bare `vix` column.
**Alternatives considered:** Leaving the notebook broken and reporting the failure without fixing it; regenerating `fomc_dates.parquet`/`vix.parquet` to restore the old column names instead of updating the consumer.
**Reasoning:** The value sets underlying both renamed columns are unchanged (FOMC decision categories: hold/hike/cut/emergency_cut/taper, counts identical to previously documented; VIX levels unaffected) — this is a pure naming drift in the upstream raw file, not a semantic or data-quality change, so fixing the consumer (this notebook) is the minimal, correct repair per the mission's own "identify root cause, fix minimally, rerun, continue" instruction. Reverting the raw files would be a larger, riskier, and undocumented change to `data/raw/`, which the project brief instructs against doing casually.
**Reversible:** Yes — if `data/raw/fomc_dates.parquet` is ever regenerated with the old column names, this patch should be reverted; until then, `rate_decision` is the correct reference.

---

### 2026-07-06 — Mission 03: cache-merge row-explosion bug found and fixed during execution

**Decision:** The sentiment-scoring cache-hit path (cell `finbert-run`) merges cached `sentiment_label`/`sentiment_score` from `events_tagged.parquet` onto `app` via `merge(..., on='title', how='left')`. Because many titles repeat across `app` (up to tens of thousands of times for generic titles like "Press Briefing by Press Secretary..."), merging against a non-deduplicated cache multiplies rows combinatorially. Fixed by deduplicating the cache to one row per title (`drop_duplicates(subset=['title'], keep='first')`) before merging.
**Alternatives considered:** Merging on `(date, title, doc_type)` instead of `title` alone (a tighter key); abandoning the cache entirely and re-scoring every run.
**Reasoning:** Deduplicating on `title` alone preserves the cache's actual intent (a per-title sentiment lookup, since titles are largely deterministic given the classification/scoring logic) with a one-line fix, whereas switching the merge key would be a larger behavioural change not required to fix the immediate bug. No sentiment *values* changed — only the accidental row multiplication was removed.
**Reversible:** Yes — revisit if a future need arises to cache sentiment per `(date, title)` rather than per `title` (e.g. if the same title genuinely warrants different sentiment on different dates, which the current rule-based/lexicon/FinBERT methods do not produce).

---

### 2026-07-06 — Mission 03: `05_data_dictionary.md` row counts for `events_tagged.parquet`/`high_impact_events.parquet` corrected after live execution

**Decision:** Update `05_data_dictionary.md` to record the actual, freshly-verified row counts — `events_tagged.parquet` 11,664 rows (not the previously documented 180,594) and `high_impact_events.parquet` 4,100 rows (not 7,864).
**Alternatives considered:** Leaving the old figures in place on the assumption they reflected a since-superseded but still "real" prior state.
**Reasoning:** A live, freshly-executed, freshly-verified (post-save file check) run is the more authoritative source than a documentation entry that cannot currently be reproduced — 11,664 = 11,570 core-president APP docs + 89 FOMC + 5 GDELT exactly, a clean, explainable one-row-per-event total, whereas 180,594 had no similarly clean explanation and could not be reproduced by re-running the pipeline from the current raw inputs.
**Reversible:** Yes, if a future re-run legitimately produces a larger catalogue (e.g. after full-text scraping or GDELT backfill) — update again with the new verified count and note the reason for the change, per this document's own append-only convention.

---

### 2026-07-06 — Mission 03: sentiment cache is dominantly FinBERT-sourced, not lexicon — flagged, not silently reconciled

**Decision:** Flag, rather than silently resolve, a discrepancy between documented methodology and actual pipeline output: `03_methodology.md` and an earlier entry in this log state the lexicon method was selected as the *primary* sentiment signal (FinBERT retained only as a robustness check), reasoning that FinBERT's title-level output on policy language is dominated by neutral labels. However, the live-executed `events_tagged.parquet`'s `sentiment_source` column shows 11,570 of 11,664 rows (99.2%) are sourced `finbert` (reused from a prior cached FinBERT run when PyTorch was available in that environment), with only 89 `rule` (FOMC) and 5 `gdelt_goldstein` rows from other sources — lexicon accounts for 0 of the currently cached APP rows (the 59 titles newly scored this run used lexicon only because they were absent from the cache, not by design preference). The resulting sentiment distribution (95.1% neutral, mean +0.0054) closely matches the FinBERT numbers previously reported (95.3% neutral, +0.0067), not the lexicon numbers (73.1% neutral).
**Alternatives considered:** Silently updating the methodology documents to describe FinBERT as primary (matches the data); forcibly re-scoring the entire catalogue with the lexicon method now to make the data match the documented decision.
**Reasoning:** Either fix would be a substantive methodology change with downstream consequences for `daily_sentiment.parquet` → `master_dataset.parquet` → `feature_matrix.parquet` → the already-frozen Mission 06/07 model results — squarely out of scope for an "execute, validate, and freeze Notebook 03" mission that explicitly forbids modifying frozen datasets. The correct action here is to surface the discrepancy clearly for a deliberate decision in a dedicated future session, not to paper over it in either direction.
**Reversible:** N/A — this is a flag, not a decision. **Action required from the founder/Project Director:** confirm whether `03_methodology.md`'s "lexicon is primary" statement should be corrected to reflect that the frozen cache is actually FinBERT-sourced, or whether the catalogue should be re-scored with lexicon to match the documented methodology — either has downstream implications that should be weighed before acting.

**Resolved 2026-07-06 — see the Sentiment Engine Freeze v1.0 (SEF) entry near the end of this log.** The Project Director chose the first alternative: correct the documentation to match the actual FinBERT-dominant pipeline output, rather than re-score the catalogue with lexicon.

---

### 2026-07-06 — Architecture diagram cleanup: archive superseded ARIMA-era diagrams, delete misattributed files, rebuild 5 canonical diagrams

**Decision:** Of the 13 SVGs (+2 PNGs) in `docs/architecture/`, 11 were moved to `docs/architecture/archive/` (describing an ARIMA/SARIMAX intervention-analysis methodology that was never implemented in the frozen pipeline, or referencing NewsAPI/ECB as core sources, or structurally outdated relative to the current repo), 2 were deleted outright (`data_pipeline_architecture_rebuilt.{svg,png}`, titled "GOVFIN Analytical Pipeline" — a different project; `project_structure.svg`, already marked deprecated and also referencing a `govfin/` directory), 1 was corrected in place (`car_formula.svg` — removed an "ARIMA forecast" reference, corrected the event window to config.yaml's actual −5/+10 days, corrected the asset index note to reflect SPY as the actual RQ1 evidence base), and 5 new canonical diagrams were built from scratch: `project_pipeline.svg`, `data_lineage.svg`, `research_governance.svg`, `rq_traceability.svg`, `modelling_flow.svg`.
**Alternatives considered:** Updating the old diagrams in place instead of archiving; deleting everything instead of archiving; keeping the mission brief's literal RQ1/RQ2 lane assignment in `rq_traceability.svg` even though it doesn't match this project's frozen research questions.
**Reasoning:** The old diagrams describe a methodology (ARIMA baseline + intervention equation + SARIMAX) that does not exist anywhere in `src/` or the notebooks — the project was narrowed to a market-model event study + DoWhy backdoor estimation + LASSO/XGBoost/LightGBM design before this Research Bible existed (see the "narrow project scope" 2026-05 entry above), and the diagrams were never updated to reflect that narrowing. Updating 11 diagrams describing an abandoned methodology in place would be more effort than a clean rebuild and would still leave stale terminology; archiving preserves them as a historical record of the project's evolution without letting them mislead an examiner. For `rq_traceability.svg` specifically: the mission brief specified RQ1→Event Study and RQ2→Causal Analysis as separate lanes, but `01_research_questions.md` clearly assigns **both** the event study and the causal analysis to RQ1, with RQ2 answered by feature importance/SHAP instead. Rendering the brief's literal (inaccurate) lane assignment would itself create a new, wrong architecture diagram — directly against the mission's own stated goal of "accurate SVG diagrams." The diagram was built to match the frozen RQs, with an on-figure caption explaining the correction, rather than silently complying or silently diverging without explanation.
**Reversible:** Yes for the archive (files are on disk, not lost); no for the 2 deleted files (irrecoverable, but both were unambiguously misattributed to a different project and contained no unique accurate content worth preserving).

---

### 2026-07-06 — Sentiment Engine Freeze v1.0 (SEF): FinBERT ratified as the project's official primary sentiment engine

**Decision (Project Director, approved):** FinBERT is now the project's official, primary sentiment engine. The lexicon-based sentiment scorer is retained in the repository only as a fallback mechanism (used when FinBERT/PyTorch is unavailable in the runtime environment) and as a historical prototype from earlier in the project. It is not the primary methodology used to generate any frozen dataset. This decision is frozen as **Sentiment Engine Freeze v1.0 (SEF v1.0)** and is a documentation/governance update only — no code, datasets, models, or statistical outputs were changed.

**Why this decision was made:** Live top-to-bottom execution of `03_event_detection.ipynb` (Mission 03, 2026-07-06) established that `events_tagged.parquet`'s `sentiment_source` column is 11,570 of 11,664 rows (99.2%) `finbert`-sourced, with only 89 `rule` (FOMC) and 5 `gdelt_goldstein` rows from other sources — lexicon accounts for 0% of the currently cached APP rows. This directly contradicted the documentation then in force (`03_methodology.md` and the 2026-07-XX "lexicon as primary" entry above), which described the lexicon scorer as primary and FinBERT as a robustness check only. That discrepancy was flagged, not silently resolved, in the 2026-07-06 Mission 03 execution entry above, pending a founder/Project Director decision.

**Evidence:**
- `events_tagged.parquet` `sentiment_source` breakdown: `finbert` 11,570 (99.2%), `rule` 89 (0.8%), `gdelt_goldstein` 5 (0.04%) — see `05_data_dictionary.md`, `09_results_log.md` (2026-07-06 Mission 03 entry).
- Resulting sentiment distribution (95.1% neutral, mean +0.0054) closely matches the previously-reported FinBERT numbers (95.3% neutral, +0.0067), not the lexicon numbers (73.1% neutral) — see `09_results_log.md` (2026-07-04 entry).

**Alternatives considered:** (1) Re-score the entire event catalogue with the lexicon method to make the data match the previously documented "lexicon is primary" decision — rejected, since it would be a substantive methodology change with downstream consequences for `daily_sentiment.parquet` → `master_dataset.parquet` → `feature_matrix.parquet` → the already-frozen Mission 06/07 model results, none of which this mission is authorised to touch. (2) Leave the discrepancy flagged indefinitely — rejected, since an examiner reading `03_methodology.md` alongside the actual data would reasonably conclude the Research Bible is internally inconsistent, a genuine dissertation-quality risk.

**Decision:** Correct the documentation to describe FinBERT as the official primary sentiment engine, matching what the pipeline actually runs and has actually produced in every frozen artefact inspected. The lexicon scorer's role is re-described as fallback/historical rather than deleted or removed from the repository — it remains in `src/event_detector.py`/Notebook 03 as a legitimate fallback path and is still directly relevant to `causal_estimates.parquet`'s historical (pre-SEF) treatment-variable choice.

**Documentation updated:** `docs/research_bible/03_methodology.md` (Causal DAG note, §3 sentiment scoring section), `11_limitations.md` (L6), `06_feature_dictionary.md` (Sentiment Features category header), `dataset_version.md` (sentiment column description), `04_statistics_plan.md` (Part E per-notebook table), `09_results_log.md` (2026-07-04 and 2026-07-06 entries, appended notes — numbers unchanged), `14_project_dashboard.md` (Mission row, Top 3 risks), `future_improvements.md` (item 23, marked resolved), root `README.md` (Sentiment Scoring section, Phase 4 methodology line), `notebooks/03_event_detection.ipynb` (markdown cells `finbert-header`, `interp-sentiment`, `handoff-header`, `title`). `docs/architecture/` diagrams were checked and contain no sentiment-primacy claims requiring correction.

**Explicitly NOT changed:** `data/processed/events_tagged.parquet`, `daily_sentiment.parquet`, `master_dataset.parquet`, `feature_matrix.parquet`, any trained model, any evaluation metric, any statistical test result, `01_research_questions.md`, `02_hypotheses.md`, SAP v1.0 (`statistical_analysis_plan.md`, `statistical_decision_matrix.md`, `statistical_assumptions.md`, `statistical_reporting_guidelines.md`), FES v1.0 (`feature_contract.md`), or MCP v1.0 (`model_contract.md`). Numbers reported against "lexicon sentiment" in `02_hypotheses.md`, `05_data_dictionary.md` (`causal_estimates.parquet` row), and the frozen SAP bundle are retained unchanged as historical, pre-SEF results — they describe what was actually computed at the time, not the project's current official methodology.

**Reversible:** Yes — this is a documentation/governance decision, not a code or data change. If full-text (not title-only) scoring is added in a future iteration and reduces FinBERT's domain-mismatch effect (`11_limitations.md` L6), or if a future mission deliberately re-scores the catalogue with the lexicon method, this decision should be revisited and a new version (SEF v1.1 or v2.0) logged rather than silently overwritten.

---

### 2026-07-06 — Project Governance Freeze: `00_project_freeze.md` created as the final governance milestone

**Decision:** Create `docs/research_bible/00_project_freeze.md`, the master governance declaration for DATSCI7030, and designate it the first document any future contributor should read. It formally closes the governance sequence that began with Dataset Version 1.0 and states, in one place: what is frozen (Section 3), why (Section 1), what changes remain allowed (Section 6), and what is prohibited (Section 7).

**Governance sequence this closes (chronological):** Dataset Version 1.0 (2026-07-04) → Dataset Contract (2026-07-04) → SAP v1.0 (2026-07-04) → FES v1.0 (2026-07-05) → MCP v1.0 (2026-07-05) → Sentiment Engine Freeze v1.0 (2026-07-06, above) → Research Bible Complete (2026-07-06) → Architecture Complete (2026-07-06) → **Project Governance Freeze (2026-07-06, this entry)**.

**Reasoning:** Each prior freeze fixed one layer of the project (data, statistics, features, models, sentiment methodology) independently, but no single document stated that the *project as a whole* is now frozen and moving into the dissertation-writing phase. Without this, a future contributor (or the author, months later) could reasonably assume any individual frozen artefact was still open to casual revision as long as no other document explicitly forbade it. `00_project_freeze.md` closes that gap by stating the default explicitly: nothing listed in its Section 3 may change without a new, dated, numbered version and a corresponding decision-log entry — silence is no longer ambiguous.
**Alternatives considered:** Folding this content into `00_project_overview.md` (rejected — that document narrates *what the project is*, a different purpose from *what is frozen and why*, and conflating them would make both harder to maintain); relying on the sum of individual freeze documents without a single master declaration (rejected — an examiner or new contributor would have to reconstruct the freeze status by reading nine separate documents).
**Files updated:** `docs/research_bible/00_project_freeze.md` (created), `docs/research_bible/README.md` (added as the folder's recommended starting point, without renumbering `00_project_overview.md`), root `README.md` (one-sentence link), `14_project_dashboard.md` (Project Status fields + mission row), this entry.
**Explicitly not changed:** Any dataset, model, statistical output, Research Question, hypothesis, SAP, FES, MCP, or SEF content — this is a documentation/governance addition only, referencing those artefacts rather than modifying them.
**Reversible:** In principle yes (it is a document), but in practice this is intended to be the terminal governance milestone before dissertation writing — reopening it would itself be a significant, dated decision, not a routine edit.

---

### 2026-07-06 — Final Notebook Alignment: notebooks 04–08 style-aligned to the Notebook 03 standard; legacy-pipeline gap in 05–07 surfaced and documented, not silently fixed

**Decision:** Align `notebooks/04_causal_analysis.ipynb` through `08_results_visualisation.ipynb` to the same structural/style standard as `03_event_detection.ipynb` (purpose/inputs/outputs/RQ/pipeline-position/scope-in/scope-out headers, interpretation cells after major results, validation cells after saves, handoff cells). Fix only minimal, non-output-altering bugs. Do not redesign, retrain, or regenerate any frozen artefact.

**What was found:**
1. **A genuine schema-drift bug beyond Notebook 03.** `data/raw/vix.parquet`'s `vix`→`vix_close` rename (already fixed in Notebook 03's context) also broke `04_causal_analysis.ipynb` (two call sites: `load-data`, `event-study`) and `05_feature_engineering.ipynb` (`load-data`) — all three hardcoded the old `vix` column name and would raise `KeyError: "['vix'] not in index"` on a fresh run (this is the same error the user hit independently while running `04_causal_analysis.ipynb` locally). Fixed by aliasing `vix_close` → `vix` immediately after load in each notebook — a one-line rename at the point of load, not a change to any formula, threshold, or output value.
2. **A significant, pre-existing reproducibility gap.** `notebooks/05_feature_engineering.ipynb`, `06_model_training.ipynb`, and `07_model_evaluation.ipynb` are all dated 2026-05-31 and implement the **legacy, pre-freeze** pipeline: `model_features.parquet` (88 candidate / 52 RF-selected features, in-memory re-derivation), a single undifferentiated LASSO/XGBoost/LightGBM comparison with no market-only baseline. The actual frozen `Baseline_LASSO` (Mission 06, `models/baseline/`) and retrained `Event_LASSO`/`XGBoost`/`LightGBM` (Mission 07, `models/event/`) — the models the dissertation's RQ3 verdict (H0₃ not rejected) is based on — were **not produced by these notebooks**; no script or notebook in this repository reproduces that training run end-to-end. This was already partially flagged (`06_feature_dictionary.md`'s header note, `15_traceability_matrix.md`'s legacy-file rows) but not previously stated as plainly as "there is no reproducible notebook for the frozen RQ3 result."
3. **One overstated causal claim.** `07_model_evaluation.ipynb`'s summary cell printed "Confirms causal event signal is the primary driver" based on a SHAP importance ranking — SHAP is an associational attribution method, not a causal-inference method, and this notebook doesn't run DoWhy. Corrected the wording to describe it as the highest SHAP attribution, with a pointer to `04_causal_analysis.ipynb` for the actual causal evidence.
4. **08c/08d figures confirmed stale**, not merely pending: they are generated from the legacy `model_features.parquet`/`model_comparison.parquet`, so they necessarily show the pre-baseline 3-model comparison, not the frozen MCP v1.0 result — root-causing what `08_figures_plan.md` had already flagged as needing regeneration.

**Alternatives considered (for the legacy-pipeline gap):** Rewrite `05`–`07` to actually consume `feature_matrix.parquet`/`feature_contract.md` and reproduce `Baseline_LASSO`/`Event_LASSO` training end-to-end — rejected for this mission. That would be a substantive code change, not a style/documentation alignment, and carries real risk of producing numbers that don't exactly match the already-frozen, already-cited `models/baseline/`/`models/event/` results (different random seeds, library versions, or CV fold ordering could all shift a retrained model's coefficients). Silently leaving the notebooks unlabelled and treating the alignment mission as complete without mentioning this — also rejected, since an examiner would reasonably expect `notebooks/` to be the reproducible source of the dissertation's headline result.

**Decision:** Apply full style alignment plus a prominent "⚠ Legacy status" section to each affected notebook's title cell, clearly stating what it does and does not reproduce, and pointing to the actually-frozen artefacts (`model_contract.md`, `baseline_model_specification.md`, `baseline_evaluation.md`, `reports/statistics/07_event_models_summary.md`) as authoritative. Do not attempt to rewrite the training logic in this pass. `04_causal_analysis.ipynb` and `08a`/`08b` of `08_results_visualisation.ipynb` remain current and required no legacy framing — only the vix bug fix and style additions.

**Files updated:** `notebooks/04_causal_analysis.ipynb` (vix fix ×2, full style header, 3 interpretation cells, validation + post-save-verification cells, handoff cell, lexicon-primacy language qualified against SEF v1.0), `05_feature_engineering.ipynb` (vix fix, legacy-status title + handoff note), `06_model_training.ipynb` (legacy-status title + handoff note), `07_model_evaluation.ipynb` (legacy-status title + handoff note, overstated-causal-claim fix), `08_results_visualisation.ipynb` (legacy-status title distinguishing current 08a/08b from legacy 08c/08d, handoff note), `15_traceability_matrix.md` (Notebooks table legacy annotations), `future_improvements.md` (items 29–31).

**Explicitly NOT changed:** Any frozen dataset (`master_dataset.parquet`, `feature_matrix.parquet`, `car_results.parquet`, `causal_estimates.parquet`, `events_tagged.parquet`, `daily_sentiment.parquet`), any model artefact (`models/baseline/`, `models/event/`, `models/archive/`), any statistical test result, the Research Questions, SAP v1.0, FES v1.0, MCP v1.0, or SEF v1.0. No notebook was executed as part of this pass — the vix-column fix is a static code correction, verified by reading the merge logic, not by re-running the notebook (this sandbox lacks the user's local `datsci` environment/DoWhy installation, and re-running would risk producing a different `car_results.parquet`/`causal_estimates.parquet` than the already-frozen, already-cited one).
**Reversible:** Yes — all changes are additive markdown/documentation plus a minimal, verifiable one-line bug fix per notebook. Rebuilding `05`–`07` to close the reproducibility gap (item 29, `future_improvements.md`) remains open for a future, dedicated mission.

---

### 2026-07-06 — Mission 05–07 Reproducibility Rebuild: `05_feature_engineering.ipynb`, `06_model_training.ipynb`, `07_model_evaluation.ipynb` rewritten to reproduce the frozen pipeline end-to-end; reproducibility gap (item 29, above) closed

**Decision:** Rewrite `notebooks/05_feature_engineering.ipynb`, `06_model_training.ipynb`, and `07_model_evaluation.ipynb` so that, executed in order on a clean checkout, they reproduce the already-frozen `feature_matrix.parquet` (FES v1.0), `Baseline_LASSO` (`models/baseline/`), and `Event_LASSO`/`XGBoost`/`LightGBM` (`models/event/`) artefacts from their documented contracts and recorded parameters — closing the gap identified in the Final Notebook Alignment entry above (item 29, `future_improvements.md`). No frozen dataset, model, RQ, hypothesis, SAP, FES, MCP, or SEF content was changed; this is a reconstruction of the reproducing code path, not a re-derivation of new results.

**Method:** Each notebook was rebuilt from its governing contract document rather than from the legacy 2026-05-31 code:
- **05** reads only the two FES v1.0-approved inputs (`master_dataset.parquet`, `car_results.parquet`), derives all 95 features per `06_feature_dictionary.md`'s exact formulas (Market 27, Macro/VIX 16, Sentiment 25, Event 14, Temporal 5, Interaction 8), trims via `dropna()` (the true trim boundary is `days_since_car_event` being undefined before the first CAR event on 2016-01-05, not rolling-window warm-up as previously assumed), and computes scaling parameters using population `std` (`ddof=0`) to match the frozen convention.
- **06** reads only `feature_matrix.parquet` + `feature_profile.json`, asserts `feature_matrix_validation.json` status is PASS, trains `Baseline_LASSO` on exactly the 27 Market-category features per `baseline_model_specification.md` (`LassoCV(cv=TimeSeriesSplit(n_splits=5), random_state=42, max_iter=50000, tol=1e-6)`).
- **07** reads only `feature_matrix.parquet` (95 features), trains `Event_LASSO` identically to Baseline_LASSO but on the full feature set, and refits `XGBoost`/`LightGBM` directly from the `best_params` already recorded in `models/event/event_model_metadata.json` rather than re-running `RandomizedSearchCV` (the original search space was never persisted — refitting from the recorded winning parameters is the reproducible, honest choice; re-searching a reconstructed space was tried in an earlier mission and did not reproduce the frozen numbers).
- Every notebook ends with a reproduction-check cell that loads the corresponding frozen artefact and asserts numerical equality (`atol=1e-6` for feature values and model coefficients; metric-level comparison for RMSE/MAE/R²/Dir_Acc/IC and DM/z-test statistics), plus a frozen-artefact save guard (`if not fpath.exists(): write() else: print("already exists (frozen) — NOT overwritten")`) so re-running the notebooks never clobbers the cited results.
- All three notebooks were executed end-to-end in a clean sandboxed Python 3.10 environment (scikit-learn 1.7.2, xgboost 3.0.5, lightgbm 4.6.0, scipy 1.15.3 — one minor version above the originally recorded xgboost 3.2.0 due to a Python-version constraint; no numerical effect observed) with zero cell errors.

**Result — reproduction confirmed exact:**
- **05:** all 95 reconstructed feature columns matched the frozen `feature_matrix.parquet` to within 1e-6 (0 mismatches out of 96 compared columns); category counts, row count (2511), and date range (2016-01-05 to 2025-12-29) all matched `feature_profile.json`/`feature_matrix_validation.json` exactly. `REPRODUCTION_EXACT = True`.
- **06:** `Baseline_LASSO` alpha (0.0018492955165777085), intercept (0.0004333597638550524), all-27-coefficients-zero result, and all train/test metrics matched `models/baseline/baseline_model_metadata.json`/`reports/baseline/baseline_metrics.json` exactly. `REPRODUCTION_EXACT = True`.
- **07:** `Event_LASSO`, `XGBoost`, and `LightGBM` train/test RMSE/MAE/R²/Dir_Acc/IC all matched `reports/model_comparison/model_comparison.parquet` (no mismatches > 1e-4); Diebold-Mariano and two-proportion z-test statistics and p-values all matched `statistical_tests.json` (no mismatches > 1e-6); the RQ3 verdict reproduced exactly ("H0₃ not rejected — no event-enhanced model beats `Baseline_LASSO` at Bonferroni α=0.0167").

**Two implementation bugs found and fixed during reproduction (neither affects any previously reported/frozen number, since both were caught before being saved):**
1. **Cell-ordering `NameError` in 07** — the statistical-comparison cell referenced `metrics_test_baseline` before the cell defining it; fixed by reordering the two cells. Cosmetic/structural only.
2. **Two-proportion z-test one-sided p-value formula (substantive, caught by the reproduction-check itself)** — the initial implementation used a sign-conditional formula, `p = 1 - norm.cdf(z) if z > 0 else norm.cdf(z)`, which silently reports whichever tail is smaller rather than testing the actually-specified fixed-direction hypothesis (H1: event-enhanced model's directional accuracy > baseline's). This was detected because the reproduction-check showed the z-statistic matching the frozen value exactly while the z p-value did not (diffs of 0.32–0.68) — pinpointing the bug to the p-value derivation, not the statistic. Fixed to the correct fixed-direction form, `p = 1 - norm.cdf(z)` (always), after which all z p-values matched frozen exactly. This is exactly the class of finding the mission brief required to be surfaced rather than hidden; it is now documented in Notebook 07's own `interp-reproduction` cell as a worked example of why validating against frozen ground truth (not just internal code self-consistency) matters. No previously published z-test p-value in `reports/model_comparison/statistical_tests.json` or the dissertation-facing summaries was ever affected, since that frozen file was produced by a different (correct, at the time) implementation — this bug existed only in the newly-written reconstruction code during this mission, and was caught before any output was saved.

**Files updated:** `notebooks/05_feature_engineering.ipynb`, `06_model_training.ipynb`, `07_model_evaluation.ipynb` (full rewrites, Version 3.0, "FES v1.0 / MCP v1.0 Reproduction" framing replacing the 2026-05-31 legacy-pipeline code and its "⚠ Legacy status" framing from the entry above), `08_results_visualisation.ipynb` (title/legacy-status cells updated to reflect that 05–07 are no longer legacy, while 08c/08d remain genuinely stale pending a follow-up), `future_improvements.md` (item 29 marked ✅ Resolved with full detail; new item 32 added for the still-open 08c/08d follow-up, under a new "Newly identified (Final Notebook Alignment, 2026-07-06, continued)" subsection header to avoid colliding with the pre-existing item 30).

**Explicitly NOT changed:** `feature_matrix.parquet`, `feature_profile.json`, `feature_matrix_validation.json`, `master_dataset.parquet`, `car_results.parquet`, `models/baseline/*`, `models/event/*`, `reports/baseline/*`, `reports/model_comparison/*`, any Research Question, hypothesis, SAP v1.0, FES v1.0, MCP v1.0, or SEF v1.0 document. Every save cell in the rebuilt notebooks is guarded to detect that these files already exist and skip overwriting them — confirmed in execution output for all 3 notebooks.

**Reversible:** Yes — the rebuilt notebooks are pure reconstructions of already-frozen, already-cited results; no downstream document (dissertation chapters, `09_results_log.md` figures, `reports/`) needs updating as a result, since no number changed. If a future mission wants to regenerate the frozen artefacts from scratch (rather than merely reproduce them), that would be a new, separately-authorised, dated decision.

---

### 2026-07-06 — Results Visualisation Freeze v1.0 (RVF v1.0): `08_results_visualisation.ipynb` repointed at the canonical MCP v1.0 pipeline; last legacy dependency in the notebook chain removed

**Decision:** Rebuild `notebooks/08_results_visualisation.ipynb`'s `load-data` cell and figures `08c_predictive_pipeline.png`/`08d_full_dashboard.png` to read exclusively from the frozen canonical pipeline (`feature_matrix.parquet`, `reports/model_comparison/`, `reports/baseline/`) instead of the legacy `data/processed/model_features.parquet`/`feature_metadata.parquet`/`test_predictions.parquet`/`shap_values.parquet`/`model_comparison.parquet`/`evaluation_summary.parquet`/`models/model_metadata.json`. This closes `future_improvements.md` item 32 and removes the last legacy-pipeline dependency anywhere in `notebooks/01`–`08`.

**What the audit found:**
1. **`load-data` was the single point of legacy dependency**, feeding both `08c` and `08d`. Confirmed by direct inspection: the cell's stored output already showed it reading `model_features.parquet` (91-feature legacy schema) and computing `BEST_MODEL` from `models/model_metadata.json` — an artefact from the pre-freeze pipeline, unrelated to the frozen `Baseline_LASSO`/`Event_LASSO`/`XGBoost`/`LightGBM` comparison.
2. **A second, subtler legacy dependency inside `fig-08a`** (previously believed fully current): Panel 5 (rolling sentiment vs. SPY cumulative return) read `feat_df['log_return']` and `TRAIN_CUT` — both sourced from the same legacy `load-data` cell. Because the fix repoints the shared variable names `feat_df`/`TRAIN_CUT` at `feature_matrix.parquet` and the FES v1.0 split boundary rather than renaming them, `fig-08a` required no code changes at all — it now draws on canonical data automatically. This is recorded because the prior "Final Notebook Alignment" mission (2026-07-06, above) had asserted `08a`/`08b` were "current" without having traced this specific line.
3. **`fig-08c`/`fig-08d` were fully legacy**: feature groups (6 legacy groups: price/technical/sentiment/event/macro/interaction, not the 6 FES v1.0 categories), model comparison (3 models, no baseline), SHAP values, and predictions all came from the pre-freeze pipeline. Neither figure could have been read as evidence for the frozen RQ3 verdict.
4. **A bug introduced during the rebuild itself, caught before being treated as final**: the first draft of the SHAP panel (`fig-08c`) did not exclude the `base_value` column present in `shap_values_event_lasso.parquet` (the SHAP expected-value/intercept term), so it appeared in the "Top 10 SHAP Features" ranking as if it were a real feature (4th by mean |SHAP|). Caught by visually inspecting the rendered figure, not by any automated check — fixed by excluding `base_value` alongside `date`/`split` when building the feature list, and the notebook was re-executed to confirm the corrected figure.

**Design decision — "representative model," not "best model":** the legacy `08c`/`08d` picked a single `BEST_MODEL` from `models/model_metadata.json` for the single-model panels (SHAP, predicted-vs-actual, rolling IC, signal, strategy). Since the frozen MCP v1.0 result is that **no event-enhanced model beats `Baseline_LASSO`** (H0₃ not rejected, `statistical_tests.json`), silently carrying forward a "best model" framing risked implying a validated winner that does not exist. The rebuild instead names `Event_LASSO` the **representative** model (smallest Diebold-Mariano p-value and largest RMSE improvement of the three, i.e. numerically closest to significance) and prints/annotates the RQ3 null result directly on both figures (`fig.text(...)` captions on `08c` and `08d`), so neither figure can be read in isolation as claiming a model "wins."

**Statistical validation:** every metric plotted in `08c` (RMSE/MAE/R²/Dir_Acc/IC bars, predicted-vs-actual R²/IC) is computed live from `reports/model_comparison/model_comparison.parquet` and `reports/model_comparison/event_model_predictions.parquet` — the same frozen files verified byte-for-byte in the Mission 05-07 Reproducibility Rebuild (above) — so it is definitionally consistent with the frozen Mission 06/07 numbers, not a separately-risked re-derivation. No hardcoded/copied numbers appear anywhere in either figure's code.

**Files updated:** `notebooks/08_results_visualisation.ipynb` (`load-data`, `fig-08c`, `fig-08d` cells rebuilt; `title` and `legacy-status-note` markdown cells rewritten to remove the "⚠ Status" legacy warning; one stray empty trailing code cell removed), `reports/figures/08c_predictive_pipeline.png` and `08d_full_dashboard.png` (regenerated), `docs/research_bible/08_figures_plan.md` (figure notes updated to reflect the rebuild).

**Explicitly NOT changed:** `feature_matrix.parquet`, `reports/model_comparison/*`, `reports/baseline/*`, `models/baseline/*`, `models/event/*`, any Research Question, hypothesis, SAP v1.0, FES v1.0, MCP v1.0, or SEF v1.0 document, and no model was retrained. `06a_model_comparison.png` (produced by `06_model_training.ipynb`) was out of scope for this mission (Notebook 08 only) and was not re-verified.

**Remaining, deliberately out-of-scope item:** the repeated x-axis tick labels across `08d`'s shared-x subplots (a cosmetic matplotlib rendering quirk inherited unchanged from the original design, `sharex` re-synchronising formatters at draw time) and the inherent thinness of single-day bars across a 3-year test window in `08d` panels 3/4 (confirmed present via pixel-level inspection of the saved PNG, not actually empty — just visually sparse at chat-preview resolution) are noted but not fixed, since neither affects data correctness and redesigning the panel layout was not authorised by this mission's brief.

**Reversible:** Yes — this is a data-source repoint plus documentation update; no frozen artefact, dataset, or statistical result was changed.

---

### 2026-07-06 — `03_methodology.md` §4/§5 corrected to describe FES v1.0/MCP v1.0 (surfaced during Dissertation Chapter 3 consistency check)

**Decision:** Rewrite `03_methodology.md`'s Feature Engineering Methodology (§4) and Machine Learning Methodology (§5) sections, which still described the pre-freeze pipeline (91 engineered / 52 RF-selected features, a 52-feature 2,013/750-row split, a three-model LASSO/XGBoost/LightGBM comparison with an explicit "⚠️ Missing baseline (RQ3-blocking)" flag) despite `feature_contract.md` (FES v1.0) and `model_contract.md` (MCP v1.0) having frozen the actual, current pipeline (95 features across 6 categories, 2,511 rows, `Baseline_LASSO` + three event-enhanced candidates, Bonferroni-corrected DM/z-test promotion criteria) on 2026-07-05.

**Why this was found now:** the "Dissertation Writing Mission D1 — Chapter 3 Methodology Freeze" mission named `03_methodology.md` as a primary source to write the dissertation's Methodology chapter from, alongside `feature_contract.md`/`model_contract.md`/`00_project_freeze.md`. Reading all of them together for the mandated pre-section consistency check surfaced a direct contradiction: `03_methodology.md` §4/§5 would have caused the dissertation to describe a pipeline that no longer exists (no baseline, 52 features, legacy output filenames), while the governance-frozen contracts describe the pipeline that actually produced every number in `09_results_log.md`. This is exactly the class of inconsistency the mission's brief required to be surfaced, not silently written around.

**Decision (continued):** Per the Project Director's direction, `03_methodology.md` was corrected now, before drafting any dissertation section that depends on it, rather than left stale with the dissertation sourcing the contracts directly. Both corrected sections open with an explicit dated note stating what previously stood there and why it changed, so the correction itself is auditable.

**Files updated:** `docs/research_bible/03_methodology.md` (§4, §5 rewritten; category table, target variable, train/test split, scaling rule, baseline-eligibility rule, approved-model roster, validation protocol, promotion criteria, inputs/outputs/notebook references all corrected to match `feature_contract.md`/`model_contract.md` exactly).

**Explicitly NOT changed:** `feature_contract.md`, `model_contract.md`, `feature_matrix.parquet`, any model artefact, any statistical result, any Research Question or hypothesis. This is a documentation-only correction bringing one stale document in line with contracts that were already frozen and already correctly described elsewhere (`00_project_freeze.md`, `08_figures_plan.md`, `15_traceability_matrix.md`).

**Reversible:** Yes — documentation correction only.

---

### 2026-07-06 — Repository-wide documentation currency audit: root `README.md`, `docs/00_project_workflow.md`, and folder-level READMEs corrected to match the frozen pipeline

**Decision:** Audit and correct every non-Research-Bible documentation file in the repository (root `README.md`, `docs/00_project_workflow.md`, and the folder-level READMEs under `data/`, `models/`, `logs/`, `src/`, `scripts/`, `tests/`, `reports/`) for currency against the frozen governance state (Dataset Contract, SAP v1.0, FES v1.0, MCP v1.0, SEF v1.0), prompted by the user noting the root README's date looked stale.

**What was found (all pre-dated one or more of the FES v1.0/MCP v1.0/SEF v1.0 freezes and had not been updated since):**
1. **Root `README.md`** — the top-level Project Overview and "Key Methods" sections claimed causal inference used "DoWhy / CausalML" (CausalML was never used anywhere in this project) and event detection used "spaCy NER" (never used in the frozen pipeline; only rule-based keyword classification + FinBERT). The H3 hypothesis summary listed "Random Forest / XGBoost / LightGBM" as the RQ3 predictive candidates — Random Forest is an RQ2-only feature-importance tool per `model_contract.md`, not an RQ3 candidate, and `Baseline_LASSO`/Event_LASSO were omitted entirely. A separate "Notebooks" table (distinct from the already-current "Phase Progress" table) used un-numbered filenames and described notebook 06 as training "XGBoost/LightGBM" and notebook 07 as only "evaluation" — both stale relative to the actual 06=baseline/07=event-training-and-comparison split. The repository-structure tree omitted `docs/research_bible/` entirely. A "Current Project Status (as of 2026-07-04)" section claimed "no commits made yet," contradicting `14_project_dashboard.md`'s confirmed git-tracked state, and listed "four overlapping Data Collection Report drafts" when only one such report (in two file formats) actually exists on disk.
2. **`docs/00_project_workflow.md`** — Phase 8 ("Machine Learning") still described "Random Forest, XGBoost, and LightGBM trained on the event-augmented feature set" as the model roster, the same RQ2/RQ3 conflation as the README. Its "Notebook naming note" described the 8-notebook-vs-10-phase divergence as an still-open decision, when the Project Governance Freeze has since closed it.
3. **`data/raw/README.md` and `data/processed/README.md`** — both described files that do not exist under those names (`fred_macro.parquet`, `gdelt_events.parquet`, `features.parquet`, `target.parquet`) and omitted most of what is actually on disk (`app_presidential_documents_raw/economic.parquet`, `spy_returns.parquet`, `master_dataset.parquet`, `feature_matrix.parquet` + companions, etc.). This is the exact "stale `data/raw/`/`data/processed/` READMEs" P1 item already flagged in `14_project_dashboard.md` but not yet actioned.
4. **`data/external/README.md`** — described four CSV files (`sp500_constituents.csv`, `fomc_dates.csv`, `earnings_calendar.csv`, `macro_events.csv`) that were never actually collected; the folder is empty (`.gitkeep` only) in the frozen pipeline.
5. **Missing folder READMEs** — `src/`, `scripts/`, `tests/`, and root `reports/` had no README at all, another already-flagged P1 item.
6. **New discovery, not previously tracked anywhere:** `docs/Project_Summary.docx` (2026-05-04, "Version 1.1") is a third, undiscovered duplicate of `docs/research_bible/00_project_overview.md`'s role, describing SPY/QQQ/GLD/TLT as four co-equal "target assets" (contradicting the frozen scope, where only SPY is a target asset and QQQ/GLD/TLT are optional/exploratory) and omitting `Baseline_LASSO` from its model list. Flagged to the Project Director; not modified in this pass (a `.docx`, and not referenced by any governance document, so its disposition — archive, delete, or update — is a decision for the Project Director rather than an automatic documentation-sync fix).

**Decision (continued):** All items in 1–5 were corrected in place (not left as `future_improvements.md` entries), since they are exactly the kind of "documentation clarifications/corrections" `00_project_freeze.md` Section 6 permits without a new governance version. Item 6 (`docs/Project_Summary.docx`) was surfaced to the Project Director rather than acted on unilaterally.

**Files updated:** `README.md`, `docs/00_project_workflow.md`, `data/raw/README.md`, `data/processed/README.md`, `data/external/README.md`, `models/README.md` (already current, unchanged), `src/README.md` (created), `scripts/README.md` (created), `tests/README.md` (created), `reports/README.md` (created).

**Explicitly NOT changed:** Any dataset, model, statistical result, notebook code, source module (`src/*.py`), Research Question, hypothesis, SAP v1.0, FES v1.0, MCP v1.0, or SEF v1.0 document. `docs/Project_Summary.docx` was read and flagged but not edited.

**Reversible:** Yes — documentation-only correction pass.

---

### 2026-07-07 — Event study methodology description corrected to match `04_causal_analysis.ipynb`'s actual implementation (surfaced during Dissertation Chapter 3, Section 3.3 consistency check)

**Decision:** Correct `03_methodology.md` §1 (Event Study Methodology), `04_statistics_plan.md` ("Event Study Tests"), and `11_limitations.md` (L4) to describe what `04_causal_analysis.ipynb` actually implements and what actually produced the frozen `car_results.parquet`, rather than the previously-documented (and previously-planned but never implemented) design.

**What was found:** Cross-checking `03_methodology.md` §1 against `04_causal_analysis.ipynb` while drafting Section 3.3 of the dissertation surfaced a three-way mismatch:
1. **Estimator.** `03_methodology.md` and `src/causal_engine.py::EventStudy` both describe/implement a market-model event study — an OLS regression of SPY returns on a market-proxy series (alpha/beta), over a 120-day estimation window. `04_causal_analysis.ipynb`'s `compute_car()` (Cell 5) does not import or call `src/causal_engine.py` at all; it implements a constant-mean-return (mean-adjusted) model — expected return is simply SPY's own historical mean log return over an estimation window — with no market-proxy regression. `src/causal_engine.py::EventStudy` is unused legacy module code.
2. **Estimation window.** Documented as "120 trading days before the event window"; the notebook's actual constant (`ESTIMATION_WINDOW = (-252, -21)`, Cell 1) is a ~231-trading-day window ending 21 days before the event, not 120 days ending immediately before it.
3. **Event-type scope.** Documented as CAAR aggregated over seven types (including health and labour); the event study only runs on `high_impact_events.parquet`, whose types are capped by `HIGH_IMPACT_TYPES = {monetary, geopolitical, trade, regulatory, energy}` (five types) — health and labour events cannot be flagged high-impact and never enter the event study.
4. **Significance testing.** `04_statistics_plan.md` prescribed testing CAAR (event-type averages, to mitigate event-clustering non-independence) with a Benjamini-Hochberg FDR correction across event types. `04_causal_analysis.ipynb` Cell 7 tests pooled, per-event CAR directly (not CAAR-averaged) with no FDR correction, plus a Wilcoxon signed-rank robustness check and a separate positive-vs-negative sentiment-direction test that neither prior document mentioned. `11_limitations.md` L4 additionally contained a factually incorrect claim that the clustering violation was "mitigated by testing on CAAR" — verified false against the actual code.

**Alternatives considered:** (a) Treat the Research Bible/statistics-plan design as authoritative and rewrite `04_causal_analysis.ipynb` to match it (market-model regression, 120-day window, CAAR-averaged + BH-corrected significance testing), regenerating `car_results.parquet`. (b) Document the notebook's actual, executed methodology as the authoritative description, correcting the Research Bible and Limitations documents to match, and reporting the unimplemented CAAR-averaging/FDR-correction as an explicit, named limitation rather than a silently-claimed mitigation.

**Reasoning:** Per the Project Director's decision, option (b) was adopted for both the estimator/window question and the significance-test question: the dissertation must describe what actually produced the frozen results, not an aspirational design that was never executed. Rewriting the notebook now would change the frozen `car_results.parquet` and downstream `causal_estimates.parquet`, which is out of scope for a documentation-consistency pass and would itself require a new governance version under `00_project_freeze.md`. Documenting reality accurately, and naming the CAAR/FDR gap as an open limitation (already partially tracked in `11_limitations.md` L3), preserves reproducibility and honesty without altering any frozen artefact.

**Files updated:** `03_methodology.md` §1 (and its cross-reference in §2), `04_statistics_plan.md` ("Event Study Tests"), `11_limitations.md` (L4).

**Explicitly NOT changed:** `04_causal_analysis.ipynb`, `src/causal_engine.py`, `car_results.parquet`, `causal_estimates.parquet`, or any other frozen dataset/model artefact.

**Reversible:** Yes — if the CAAR-averaging + Benjamini-Hochberg correction described in the original statistics plan is implemented in a future notebook re-run, this entry and the affected documents should be updated again to describe the corrected procedure, and `car_results.parquet`'s significance flags would need to be regenerated and re-reported.

---

### 2026-07-07 — "Log return" corrected to "simple (percentage) return" in Section 3.3 / `03_methodology.md` §1 (surfaced during Section 3.4 consistency check)

**Decision:** Correct the already-frozen dissertation Section 3.3 and `03_methodology.md` §1 to describe the SPY return series used in `04_causal_analysis.ipynb` as a simple (percentage) return, not a log return.

**What was found:** While researching Section 3.4 (Causal Inference), re-reading `04_causal_analysis.ipynb` Cell 3 confirmed `spy['spy_return'] = spy['close'].pct_change()` — a simple percentage return — is the only return series computed in this notebook, used identically by the event study (Cell 5) and all three DoWhy causal fits (Cells 10, 15, 17). No log transform (`np.log`) appears anywhere in this notebook. This contradicted the "log return" language already written into the frozen Section 3.3 and `03_methodology.md` §1 (inherited from the pre-correction dissertation text and not caught during the Section 3.3 consistency check, which focused on the estimator/window/significance-test discrepancy rather than the return-type detail).

**Scope check:** `05_feature_engineering.ipynb` (Section 3.5) genuinely does compute log returns via `np.log(df['spy_close'] / df['spy_close'].shift(1))` (and equivalents for high/low), independently of `04_causal_analysis.ipynb`. The "log return" language describing `master_dataset.parquet`'s `log_return` column and `fwd_return_1d` target (Section 3.5/3.6 and Chapter 4) is accurate and was left unchanged — only the Section 3.3/§1 event-study description, which refers to `04_causal_analysis.ipynb`'s `spy_return`, was wrong.

**Alternatives considered:** Leave Section 3.3 as-is and only use correct terminology going forward in Section 3.4; or correct both the frozen section and the source document immediately.

**Reasoning:** Per the Project Director's decision, corrected immediately rather than deferred, since an examiner cross-checking Section 3.3 against the notebook would find the same discrepancy, and leaving a known-wrong statement in a "frozen" section for the sake of not reopening it would misrepresent the actual computation.

**Also discovered but not requiring a stop (handled directly in the Section 3.4 draft, since that section was not yet frozen):** (1) `vix_regime` is a fixed absolute threshold (`vix > 20`), not "VIX exceeded its 12-month rolling mean" as the old, unrevised Section 3.4 text claimed. (2) The DAG used for the overall causal estimate (Cell 10) includes `spy_return_lag1 → treatment` as an edge; the DAGs used for the five per-event-type estimates (Cell 15) and the FinBERT robustness estimate (Cell 17) omit that edge, modelling only `vix_regime` as a common cause of treatment and outcome in those sub-analyses. (3) `docs/architecture/car_formula.svg` still labels the event-study estimator "Market-model expected return" with a "120-day pre-window" — stale relative to the Section 3.3 correction above; flagged for a follow-up fix, not actioned in this pass since it is an architecture diagram, not dissertation text.

**Files updated:** `03_methodology.md` §1 (added return-type note), dissertation `.docx` Section 3.3 paragraph 1 (unpacked working copy only, not yet repacked).

**Explicitly NOT changed:** `04_causal_analysis.ipynb`, `05_feature_engineering.ipynb`, any frozen dataset, `master_dataset.parquet`/`feature_matrix.parquet`'s genuine log-return columns, or Sections 3.5/3.6/Chapter 4 text (all correctly describe log returns for their own, different, notebook).

**Reversible:** Yes — purely a terminology correction; would only need revisiting if `04_causal_analysis.ipynb` itself were changed to use log returns in a future re-run.

---

### 2026-07-07 — Random Forest (feature-importance tool) confirmed non-reproducible from the current repository; Section 3.6 to describe only the four reproducible models

**Decision:** Describe only `Baseline_LASSO`, Event_LASSO, XGBoost, and LightGBM in dissertation Section 3.6 (Predictive Modelling) — the four models actually trained by `06_model_training.ipynb`/`07_model_evaluation.ipynb`. Random Forest's feature-importance role is treated as a frozen, currently-non-regenerable artefact and stated as such, not described as if a live training step for it exists.

**What was found:** While verifying Section 3.6 against the notebooks (prompted by the Project Director's specific request to check line-by-line for lingering references to superseded designs), `model_contract.md` and `03_methodology.md` §5 were found to list Random Forest as an approved MCP v1.0 model (RQ2 feature-importance tool, full 95-feature set). No notebook (`05`–`08`) or script (`scripts/`, `src/`) currently in the repository contains code that trains a Random Forest model. `07_model_evaluation.ipynb` explicitly states "SHAP/importance analysis itself is Notebook 08's job here, not repeated in this notebook"; `08_results_visualisation.ipynb` only *reads* pre-existing `reports/model_comparison/feature_importance.parquet` and the three event-model SHAP parquets — it computes none of them, and there is no `shap_values_random_forest.parquet` at all. A trained model artefact, `models/event/random_forest_importance_tool.joblib`, does exist on disk, alongside `feature_importance.parquet` (95 rows, matching FES v1.0) — so the model was trained at some point, but the code that trained it is not present in any notebook currently in the repository, most likely because it was not carried into the four RQ3-comparison notebooks rewritten during the Mission 05-07 Reproducibility Rebuild (`10_decision_log.md`, 2026-07-06 entry), which focused on reproducing the primary RQ3 model roster and did not re-include the separate RQ2-only Random Forest step. (The one Random Forest result actually reported in prose, `09_results_log.md` line 66, is unrelated — it is explicitly sourced from `data/processed/feature_metadata.parquet`, a pre-freeze, 52-feature-pipeline artefact, not the current 95-feature FES v1.0 matrix.)

**Alternatives considered:** (a) Describe Random Forest in Section 3.6 as the contract states, with a footnote about reproducibility. (b) Pause the dissertation mission and write the missing Random Forest training step into a notebook before drafting Section 3.6. (c) Describe only the four models the current notebooks actually reproduce, and log the gap as a named limitation rather than silently describing an unreproducible step as if it were live.

**Reasoning:** Per the Project Director's decision, (c) was adopted. Describing Random Forest as a routinely reproducible part of the pipeline when its training code does not exist anywhere in the repository would misrepresent the project's actual reproducibility to an examiner re-running the notebooks. Closing the gap by writing new training code was judged out of scope for a documentation/dissertation-consistency mission and risks changing a frozen artefact (`feature_importance.parquet`) without a governance-version bump; it is left as an explicit, named limitation (`11_limitations.md` L15) with two possible remediation paths (restore the training step, or formally re-scope Random Forest out of the frozen contract) for the Project Director to close before submission.

**Files updated:** `11_limitations.md` (new L15 entry).

**Explicitly NOT changed:** `model_contract.md`, `03_methodology.md` §5 (both still list Random Forest as approved — this entry does not retract that governance decision, it only documents that the artefact is currently frozen/non-regenerable), `feature_importance.parquet`, `random_forest_importance_tool.joblib`, or any notebook.

**Reversible:** Yes — if the Random Forest training step is restored to a notebook and verified to reproduce the frozen `feature_importance.parquet` exactly (in the same style as the Mission 05-07 reproduction checks), this entry and `11_limitations.md` L15 should be updated to reflect the closed gap, and Section 3.6 could then describe Random Forest as a reproducible step.

---

### 2026-07-07 — Dissertation Section 4.7 corrected: residual diagnostics do exist for Event_LASSO/XGBoost/LightGBM, not only Baseline_LASSO

**Decision:** Correct the already-frozen dissertation Section 4.7 (Residual Diagnostics) to report persisted residual diagnostics for all four MCP v1.0 models, not only `Baseline_LASSO`.

**What was found:** While assembling evidence for Chapter 5 (Discussion), re-reading `reports/model_comparison/statistical_tests.json` in full showed that each of the three event-enhanced models carries its own `residual_diagnostics_test` block (Event_LASSO: Durbin-Watson 1.9826, heteroskedasticity correlation 0.1980, kurtosis 18.0056, skewness 0.6366, residual mean +0.000356; XGBoost: 2.0240 / 0.2339 / 17.5576 / 0.7675 / +0.000796; LightGBM: 2.0795 / 0.2734 / 16.6002 / 0.6979 / +0.000653), distinct from `reports/baseline/baseline_metrics.json`'s `Baseline_LASSO` block used previously. This was missed when the Phase 1 Results Master Sheet was built (only `baseline_metrics.json` was checked for this row), and the resulting Section 4.7 text — which stated "no equivalent diagnostics are persisted for Event_LASSO, XGBoost, or LightGBM" — was consequently factually wrong about a claim already frozen in Chapter 4.

**Alternatives considered:** (a) Leave Section 4.7 as-is and describe the gap only for Baseline_LASSO going forward, treating the newly-found data as out of scope for this pass; (b) correct Section 4.7 immediately to include the verified diagnostics for all four models, and update the Results Master Sheet accordingly before proceeding to Chapter 5.

**Reasoning:** Per the Project Director's decision, (b) was adopted — Chapter 5's Discussion needed to interpret residual diagnostics accurately, and knowingly carrying forward a false "does not exist" claim into a chapter that would then discuss it risked compounding the error. The correction is additive only (no existing Baseline_LASSO numbers changed) and does not require reopening any other part of the already-approved Chapter 4.

**Files updated:** Dissertation `.docx` Section 4.7 (unpacked working copy only, not yet repacked).

**Explicitly NOT changed:** Any notebook, `statistical_tests.json`, `baseline_metrics.json`, or any other frozen dataset/model artefact — this is a documentation-accuracy correction only, not a re-analysis.

**Reversible:** N/A — this is a factual correction, not a discretionary decision; it should not be reverted.

---

### 2026-07-08 — Final Appendix Cleanup & Architecture SVG Enhancement mission

**Decision:** Two independent pieces of work, both approved and scoped in advance. (1) Dissertation: removed `project_pipeline.svg` (Appendix Figure A7) entirely from `7030DATSCI-08_07_2026.docx` per explicit instruction that it is repository/notebook documentation rather than dissertation evidence and was not earning its page cost; replaced its Section 3.1 sentence with a short methodology-only sentence; corrected the pre-existing "Figure A1. A1." / "Figure A2. A2." duplicate-numbering bug in Appendix A captions (A1–A6) to the standard "Figure A1." form. (2) Repository: `car_formula.svg` was rewritten — both its factual content and its visual style — since it was the one canonical diagram never rebuilt from scratch in the 2026-07-06 cleanup (it was "corrected in place" that day, which is why it kept black borders/gray arrows/no italic subtitle while the other five got the steel-blue house style). `data_lineage.svg` gained a `causal_estimates.parquet` branch and two label/spacing fixes. `rq_traceability.svg` had two connector polylines rerouted around box edges — they previously ran directly through the "SHAP / RF Importance" and "Statistical Comparison" box text.

**What was found in `car_formula.svg` (verified against the frozen dissertation text directly, not from memory):** it stated `E[Rₜₐ]` was a "Market-model expected return" and referenced a "120-day pre-window estimation," both contradicting the frozen Section 3.3 text, which explicitly uses a constant-mean-return (mean-adjusted) design — "rather than being estimated from a regression against a separate market-proxy series" — with a 252-to-21-trading-day estimation window. The diagram also labelled `Rₜₐ` as a "log return," but Section 3.3's actual formula inputs are simple (percentage) returns (a distinction the project corrected in dissertation text back on 2026-07-07 — see that entry — but which had never been propagated to this diagram). Additionally, the diagram used a `CARₑₐ`/`Rₜₐ` two-index (event, asset) notation that does not appear anywhere in the dissertation, which uses the simpler single-index `AR_τ = R_τ − μ̂` / `CAR(t₁,t₂) = Σ AR_τ` notation instead.

**Alternatives considered:** (a) Patch only the two explicitly-named issues (market-model wording, 120-day window) and leave the two-index notation and the return-type label as-is; (b) rebuild the equation section entirely to match the dissertation's actual notation, fixing all discovered inconsistencies, not only the two named ones.

**Reasoning:** The mission brief's own instruction was to "update the SVG so it matches frozen Section 3.3 exactly" — a standard that the two-index notation and the log/simple-return mislabel also fail, independently of the two issues explicitly named in the brief. Patching only the named issues would have left a diagram that matches frozen text in two respects while still contradicting it in two others. Full alignment to the dissertation's own notation was judged the more defensible reading of "matches ... exactly."

**Additional judgment call — `car_formula.svg` was not inserted into the dissertation.** Per the mission brief, correcting the diagram does not imply using it. Recommendation logged in `docs/architecture/README.md`: since the dissertation already states both equations in full (prose + two display equations in Section 3.3), the diagram is not adding new information — if used at all, it belongs in the Appendix as a supplementary visual reference, not in Chapter 3 body text where it would duplicate the existing equations. No placement decision has been made; this is left open for the dissertation author.

**Files updated:** `reports/dissertation/7030DATSCI-08_07_2026.docx` (Appendix Figure A7 removed, Section 3.1 sentence replaced, Appendix A1–A6 caption numbering fixed); `docs/architecture/car_formula.svg` (rewritten); `docs/architecture/data_lineage.svg` (branch added, two label fixes); `docs/architecture/rq_traceability.svg` (connector reroute); `docs/architecture/README.md` (table and mission history updated).

**Explicitly NOT changed:** Any methodology, statistic, finding, conclusion, reference, or table in the dissertation; `data_lineage.svg`'s and `modelling_flow.svg`'s existing placement in Chapter 3 (kept exactly as approved); `project_pipeline.svg`, `research_governance.svg` (content unchanged, style already consistent); the two pre-existing duplicate `w14:paraId` values noted in an earlier mission (still present, still out of scope).

**Reversible:** Yes — `car_formula.svg`'s Appendix placement is an open, reversible decision (see above); `project_pipeline.svg` could be re-approved for dissertation use in a future mission if a reviewer judges the pipeline-overview context is worth its page cost after all.

---

### 2026-07-13 — GDELT full historical backfill: integrate as a continuous daily confounder, not one catalogue event per calendar day

**Decision:** `data/raw/gdelt_daily_summary.parquet` (the newly-completed 2015–2025 daily GDELT aggregate — event count, mean Goldstein scale, mean tone, derived from a 25GB raw event stream) will be merged into the pipeline as a **continuous daily control series joined onto the trading calendar**, the same role VIX already occupies, rather than being expanded into one discrete "event" catalogue row per calendar day as drafted in `03_event_detection_revised.ipynb`'s current GDELT section. A genuine discrete "GDELT shock event" class (e.g. top/bottom percentile of day-over-day Goldstein/tone change vs. a rolling baseline) is deferred, not rejected — it may be added later as a separate, explicitly-justified event class if RQ1 benefits from it, but is out of scope for the immediate fix.

**Context — why this needed a decision now:** Notebooks 01/02 were rewritten this session to perform the genuine GDELT historical backfill that `future_improvements.md` items 26/27 and dissertation §5.4 ("Where This Could Go Next") both named as the natural next step beyond the previously-frozen 5-day proof-of-concept sample. This is a real capability upgrade, not a cosmetic change. However, `03_event_detection_revised.ipynb` (drafted in parallel, not yet finalised) consumes the new daily summary by minting one new catalogue row per calendar day (`doc_type = 'gdelt_daily'`, `event_type = 'geopolitical'`), then runs each row through the same `sentiment_score ≥ 0.70` high-impact filter used for FinBERT-scored document titles.

**What was found:** Direct inspection of `gdelt_daily_summary.parquet` (4,018 rows, 2015-01-01 to 2025-12-31) shows `gdelt_mean_goldstein` has mean 0.546, std 0.178, min −1.77, max 1.49. After the existing `/10` scaling to [−1, 1], the resulting `sentiment_score` (its absolute value) never exceeds ~0.18 for any day in the entire series — reaching the 0.70 threshold would require a raw daily mean magnitude of ≥7, which never occurs because each day's mean is itself an average over roughly 87,000–278,000 raw GDELT records, and averaging at that scale collapses variance by the law of large numbers. Concretely: **0 of 4,018 days would ever be flagged high-impact** under the current design. The practical effect is that the entire 25GB backfill, as currently wired, would have zero influence on the high-impact event set, the event study, or feature engineering — it would silently do nothing, despite the ingestion effort already spent. A second, independent problem: minting a catalogue row for every calendar day (including non-trading weekends) conflates a continuous background-risk level with a discrete, dateable event, which is the premise the entire event-study/CAR methodology (§3.3) depends on — it would also inflate "geopolitical" catalogue counts by ~4,018 rows, dwarfing every other category and invalidating the counts currently reported throughout dissertation Chapter 4.

**Alternatives considered:** (a) Keep the per-day catalogue-event design as drafted, and separately lower/rework the high-impact confidence threshold specifically for GDELT rows so some days can qualify. (b) Merge the daily aggregate as a continuous confounder/feature only, mirroring VIX, with no catalogue-row representation at all. (c) Do (b), plus a separately-justified discrete "GDELT shock" event class built on an anomaly rule (percentile of day-over-day change) rather than reusing the absolute FinBERT-style threshold.

**Reasoning:** (a) was rejected because a GDELT-specific threshold would be an ad hoc carve-out with no principled cutoff, and still wouldn't resolve the deeper issue of treating every calendar day as an "event." (b) was adopted as the immediate fix because it matches how the project already treats VIX — a continuous, always-available daily series used as a feature/confounder rather than a discrete event source — and because it lets the real GDELT signal actually reach feature engineering (§3.5) and, if wired in, the causal model, instead of being silently neutralised by a mismatched threshold. (c) is recorded as a legitimate future extension rather than discarded, since a genuinely rare, anomaly-defined GDELT shock event is methodologically defensible and distinct from the current per-day approach — but building and validating that anomaly rule is separate work from the immediate correctness fix, and is deferred to avoid scope creep this close to the Phase 1 rerun.

**Files to be updated in Phase 1 (not yet changed by this entry):** `03_event_detection.ipynb` (finalise the revision, replacing the per-day catalogue approach), `dataset_contract.md`, `dataset_version.md`, `05_data_dictionary.md`, `06_feature_dictionary.md` (new confounder/feature, version bump), `11_limitations.md` (retire the "5-day sample" limitation; add the daily-averaging/smoothing caveat instead).

**Explicitly NOT changed by this entry:** No notebook code, no frozen parquet artefact, and no dissertation text — this entry records the design decision only; implementation is Phase 1.

**Reversible:** Yes — this is a design choice about *how* GDELT enters the pipeline, not a claim about the data itself. If a validated discrete shock-event definition (alternative (c)) is developed later, it can be layered on top of the continuous-confounder treatment without contradicting this decision.

---

### 2026-07-13 — DoWhy/networkx/pandas compatibility shim added to `04_causal_analysis.ipynb`

**Decision:** Add a small, explicitly-documented compatibility patch cell (`dowhy-compat-patch`) immediately after the imports cell in `04_causal_analysis.ipynb`, restoring two functions dowhy 0.8 expects under names that networkx and pandas have since deprecated or removed, rather than changing the installed package versions in the working environment.

**What was found:** `04_causal_analysis.ipynb` Section 4 (DoWhy Causal Model) failed with `AttributeError: module 'networkx.algorithms' has no attribute 'd_separated'`. Investigation of the actual working environment (`~/Devops/env`, Python 3.14.6) found `dowhy 0.8` installed against `networkx 3.6.1` and `pandas 3.0.3` — despite `requirements.txt` specifying `dowhy>=0.11.1`. The mismatch exists because **no released dowhy version supports Python 3.14**: even the newest, 0.14, declares `Requires-Python <3.14`, so pip silently fell back to 0.8 (2022), the oldest release without an explicit upper bound. Patching the first break (networkx renamed `d_separated` → `is_d_separator` around v3.3) was verified to fix `identify_effect()`, but immediately surfaced a second, independent break in `estimate_effect()`: `dowhy/causal_estimators/regression_estimator.py`'s `RegressionEstimator._estimate_effect()` does `self.model.params[0]` — bare integer indexing into a statsmodels `params` Series indexed by parameter name. Modern pandas (≥2.x) no longer falls back to positional access for this and raises `KeyError` instead of silently doing the old (deprecated) thing. A full source search (`grep -rn` across the installed `dowhy` package) confirmed this pattern appears in exactly one file, two lines, both inside this one method — which is the base implementation for `backdoor.linear_regression`, the estimator this entire notebook uses for every primary/per-category/robustness estimate. Both patches were tested end-to-end outside the notebook first (`identify_effect`, `estimate_effect`, and all three refutation methods — random common cause, placebo treatment, data subset — against the real `causal_df`) and confirmed to pass cleanly before being added to the notebook.

**Alternatives considered:** (a) Downgrade `networkx` in the working venv to a version predating the `d_separated` rename — would fix the first break but not the second (pandas-related) one, and only partially addresses the root cause. (b) Set up a dedicated Python 3.11/3.12 virtual environment so `dowhy>=0.11.1` (already specified in `requirements.txt`) can actually be installed — the durable, "do it properly" fix, since a maintained dowhy release would already handle both the networkx rename and the pandas indexing change. Not available in this environment: `/Library/Frameworks/Python.framework/Versions/` contains only 3.14, and neither `brew`, `conda`, nor `pyenv` are installed to provision an older interpreter without installing new system software first. (c) Patch the two specific breaks in place via a documented compatibility cell, changing no installed package and no system software.

**Reasoning:** (c) was adopted as the immediate fix because it is fully reversible, requires no environment or system changes, and was verified — not assumed — to resolve the complete pipeline (identification, estimation, and all three refutation tests) rather than just the symptom originally reported. (b) remains the better long-term fix and is recorded here as the one to prefer once an older Python interpreter is available in this environment; at that point `dowhy>=0.11.1` should be installed for real and this shim cell removed, since it is a workaround for a specific old dowhy release, not a permanent feature of the pipeline.

**Files updated:** `notebooks/04_causal_analysis.ipynb` (new `dowhy-compat-patch` code cell inserted after `imports`; no other cell changed, no methodology or numerical logic altered).

**Explicitly NOT changed:** `requirements.txt` (still correctly specifies `dowhy>=0.11.1` — the shim does not change what *should* be installed, only compensates for what currently *is*), the working venv's installed package versions, any causal-model specification, DAG, or estimator choice.

**Reversible:** Yes, and expected to be reverted — once a Python ≤3.13 environment with `dowhy>=0.11.1` installed is available, this shim cell should be deleted rather than kept alongside a fixed dependency (keeping both would be redundant, not harmful, but the shim's whole purpose is to be temporary).

---

### 2026-07-13 — `04_causal_analysis.ipynb` executed end-to-end; interpretation cells corrected against new (not frozen v1.0) output — RQ1 evidence base has genuinely changed

**Decision:** Update `04_causal_analysis.ipynb`'s markdown interpretation cells to describe this session's actual execution output, rather than continuing to describe the dissertation's previously-frozen v1.0 numbers as if unchanged. No code cell was modified as part of this pass (only the `dowhy-compat-patch` cell and the `boxplot` `tick_labels` fix from earlier the same day) — this entry covers documentation-only corrections following a real, successful notebook run.

**What was found, comparing this run's printed output against the notebook's own prose:** (1) `car_results.parquet` now has 264 rows, not the frozen 1,796 — a consequence of Notebook 03's current 916-document economic pre-filter and 344-event high-impact subset, not a bug in this notebook. (2) The Section 3 significance-test interpretation flatly claimed "geopolitical events show the clearest significant CAR signal," but this run's actual printed table shows **no event type reaches significance at even p<0.10** (geopolitical p=0.293, monetary closest at p=0.116) — a genuine null result the old prose directly contradicted. (3) Section 4's interpretation cited `09_results_log.md`'s old CI (+0.0051, [+0.0014,+0.0087]) as if it were this run's result; this run's actual overall causal estimate is +0.005601 (95% CI [+0.002295, +0.008907], p=0.0009) — similar in spirit but a different, newly-computed number. (4) The notebook's "Lexicon" vs. "FinBERT" labelling throughout (title cell, Sections 4 and 6) is stale: there is no lexicon-scored sentiment anywhere in the current pipeline (Sentiment Engine Freeze v1.0, FinBERT-only). What Section 4 actually estimates on is FinBERT+FOMC combined daily sentiment; what Section 6 calls the "FinBERT robustness check" is FinBERT-only (APP events, excluding FOMC) — the two estimates differ because of this compositional difference, not a lexicon-vs-FinBERT domain mismatch as originally framed. (5) Section 5's per-event-type causal estimates had **no interpretation cell at all**, despite two of the five (`geopolitical`, `energy`) resting on `n_events=1` — a single non-neutral-sentiment day each, meaning their nominally "significant" confidence intervals are a spurious-precision artefact of an almost-degenerate sample, not a reliable finding. (6) The summary cell's printed "KEY NOTES" contains two stale claims baked into a code cell's f-string (the "27% non-neutral" lexicon stat, and "GDELT excluded... 5-day sample insufficient" — the latter contradicted by the same day's GDELT backfill decision). (7) The handoff cell explicitly claimed "did not re-run the notebook or change any reported number," which was accurate for the previous (2026-07-06) alignment-only pass but is now false.

**A genuine positive finding surfaced by this same comparison, not just corrections:** the `monetary` per-category causal estimate in this run (+0.007094, 95% CI [+0.002577, +0.010952]) closely replicates the frozen v1.0 dissertation figure (+0.0075, CI [+0.0033, +0.0115]) despite a completely rebuilt upstream pipeline (916 vs. 11,570 APP documents, FinBERT-only vs. lexicon-primary sentiment, current vs. historical GDELT scope). This is flagged in a new interpretation cell as a stronger, incidental form of robustness than the notebook's own refutation tests provide, since it wasn't run as a deliberate check.

**Alternatives considered:** (a) Leave the interpretation cells as-is, since the underlying methodology (event study design, DAG specification, refutation tests) did not change — only the input data did. (b) Correct every interpretation cell against this run's real output, including inserting a new cell where none existed for a real statistical caveat (Section 5's n=1 problem), and flagging (without editing) the two stale code-cell print statements via an adjacent markdown note.

**Reasoning:** (b) was adopted, consistent with the correction pattern already applied this session to Notebooks 02 and 03: an interpretation cell that contradicts its own notebook's printed output (the Section 3 case) is a correctness problem, not a style one, and describing a "significant" per-category estimate built from one data point without any caveat risks that number being cited uncritically downstream. This is documentation-only — no dataset, model, or frozen artefact outside this notebook's own two output parquet files was touched, and those two files were regenerated by the notebook's own code, not hand-edited.

**Open question this entry does NOT resolve:** whether these newly-regenerated `car_results.parquet`/`causal_estimates.parquet` should replace the dissertation's frozen v1.0 versions of the same files, and how the changed RQ1 headline results (event study now null; monetary causal effect replicates; two per-category estimates are unreliable) should be reflected in the dissertation text. This is explicitly left for the Project Director, per the same governance boundary this log has used for every previous re-derivation decision.

**Files updated:** `notebooks/04_causal_analysis.ipynb` (title cell; `md-dowhy`, `interp-significance`, `interp-causal`, `interp-robustness`, `handoff-header` cells rewritten; two new markdown cells inserted — `interp-by-type` after `causal-by-type`, `interp-summary-notes` after `summary-cell`).

**Explicitly NOT changed:** Any code cell other than the two same-day compatibility fixes already logged above; `data/processed/car_results.parquet` or `causal_estimates.parquet` (regenerated by the notebook's own execution, not hand-edited); the dissertation text; any other notebook.

**Reversible:** Yes — these are markdown corrections describing real code output; if the notebook is re-run again with different upstream data, these interpretation cells will need the same kind of check-against-output pass again, not a reversion of this entry.

---

### 2026-07-13 — `master_dataset.parquet` v1.1 rebuilt as a dedicated freeze step; `05_feature_engineering.ipynb`'s stale hardcoded row/split-count asserts corrected

**Decision:** (1) Rebuild `data/processed/master_dataset.parquet`, which no longer existed on disk, as its own standalone script (`scripts/build_master_dataset.py`) rather than having `05_feature_engineering.ipynb` re-derive the merge itself. (2) Fix `05_feature_engineering.ipynb`'s `validation-checks` cell, which asserted the exact old row count (2,511) and split counts (`{'train': 1761, 'test': 750}`) — both guaranteed to differ now that `car_results.parquet`'s date coverage has changed — replacing them with structural sanity checks (non-empty, both splits present, both above a minimum size) instead of pinned historical values.

**What was found:** `05_feature_engineering.ipynb`'s first cell failed with `FileNotFoundError` for `master_dataset.parquet`. A full-repository search (`to_parquet` calls referencing this filename, across every notebook and script) found **no code anywhere that builds this file** — `dataset_version.md` documents it as a "frozen v1.0" artefact created 2026-07-04, but its construction was evidently done once, outside any notebook currently in this repository, and the file itself was lost along with the rest of `data/processed/` earlier in this session's pipeline rebuild. `dataset_contract.md` term 1 requires a schema/content change to go through "a dedicated freeze step," and term 2 explicitly prohibits `05_feature_engineering.ipynb` from independently re-deriving the raw price/VIX/macro/sentiment merge ("would create two divergent ground truths") — so the fix is a standalone rebuild script, not a patch inside Notebook 05.

**Rebuild details, verified against `dataset_version.md`'s documented v1.0 schema and validation report:**
- **Adjusted OHLC:** confirmed `spy_ohlcv.parquet`'s `adj_close` is byte-identical to `spy_returns.parquet`'s `spy_close` (0.0 max abs diff). Open/high/low are scaled by the same per-day `adj_close/close` ratio so all four price columns share one consistent adjustment basis — using raw high/low alongside an adjusted close would have distorted `bb_width`/`bb_position`/`intraday_range` in Notebook 05, especially for 2015-16 dates where the cumulative dividend adjustment is largest.
- **Macro:** forward-filled from `macro_indicators.parquet` (already business-day frequency, 2,870 rows) onto the SPY trading calendar (2,765 rows) — zero missing values after fill, matching the documented policy.
- **Sentiment/event:** 0.0-filled from `daily_sentiment.parquet` (Notebook 03's current output), per the documented "true zero, not missing" policy.
- **GDELT:** merged directly from `gdelt_daily_risk.parquet` (the full 4,018-day series) rather than through `daily_sentiment.parquet` (which only has rows for days with a catalogued APP/FOMC event) — using the latter would have left GDELT sparse on most calendar days despite full daily data now existing. `dataset_version.md`'s "5-day sample only" caveat for these columns is now stale and should be updated when this version is formally reviewed.
- **QQQ/GLD/TLT:** *not* reconstructed — no `data/raw` source for these exists in this environment. Documented as an explicit omission (script docstring + this entry), not fabricated as placeholder data.
- **Split boundary:** train ≤ 2022-12-30, test ≥ 2023-01-03, per `dataset_contract.md` term 5 (unchanged).
- **Validation:** all of `dataset_version.md`'s v1.0 checks re-run and passed — 0 duplicate dates, monotonic increasing, max gap 4 calendar days, 0 missing values outside the two structurally-expected boundary NaNs, 0 leakage mismatches (`fwd_return_1d[t] == log_return[t+1]`), last-row `fwd_return_1d` is NaN. **Row and split counts matched the documented v1.0 figures exactly** (2,765 rows; 2,014 train / 751 test) — the underlying SPY trading calendar has not changed, only the event/sentiment composition feeding into it.

**A second, genuine (non-code) finding surfaced by running Notebook 05 against the rebuilt file:** the `labour` sentiment column — and its downstream `labour_event_day` feature — is now **constant at exactly 0.0** across the entire feature matrix. This is not a bug in the rebuild: `events_tagged.parquet` confirms all 103 `labour`-tagged documents in the current catalogue were scored FinBERT-`neutral` (`sentiment_numeric = 0.0`), so the aggregated daily mean is legitimately zero on every day, including days with labour events. Notebook 05's own `constant_columns` check correctly flags this and sets `validation_status = 'FAIL'` — that check was left untouched (it is working correctly, not a bug), and the finding is left for the interpretation-cell pass this project has been doing after each notebook run, not silently patched around. It has a real downstream consequence worth flagging now: the scaling-parameters cell computes `std = train_split[feat].std(ddof=0)`, which will be exactly 0 for `labour`/`labour_event_day` — any consuming notebook applying `(x - mean) / std` to these two features will divide by zero.

**Alternatives considered (for the missing file):** (a) Have `05_feature_engineering.ipynb` build the merge itself, matching Notebook 04's pattern of building its own local frame. Rejected — directly contradicts `dataset_contract.md` term 2. (b) Treat this as blocking and stop for the Project Director to rebuild manually. (c) Rebuild via a dedicated script, matching this session's established pattern for `scripts/run_finbert_economic.py`, with full validation against the documented v1.0 checks before use.

**Reasoning:** (c) was adopted — it respects the contract's "dedicated freeze step" language, is fully auditable (the script is committed, not a one-off shell command), and was validated line-for-line against `dataset_version.md`'s own documented checks before `05_feature_engineering.ipynb` was allowed to consume it.

**Files updated:** `scripts/build_master_dataset.py` (new); `data/processed/master_dataset.parquet` (rebuilt); `notebooks/05_feature_engineering.ipynb` (`validation-checks` cell only — hardcoded row/split-count asserts replaced with structural checks; no other cell changed, no markdown/interpretation cells touched per this session's explicit instruction to fix code only and defer interpretation to a post-run pass).

**Explicitly NOT changed:** `05_feature_engineering.ipynb`'s `constant_columns` detection logic (correct, left as-is); any markdown/interpretation cell in Notebook 05; `car_results.parquet`, `daily_sentiment.parquet`, or any other upstream file; `dataset_version.md` itself (still shows v1.0 — a formal version-history entry and GDELT-caveat update are owed but not made in this pass, since this rebuild was executed as a blocking-error fix, not a scheduled dataset review).

**Reversible:** Yes — `scripts/build_master_dataset.py` can be re-run at any time to reproduce `master_dataset.parquet` deterministically from the same inputs; the row/split-count check change is a strictly safer replacement (it still catches an empty or degenerate matrix, just not a matrix that legitimately differs in size from the old frozen run).

---

### 2026-07-13 — `06_model_training.ipynb`: validation gate scoped to Market-category features; stale pre-rebuild `reports/baseline/` files archived

**Decision:** (1) Relax `06_model_training.ipynb`'s `load-data` cell, which refused to train on any `feature_matrix.parquet` whose overall `validation_status != 'PASS'`, to instead check specifically whether the constant/duplicate columns responsible for that FAIL fall within the 27 Market-category features `Baseline_LASSO` actually reads. (2) Move `reports/baseline/baseline_metrics.json` and `baseline_predictions.parquet` — leftover outputs from the 2026-07-05 run, predating this session's pipeline rebuild — to `models/archive/pre_2026-07-13_pipeline_rebuild/reports_baseline/`, since leaving them in place would have caused the notebook's "only write if the file doesn't already exist" save guard to keep the stale metrics/predictions while writing a freshly-trained model object next to them — a real model/metrics mismatch, not a hypothetical one.

**What was found, checking the notebook before running it (matching this session's `05_feature_engineering.ipynb` pattern of catching blocking issues pre-emptively):** `feature_matrix_validation.json` correctly reports `validation_status: FAIL` because of the `labour`/`labour_event_day` constant-column finding already logged in this session's Notebook 05 entry. `06_model_training.ipynb`'s original gate (`assert fm_validation['validation_status'] == 'PASS'`) would have refused to train at all — even though `Baseline_LASSO` is contractually restricted to the 27 Market-category columns and never reads `labour` in any form. Separately, `models/baseline/` did not exist on disk while `reports/baseline/` still held two files from a different machine (Linux, Python 3.10.12, `train_rows: 1761` — the old frozen row count) — a genuine risk of a freshly-trained model paired with metrics describing a different, older model.

**Alternatives considered (for the validation gate):** (a) Leave the gate as a hard global block and require the Project Director to explicitly clear it each time any column anywhere in the matrix is imperfect. (b) Remove the gate entirely. (c) Scope the gate to the columns the specific model actually reads, printing a clear, non-hidden note when the overall matrix has a real issue elsewhere.

**Reasoning:** (c) was adopted. (a) would make this notebook permanently unrunnable for a data characteristic (FinBERT scoring one category uniformly neutral) that has nothing to do with what this model reads — not a meaningful safety property, just a blocked notebook. (b) would remove a genuinely useful check: if a constant or duplicate column ever appeared *within* the 27 Market features, that should still block training, since it's a real risk to `Baseline_LASSO` specifically. (c) preserves the actually-relevant protection while explaining rather than hiding the unrelated FAIL.

**Verification:** the full notebook was replayed end-to-end (both fixes applied) outside Jupyter before being handed back — clean run, `alpha_selected` still drives all 27 coefficients to zero (matching the frozen result's qualitative finding), test RMSE (0.009631) and Dir_Acc (0.5747) closely track the old frozen numbers despite the different train-split size (1,727 vs. 1,761 rows), consistent with a model that reduces to the training-split mean regardless of which rows are in that split. Output files written by this verification replay were deleted afterward so the user's own run produces them.

**Files updated:** `notebooks/06_model_training.ipynb` (`load-data` cell only); `reports/baseline/baseline_metrics.json`, `reports/baseline/baseline_predictions.parquet` moved to `models/archive/pre_2026-07-13_pipeline_rebuild/reports_baseline/` (not deleted).

**Explicitly NOT changed:** The model's hyperparameters, training logic, or evaluation metrics; the Market-feature contract check (`assert list(X_train.columns) == MARKET_FEATURES`) further down the same notebook, which was already correctly scoped and untouched.

**Reversible:** Yes — the archived files can be moved back if ever needed for historical comparison; the validation-gate change only narrows what blocks training, it does not remove the underlying check for the case that actually matters to this model.

---

### 2026-07-13 — `07_model_evaluation.ipynb`: NaN-scaling bug fixed, missing SHAP/prediction outputs rebuilt, `event_model_metadata.json` reconstructed from the dissertation; `08_results_visualisation.ipynb` needed no code changes

**Decision:** Fix `07_model_evaluation.ipynb` in three parts — (1) the same validation-gate relaxation as `06_model_training.ipynb`, but paired with an actual numerical fix this time since this notebook, unlike Notebook 06, reads all 95 features including the constant `labour`/`labour_event_day` columns; (2) a genuine crash bug in the `scale()` helper, where `(x - mean) / std` produces `NaN` for any zero-variance column; (3) a new section computing and persisting `event_model_predictions.parquet` and the three `shap_values_*.parquet` files, none of which any current notebook actually produces despite `08_results_visualisation.ipynb` requiring them as inputs. Also reconstructed `models/event/event_model_metadata.json` (XGBoost/LightGBM's recorded hyperparameters, required for this notebook's deliberately-non-random refit) from the dissertation's own Table 7, since the original file exists nowhere in the repository or its archive.

**What was found:** (1) `feature_matrix_validation.json`'s `validation_status: FAIL` (the `labour` constant-column finding, logged above) would have hard-blocked this notebook exactly as it did Notebook 06 — but here the fix can't just be "these columns aren't read," because they are. Left unfixed, `scale()` would produce `(0-0)/0 = NaN` for `labour`/`labour_event_day` in every row of `X_train`/`X_test`, silently poisoning `LassoCV`/`XGBoost`/`LightGBM`'s training data. (2) A full-repository search confirmed no notebook or script currently produces `event_model_predictions.parquet` or any `shap_values_*.parquet` — the versions on disk were a stale 2026-07-05 freeze from a different machine, exactly the same pattern as the `reports/baseline/` files found for Notebook 06. (3) `models/event/event_model_metadata.json` — needed to refit XGBoost/LightGBM from their *recorded* hyperparameters rather than re-running `RandomizedSearchCV` (deliberate, documented in this notebook's title cell, since the original search space was never persisted per `future_improvements.md` #18) — does not exist anywhere, including `models/archive/`. It was reconstructed from the dissertation's Table 7 ("Selected hyperparameters for XGBoost and LightGBM (frozen configuration)"), the only surviving authoritative source for these exact values.

**Method used for the new SHAP/prediction computation, taken directly from `reports/statistics/07_event_models_summary.md`'s own documentation of the original computation** (so this is a reproduction of a documented method, not a new design choice): XGBoost/LightGBM's built-in per-prediction contribution APIs (`booster.predict(..., pred_contribs=True)` for XGBoost — note the sklearn wrapper's `.predict()` no longer accepts this kwarg directly in xgboost 3.x, must go through `get_booster()`; `model.predict(..., pred_contrib=True)` for LightGBM, unchanged), and the closed-form linear-model SHAP identity for `Event_LASSO` (`SHAP_i = coef_i × scaled_feature_i`, `base_value = intercept`) — no external `shap`/`numba` dependency, matching the documented original. Each model's SHAP values were verified in the notebook itself to reconstruct that model's own `predict()` output exactly (max abs diff: Event_LASSO 0.0, XGBoost 1.6e-9, LightGBM 9.9e-18) before being trusted.

**A separate, genuine environment blocker found and fixed along the way:** `xgboost` could not even be imported (`libxgboost.dylib` failed to load — missing `libomp`, the OpenMP runtime, not installed on this machine). Fixed with `brew install libomp` (a standard, small, safe scientific-computing dependency; the same category of action as the various `pip install`s already performed this session to test-run notebooks, not a bespoke system change). Without this, `07_model_evaluation.ipynb` would fail at its very first import cell in any environment sharing this Python installation, before any of the above fixes would even matter.

**A genuine data finding surfaced by this run, not a bug:** `Event_LASSO`'s coefficients collapsed to all-zero this run (`n_nonzero_coef: 0/95`), identical to `Baseline_LASSO`. Its predictions are therefore numerically identical to the baseline's, which makes its Diebold-Mariano statistic against the baseline mathematically undefined (`0/0 = NaN`, since the loss-difference series has zero variance) — printed as `DM p=nan` rather than a crash, correctly. This is left for the post-run interpretation pass (matching this session's established pattern for Notebooks 02–05), not fixed here, since it is a legitimate model output, not an error.

**`08_results_visualisation.ipynb` required zero code changes** — it was audited (checked for hardcoded historical asserts, matching the pattern already found in 05/06/07; found none) and replayed end-to-end once Notebook 07's real outputs existed on disk, and it ran cleanly, producing all four publication figures (`08a`–`08d`). Its hardcoded `REPRESENTATIVE_MODEL = 'Event_LASSO'` constant is worth flagging for the interpretation pass, not fixing here: it was a reasonable choice when Event_LASSO's DM p-value (≈0.056) was genuinely the closest of the three candidates to significance in the old frozen run; in this run, Event_LASSO's DM statistic is undefined for the reason above, so panels built around "the representative model" (predicted-vs-actual scatter, rolling IC) will look degenerate (a flat prediction line) purely because this run's Event_LASSO happens to equal the baseline, not because of any code defect in Notebook 08.

**Verification:** Notebooks 07 and 08 were both replayed end-to-end outside Jupyter, in sequence (08 depends on 07's fresh outputs), after every fix above — both passed cleanly. All output files written by these verification replays (models, joblib files, parquet reports, and the four `08a`–`08d` PNGs) were deleted afterward so the user's own run produces them; `event_model_metadata.json` was kept, since it is a genuine reconstructed input, not a test artefact.

**Alternatives considered (for `event_model_metadata.json`):** (a) Re-run `RandomizedSearchCV` fresh on the current `feature_matrix.parquet`. Rejected for this pass — it would produce genuinely different hyperparameters (the search space itself is also lost, so this wouldn't even be a faithful re-run of the original search), which is a larger, separate decision than a bug-fix pass should make unilaterally. (b) Leave the notebook blocked until the Project Director manually supplies the file. (c) Reconstruct `best_params` from the dissertation's already-published, cited Table 7. Adopted (c): it's the only currently-available source that is both authoritative (already peer-reviewed-equivalent, cited in the frozen dissertation text) and exact (not a fresh guess).

**Files updated:** `notebooks/07_model_evaluation.ipynb` (`load-data` cell; two new cells — `compute-predictions`, `compute-shap` — plus a new markdown header; `save-outputs` and `post-save-verification` cells extended); `models/event/event_model_metadata.json` (new, reconstructed); `reports/model_comparison/{model_comparison.parquet, statistical_tests.json, event_model_predictions.parquet, shap_values_event_lasso.parquet, shap_values_xgboost.parquet, shap_values_lightgbm.parquet}` moved to `models/archive/pre_2026-07-13_pipeline_rebuild/reports_model_comparison/` (not deleted); `08_results_visualisation.ipynb` unchanged.

**Explicitly NOT changed:** `feature_importance.parquet` (Random Forest-based) — left at its stale 2026-07-05 values; no training code for that model exists anywhere in the repository (pre-existing limitation, `11_limitations.md` L15), and reconstructing it would be a new implementation, not a reproduction, out of scope for this pass. `08_results_visualisation.ipynb`'s `REPRESENTATIVE_MODEL` constant. Any model hyperparameter, training logic, or statistical test formula.

**Reversible:** Yes throughout — archived files can be restored; `event_model_metadata.json` can be replaced if the true original file is ever recovered; the SHAP/prediction computation can be re-run at any time from the trained models already in memory.

---

### 2026-07-13 — Full-pipeline consistency scan (post-user-run) + repository cleanup, including deletion of the 26GB raw GDELT files

**Decision:** After the Project Director ran all 8 notebooks end-to-end following this session's fixes, performed a full consistency scan (every notebook's reproduction-check cell against its own saved outputs, notebook 8's figure inventory against disk) and a repository cleanup: removed OS/tool junk outright, archived stale pre-rebuild artefacts with no current producer (extending `models/archive/pre_2026-07-13_pipeline_rebuild/`, the same location used earlier this session), and — with explicit Project Director confirmation — deleted `data/raw/gdelt_events.csv` (26GB) and `data/raw/gdelt_events.parquet` (201MB), the raw pre-aggregation GDELT dumps.

**Consistency scan result:** All reproduction-check cells (Notebooks 05, 06, 07) reported zero mismatches against the Project Director's own saved outputs (`REPRODUCTION_EXACT = True` for 05/06; "no mismatches >1e-4/1e-6" for 07's metrics and statistical tests respectively). Notebook 08's own printed figure inventory (26 files) matched `reports/figures/` on disk exactly, with no orphaned old-schema figures remaining. All 8 notebooks show 0 errored cells and 0 un-run cells. The full pipeline, as currently checked into the repository, is internally consistent end to end.

**Cleanup — removed outright (junk, zero information value, fully regenerable):** all `.DS_Store` files (12), all `.ipynb_checkpoints` directories (21), `.pytest_cache`, `src/__pycache__`, `tests/__pycache__`, and `notebooks/reports/` — a duplicate of `reports/tables/` confirmed stale by direct diff (2026-07-06 timestamps, old row counts e.g. `spy_close` n=2765 vs. the real file's current n=2743) and in the wrong location entirely (almost certainly produced by a run launched with the wrong working directory at some point).

**Cleanup — archived, not deleted** (stale 2026-07-05/06 pre-rebuild artefacts, confirmed to have no current producer via `grep` across all notebooks before moving): `reports/statistics/*.md` (five summary documents, including `07_event_models_summary.md`, which this session's Notebook 07 SHAP work cited as its methodology source — its four in-notebook citations were updated to the new archive path and Notebook 07 was re-verified to still replay cleanly afterward), `reports/event/*` (old per-model Mission 07A/07B/07C outputs, superseded by the consolidated Notebook 07), and five stale CSVs from `reports/tables/`. Left deliberately untouched: `notebooks/archive/` and `docs/architecture/archive/` (already correctly archived by earlier sessions), the root `DATSCI7030_Repository_Audit_Report.ipynb` (actively referenced by `docs/research_bible/README.md`, not orphaned), and `reports/08_05_2026_DATSCI7030_Data_Collection_Report.{docx,pdf}` (already flagged in `future_improvements.md` as an intentional keep — a rename was suggested there, not deletion).

**The GDELT raw files:** `data/raw/gdelt_events.csv` (26GB) and `data/raw/gdelt_events.parquet` (201MB) together accounted for 99.8% of the repository's disk footprint (everything else combined: 55MB) and are not read by any current notebook — only the 220KB aggregated `gdelt_daily_summary.parquet` (built from them, per `scripts/build_master_dataset.py` and Notebook 03) is used downstream. Given this is a large, not-quickly-reversible action (regenerating them means re-running the multi-hour bulk GDELT fetch), the Project Director was asked explicitly before deletion rather than this being bundled into the routine junk-file cleanup. Confirmed: delete both. `gdelt_daily_summary.parquet` was verified present and untouched immediately before and after the deletion. Repository size: 25GB → 106MB.

**Files updated:** Numerous deletions/moves as described above; `notebooks/07_model_evaluation.ipynb` (4 citation-path string updates only, re-verified to replay clean).

**Explicitly NOT changed:** Any notebook's methodology, any data/model/report file still in active use, `gdelt_daily_summary.parquet` itself.

**Reversible:** Partially. The junk removal and archival moves are fully reversible (nothing deleted that isn't trivially regenerable, and archives preserve content). The GDELT raw-file deletion is **not** cheaply reversible — recovering them requires re-running the bulk GDELT ingest — which is exactly why it was the one action requiring explicit confirmation rather than being bundled into the rest of this cleanup.

---

### 2026-07-13 — Architecture SVG visual enhancement pass: lighter palette, text-overflow fixes, connector-crossing fix — all verified programmatically, not by eye

**Decision:** Enhance all six canonical `docs/architecture/*.svg` diagrams per three explicit requests: a lighter colour palette, every text label fitting inside its box, and no connector lines crossing each other. Rather than eyeballing the SVG source for these three properties, each was checked with a purpose-built, reusable method: real font-metric text-width measurement (via Pillow, using the actual Arial/Arial Bold `.ttf` files also referenced by the SVGs' own `font-family`) checked against every `<text>` element's containing `<rect>`, and exact line-segment-intersection geometry checked against every `<line>`/`<polyline>`, run before and after every change.

**Colour palette (applied identically across all 6 files, since all 6 use the exact same 9 hex colours):** `#3A6EA5`→`#5B8FC7` (main border/arrow accent), `#EEF3F8`→`#F3F7FC` (box fill), `#111111`→`#1E1E1E` (title text), `#444444`→`#4F4F4F` (subtitle text), `#888888`→`#969696` (italic caption text), `#CCCCCC`→`#DCDCDC` (divider lines), `#7FA6C9`→`#A9C6E3` (secondary/branch connector), `#AAAAAA`→`#B8B8B8` (footnote text). Stroke-widths reduced (`1.4`→`1.1` box borders, `1.8`→`1.5` primary arrows, `1.6`→`1.3` secondary lines) for a lighter visual weight to match. `#FFFFFF` background unchanged.

**Text-overflow audit result:** of 90 text/box pairs checked across all 6 files, exactly 2 genuinely overflowed their box (both pre-existing, not introduced by the palette change) — `data_lineage.svg`'s `app_presidential_documents.parquet` label (needed ≈206px at a normal 11.5pt bold, box was only 163px wide) and `rq_traceability.svg`'s `Baseline_LASSO vs. Event_LASSO/XGB/LGB` subtitle (needed ≈217px, box was 190px, and widening it would have broken column alignment with the two boxes above/below it in the same lane). Fixed respectively by widening the box (163px→225px, with its vertical connector and the merge-bar it feeds repositioned to match the new centre) and by shortening+resizing the text (`Event_LASSO / XGBoost / LightGBM` at 9.5pt, comfortably inside the fixed 190px column width). Re-audit after both fixes: 0 overflows across all 6 files.

**Line-crossing audit result:** `data_lineage.svg` has 5 T-junctions (vertical connectors branching off a horizontal "bus bar" at a right angle) — confirmed geometrically to be branch points, not crossings (the branch line's own endpoint lands on the bus bar, it doesn't pass through it), and left as-is since this is standard, unambiguous flowchart notation, not visual clutter. `rq_traceability.svg` had one genuine crossing: the Causal Analysis→Results and SHAP/RF Importance→Results connector paths crossed each other at (800, 234), neither path's own endpoint being at that point. Root cause: the path starting higher up (Causal Analysis, row 1) used the *inner* routing corridor and the *upper* entry point on the Results box, while the path starting lower (SHAP, row 2) used the *outer* corridor and *lower* entry point — an interleaved, not nested, assignment. Fixed by swapping both together (Causal Analysis → outer corridor + lower entry; SHAP → inner corridor + upper entry), which makes the two paths nest instead of interleave. Re-audit after the fix: 0 true crossings in both files (the 5 intentional T-junctions in `data_lineage.svg` are unaffected and correctly still not flagged). The other 4 diagrams are single-column vertical flows with no other geometry present at the same coordinates — crossings are geometrically impossible there, confirmed by segment count alone (7-9 segments each, all on one vertical line).

**Verification:** all 6 SVGs rasterised to PNG via `rsvg-convert` (installed via `brew install librsvg` — the lightweight, purpose-built option, chosen over `cairosvg`'s heavier native-`cairo` dependency chain which failed to load on this machine) and visually reviewed before this entry was written, in addition to the two programmatic audits above.

**Files updated:** All 6 files in `docs/architecture/` (`car_formula.svg`, `data_lineage.svg`, `modelling_flow.svg`, `project_pipeline.svg`, `research_governance.svg`, `rq_traceability.svg`) — palette and stroke-width changes throughout; `data_lineage.svg` and `rq_traceability.svg` additionally had the specific geometry fixes described above; all 6 version footers updated to reflect this pass. `docs/architecture/README.md` updated to describe this pass and correctly cross-reference this decision-log entry (an earlier draft of that README edit referenced a "mission history" section within the same file that does not exist — corrected to point here instead).

**Explicitly NOT changed:** Any diagram's factual content, box text (other than the two overflow fixes, which shortened/repositioned but did not change meaning), or overall layout/reading order. The 5 intentional T-junctions in `data_lineage.svg`.

**Reversible:** Yes — this is a styling and layout-geometry pass on reference diagrams; git history holds the prior version if ever needed, and none of the changes touch any data, model, or dissertation-content file.

---

### 2026-07-13 — `data_lineage.svg` GDELT path: fixed a genuine content gap, not just a styling one

**Decision:** The Project Director asked why `gdelt_daily_summary.parquet` didn't appear in `data_lineage.svg`. It is genuinely relevant: `scripts/build_master_dataset.py` reads `gdelt_daily_risk.parquet` directly (alongside `daily_sentiment.parquet`) to build `master_dataset.parquet` — confirmed by re-reading the script's five `pd.read_parquet` calls. The diagram simply predates this session's GDELT integration work and was never updated. Fixed by adding both `gdelt_daily_summary.parquet` (raw, notebook 1) as a fifth raw-source box and `gdelt_daily_risk.parquet` (processed, notebook 3) as a third row-2 box, connected by their own path directly into `master_dataset.parquet`.

**Why a separate path, not merged into the existing `events_tagged.parquet`/`daily_sentiment.parquet` flow:** GDELT is deliberately *not* part of the event catalogue — this was the Phase 0 design decision earlier this session (continuous daily confounder, not discrete events). Routing it through `events_tagged.parquet` in the diagram would misrepresent that decision. `build_master_dataset.py` itself confirms this: it reads `gdelt_daily_risk.parquet` as an independent input, separate from `daily_sentiment.parquet`.

**Layout:** Canvas widened 980px→1030px (viewBox and background rect both updated) to fit a fifth raw-source box without shrinking any existing box or font below what the 2026-07-13 Visual Enhancement pass had already verified. `gdelt_daily_summary.parquet` box (185×56) placed to the right of `app_presidential_documents.parquet`; `gdelt_daily_risk.parquet` box (210×56) placed to the right of `daily_sentiment.parquet` in row 2; a two-segment connector (vertical + horizontal jog at y=180) links them, and a second connector (vertical + horizontal jog at y=355) enters `master_dataset.parquet` from its right edge — distinct from the top-edge entry the existing bus-bar already uses, so the two input paths remain visually distinguishable. Title/subtitle/section-label/divider all recentred for the new canvas width. Version footer bumped v2.2→v2.3.

**Verification:** Same two programmatic audits as the prior pass — text-width measurement (Pillow + real Arial fonts) confirmed both new labels fit their boxes (`gdelt_daily_summary.parquet` at 11.5pt: 164.0/169.0px usable; `gdelt_daily_risk.parquet` at 12.5pt: 145.0/194.0px usable), and re-run across all 21 text/box pairs in the file with zero overflows. Line-segment-intersection geometry confirmed the two new connectors introduce zero new crossings (the 3 flagged pairs are the same pre-existing bus-bar T-junctions from the prior pass, none involving the new coordinates — confirmed by checking which flagged segments contain the new path's coordinates). Rasterised via `rsvg-convert` and visually reviewed.

**Files updated:** `docs/architecture/data_lineage.svg`, `docs/architecture/README.md` (changelog, version, and the `data_lineage.svg` table row).

**Explicitly NOT changed:** Any other diagram, any data/model file, or the existing `events_tagged.parquet`/`daily_sentiment.parquet`/`causal_estimates.parquet` paths already in the diagram.

**Reversible:** Yes — styling/content addition to a reference diagram only; git history holds the prior version.

---

### 2026-07-13 — Project-wide documentation consistency pass: root README and governance docs brought current, closing the "5-day sample" debt this log itself flagged twice

**Decision:** The Project Director reported that after pushing the repository, `README.md` still showed `Last updated: 2026-07-06` despite the extensive same-day pipeline rebuild. Investigating the header staleness surfaced a materially bigger issue: several governing documents were still asserting facts this log had already superseded — most notably the GDELT "5-day proof-of-concept sample" description, which two earlier entries this same day (the GDELT backfill decision at line 527 and the `master_dataset.parquet` v1.1 rebuild at line 587) explicitly flagged as an owed update ("`dataset_version.md`'s '5-day sample only' caveat... is now stale and should be updated") but neither entry actually made. This entry closes that debt across the whole documentation set, not just the two files named in those earlier notes.

**Scope — files updated and what changed in each:**
- **`README.md`** (root): header bumped to `Version 2.1 — Pipeline Rebuilt, GDELT Integrated as Continuous Confounder (2026-07-13)`; Current Project Status section rewritten with the 2026-07-13 rebuild summary, the changed RQ1 evidence base (event-study now null, overall causal estimate still significant, monetary replicates), the RQ3 result (`Event_LASSO`/`Baseline_LASSO` now numerically identical, DM undefined), and the repository cleanup; Data Sources/Data Policy GDELT rows corrected; Phase 3/4/5 sections (which describe 2026-07-06-and-earlier snapshot numbers) given explicit "historical snapshot, superseded" notices with the current headline facts inline, rather than being silently left to read as current; Phase Progress table annotated with the 2026-07-13 re-verification of every notebook.
- **`docs/research_bible/dataset_version.md`**: added the missing v1.1 version-history row; corrected the `gdelt_*` column description and Missing Value Policy row; updated the Review Gate note.
- **`docs/research_bible/dataset_contract.md`**: term 6 (GDELT caveat) and term 10 (version pinning, was hardcoded to "currently v1.0") corrected.
- **`docs/research_bible/11_limitations.md`** L7: rewritten from "GDELT limited to a 5-day sample" (no longer true) to the real, narrower, current limitation — full history is integrated into `master_dataset.parquet` but not yet feature-engineered into `feature_matrix.parquet`, and doesn't qualify as a high-impact discrete event under the existing threshold.
- **`docs/research_bible/00_project_overview.md`, `03_methodology.md`, `08_figures_plan.md`, `15_traceability_matrix.md`**: each had a current-state GDELT description or status flag that still said "5-day sample" — all corrected. `15_traceability_matrix.md` additionally got a dated note at the top flagging that its Models/Statistical-tests sections describe the 2026-07-05 run and are numerically stale post-rebuild (the artefact-to-RQ mapping itself is unaffected).
- **`docs/research_bible/05_data_dictionary.md`**: the `gdelt_sample.parquet` row referenced a file that no longer exists on disk (confirmed via `ls data/raw/`) — replaced with a `gdelt_daily_summary.parquet` row reflecting its actual current schema (`date, gdelt_event_count, gdelt_mean_goldstein, gdelt_mean_tone`; 4,018 × 4).
- **`docs/research_bible/14_project_dashboard.md`**: this document's own stated update rule is "never more than one session stale," yet it still read as a 2026-07-06 snapshot. Added a dated 2026-07-13 update section at the top (RQ status changes, what's resolved, what's newly open) rather than rewriting the historical mission log below it, which remains valid as a record of what those missions produced at the time.
- **`docs/research_bible/future_improvements.md`** item 26: marked resolved with the list of files this entry actually updated, and the two genuinely still-open pieces separated out (GDELT not yet a `feature_matrix.parquet` feature; the `03_event_detection.ipynb` vs. `03_event_detection_revised.ipynb` naming ambiguity — see below).

**Deliberately NOT changed — files where the "5-day" string is a legitimate historical record, not a current-state claim:** `10_decision_log.md` itself (this document is append-only; the 2026-07-04 and earlier entries correctly describe what was true when they were written), `09_results_log.md` (dated result entries, same reasoning), `docs/stakeholder_report/01_data_collection.md` (already correctly described the full-history backfill — apparently updated in an earlier, untracked pass). `06_feature_dictionary.md` and `feature_contract.md` were deliberately **not** version-bumped or given a new GDELT feature entry — GDELT genuinely is not yet engineered into `feature_matrix.parquet` (confirmed: no `gdelt` string appears anywhere in `05_feature_engineering.ipynb`), so adding a feature-dictionary entry for it would be documenting work that hasn't happened. That remains real, separate future work.

**A genuine open item surfaced, not resolved, by this pass:** `notebooks/03_event_detection.ipynb` and `notebooks/03_event_detection_revised.ipynb` both currently exist in the repository and both carry a 2026-07-13 modification timestamp. This session's actual GDELT-integration and interpretation-cell work was done in `_revised`; the decision log's own 2026-07-04–07-06 entries refer only to the un-suffixed `03_event_detection.ipynb` as canonical. Nothing in this repository currently states which is the file of record — this is the same category of loose end as the earlier, already-resolved `01_data_collection.ipynb` duplicate, but has not gone through the same resolution step (archive the superseded one, log the decision). Flagged in `14_project_dashboard.md` and `future_improvements.md` item 26; not resolved here, since picking a canonical file is a real repository-structure decision for the Project Director, not a documentation-accuracy fix.

**Alternatives considered:** (a) Only fix the root README's date/version header, since that's literally what was reported. Rejected — a corrected date stamp on a document whose body still asserts superseded facts (GDELT "5-day sample," a "significant" CAR finding now null) would be actively misleading, not merely incomplete. (b) Rewrite every stale number throughout the Research Bible (e.g. every model metric in `15_traceability_matrix.md`'s Models/Statistical-tests tables) to current values. Rejected for this pass — many of those values would require re-deriving figures not already verified and recorded in this log (this log's own entries were used as the source of truth throughout; nothing was newly computed or guessed for this pass), which risks introducing new, unverified numbers under the guise of a "consistency" fix. Flagged with dated "superseded, see decision log" notices instead, matching the pattern this project has used since the 2026-07-05 Phase 5 legacy-table treatment.

**Reasoning:** Documentation that contradicts the data it's describing is worse than documentation that is visibly marked stale — a reader (or examiner) trusts a confidently-stated wrong fact more than a flagged historical snapshot. Every fix in this pass either (i) corrects a claim this log had already established was false, using numbers already verified and recorded here, or (ii) adds a dated "superseded, see log" pointer rather than fabricating a new precise figure. Nothing was changed based on a fresh, unverified computation.

**Files updated:** `README.md`; `docs/research_bible/{dataset_version.md, dataset_contract.md, 11_limitations.md, 00_project_overview.md, 03_methodology.md, 05_data_dictionary.md, 08_figures_plan.md, 15_traceability_matrix.md, 14_project_dashboard.md, future_improvements.md}`.

**Explicitly NOT changed:** Any data, model, or notebook file; `06_feature_dictionary.md`/`feature_contract.md` (no feature-engineering work was done); the `03_event_detection.ipynb`/`_revised` naming question (surfaced, not resolved); any historical decision-log or results-log entry.

**Reversible:** Yes — documentation-only; git history holds every prior version.

---

### 2026-07-13 — Documentation consistency pass, part 2: `DATSCI7030_Repository_Audit_Report.ipynb`, `src/README.md`, and every remaining folder-level README

**Decision:** The Project Director asked for the sweep above to be extended to `DATSCI7030_Repository_Audit_Report.ipynb` (last updated 2026-07-04, per its own header) and `src/README.md`/`tests/README.md`, plus "go in subfolders" generally. Extended the same verify-against-disk-then-correct method to these.

**`DATSCI7030_Repository_Audit_Report.ipynb`:** Added a new dated `## 10c. Session Update — 2026-07-13` section (matching the notebook's own established §10a/§10b pattern) and rewrote the Action Plan (§10) checkboxes, rather than editing the original 2026-07-04 findings in place — those are kept as the historical record of what was true when first written, exactly as this project's other append-only logs work. §10c reconciles: version control and the nested-git-repo issue (both fully resolved, 18 commits, no nested repo found), the market-only baseline (resolved by the already-logged Mission 06/07 work), the 8-vs-10 notebook decision (was actually made 2026-07-04 in this very log — the audit report's own Action Plan just never caught up), and the GDELT gap (resolved). Credentials remain the one genuinely unresolved P0 — confirmed still unrotated, `config.yaml` still has a hardcoded `api_keys:` block, no `.env.example` exists.

**New findings from this pass, not previously recorded anywhere:**
1. **`reports/dissertation/` regressed.** The 2026-07-04 audit's "resolved to one file" state doesn't hold anymore — the folder now has 5 substantive `.docx` files (2026-07-13), two of which (`7030DATSCI-11_07_2026.docx`, `7030DATSCI-13_07_2026.docx`) are byte-identical (same size, same mtime). Documented in a new `reports/dissertation/README.md`, not resolved — which draft is canonical is an author call.
2. **`data/raw/README.md` and `data/processed/README.md` had been lost entirely**, not just gone stale — both existed as of the 2026-07-04 audit (per that report's own §5 quoting their content) but neither was present on disk before this pass. Rewritten from scratch against actual current files, verified via each producing notebook's own `save_table()`/`to_parquet()` calls (`01_data_collection.ipynb` for raw, `03_event_detection_revised.ipynb`/`04_causal_analysis.ipynb`/`scripts/build_master_dataset.py` for processed) rather than guessed from filenames alone.
3. **Raw filename drift, project-wide.** `prices.parquet` and `app_presidential_documents.parquet` are named throughout the Research Bible (including `dataset_version.md`'s own "Dependencies" line, `05_data_dictionary.md`, and others touched in the first documentation pass) but neither exists on disk — current names are `spy_ohlcv.parquet`/`spy_returns.parquet` and `app_presidential_documents_raw.parquet`/`app_presidential_documents_economic.parquet` (the `_economic` variant, specifically, is what `03_event_detection_revised.ipynb` actually reads). Documented in the new `data/raw/README.md`; a full corpus-wide rename sweep was judged out of scope for this pass and is flagged, not actioned, in `14_project_dashboard.md`.
4. **`src/README.md` mis-stated one module's active dependency.** `causal_engine.py`'s `EventStudy` class was listed as "Used by: `04_causal_analysis.ipynb`" — verified false by grep: that notebook implements its own inline `EventStudy` class and never imports `src/causal_engine.py`. Corrected to the same "historically used, now inline" category as `features.py`/`models.py`/`evaluation.py`, which the README already correctly flagged. Also corrected `event_detector.py`'s "Used by" from `03_event_detection.ipynb` to `03_event_detection_revised.ipynb` (verified via import grep — the un-suffixed file does not import this module) and added `GDELTCollector` to `data_collector.py`'s description (present in the module's own docstring, absent from the README's summary).
5. **`reports/README.md` had gone stale itself** (added 2026-07-06): still listed `statistics/` and `event/` as current subfolders; both were archived to `models/archive/pre_2026-07-13_pipeline_rebuild/` during the earlier same-day cleanup pass and no longer exist at those paths. Corrected.
6. **`data/interim/README.md` described a `02_data_cleaning.ipynb` stage that has never existed** in the implemented 8-notebook pipeline (the actual `02_eda.ipynb` does EDA only; no notebook uses `data/interim/` as a staging area). Rewritten to match `data/external/README.md`'s honest "reserved but unused" framing rather than describing a step that was never built.
7. **`scripts/README.md` was missing two of the folder's three scripts** — `build_master_dataset.py` and `run_finbert_economic.py`, both added this session, were absent from the table entirely.

**Nine new/rewritten folder READMEs added this pass** (all previously missing entirely, confirmed via direct filesystem check, not assumed from the 2026-07-04 audit's now-nine-day-old list): `data/README.md`, `models/README.md`, `notebooks/README.md`, `reports/figures/README.md`, `reports/dissertation/README.md`, `docs/README.md`, plus the two lost-and-rewritten `data/raw/README.md` and `data/processed/README.md`, plus the rewritten `data/interim/README.md`.

**Reasoning:** Same as the first pass this same day — every correction here is either a fact verified directly against the filesystem/git state (file existence, import grep, byte-for-byte size/mtime comparison) or a dated "resolved/still open, see log" pointer, never a guessed number. Two items (`reports/dissertation/` drafts, the `03_event_detection` naming question) were deliberately left open rather than resolved, since choosing a canonical file is an authorial decision this log's own governance boundary reserves for the Project Director.

**Files updated:** `DATSCI7030_Repository_Audit_Report.ipynb` (3 cells: header, new §10c, rewritten §10 Action Plan); `src/README.md`; `reports/README.md`; `data/interim/README.md`; `scripts/README.md`; new files: `data/README.md`, `data/raw/README.md`, `data/processed/README.md`, `models/README.md`, `notebooks/README.md`, `reports/figures/README.md`, `reports/dissertation/README.md`, `docs/README.md`.

**Explicitly NOT changed:** `tests/README.md` (checked, already accurate — verified `EventStudy`/`FeatureEngineer` classes it references still exist in `src/`); any data, model, or notebook code; the two newly-surfaced open items (dissertation drafts, Notebook 3 naming) — flagged, not resolved; the project-wide `prices.parquet`/`app_presidential_documents.parquet` naming-drift sweep beyond `data/raw/README.md` itself.

**Reversible:** Yes — documentation-only; git history holds every prior version.

---

### 2026-07-14 — Notebook 3 naming resolved; `reports/dissertation/` cleaned; Generative AI Statement revised (all on explicit Project Director instruction)

**Decision:** Two items the 2026-07-13 documentation pass surfaced but deliberately left open — the `03_event_detection.ipynb` vs. `03_event_detection_revised.ipynb` duplication, and the re-accumulated `reports/dissertation/` drafts — were resolved today on explicit Project Director instruction, plus a companion revision to the Generative AI Statement.

**Notebook 3:** Confirmed (re-verified via the same import-grep method as the prior pass) that `src/event_detector.py` is imported only by `03_event_detection_revised.ipynb`, never by the un-suffixed `03_event_detection.ipynb`. Archived the pre-rebuild `03_event_detection.ipynb` to `notebooks/archive/` and renamed `_revised` to the canonical `notebooks/03_event_detection.ipynb`, matching the resolution pattern already used for `01_data_collection.ipynb`. Both files were git-tracked with full commit history, so `git mv` was used for both moves (fully reversible); the archive destination was then explicitly unstaged (`git reset`) so it stays gitignored consistent with every other file already in `notebooks/archive/`, rather than becoming an accidental tracked exception to that folder's established untracked convention. Every documentation reference to the `_revised` filename was swept to the new canonical name across `README.md`, `notebooks/README.md`, `src/README.md`, `data/raw/README.md`, `data/processed/README.md`, `scripts/README.md`, and `docs/research_bible/{05_data_dictionary.md, 14_project_dashboard.md, future_improvements.md}` — historical entries describing the ambiguity while it existed (including this log's own prior entries) were left untouched, per the append-only convention.

**`reports/dissertation/`:** Per instruction, kept only `7030DATSCI-13_07_2026.docx` (renamed to `7030DATSCI-14_07_2026.docx`, today's date), the revised-and-renamed Generative AI Statement (below), and `README.md`; removed everything else. **This folder has never been git-tracked** (`git status`/`git log` confirmed `??`/no history for any path under it) — unlike essentially every other change this session, there was no git safety net available. Given that, the five removed items were **archived to `reports/dissertation/archive/pre_2026-07-14_cleanup/`, not hard-deleted** — `7030DATSCI-00_07_2026.docx`, `7030DATSCI-11_07_2026.docx` (confirmed byte-identical to the kept draft by size and mtime before removal), `7030DATSCI_Reference_Audit_2026-07-08.docx`, the pre-revision `Generative AI Statement.docx`, and two harmless `~$*` Word lock files — so the instructed outcome (a clean, unambiguous folder) is achieved while the material stays recoverable.

**Generative AI Statement:** Read via `pandoc -t markdown` (installed via `brew install pandoc` — not previously available in this environment), then edited directly in `word/document.xml` (unzip → edit → rezip, per the project's docx-editing skill; `defusedxml`/`lxml` installed via `pip3 install --break-system-packages` to run the skill's `merge_runs.py`/`validate.py` helpers, neither previously available either) to add a new "Repository Maintenance, Debugging, and Documentation Assistance (2026-07-13 to 2026-07-14)" section, in the same disclosure style as the document's existing Phase 1–8 coding-assistance section: environment/compatibility bug fixes (DoWhy/networkx/pandas, xgboost/libomp, matplotlib), the Notebook 03 regex-taxonomy bug, the GDELT continuous-confounder design and integration, pipeline rebuild bug fixes, the architecture-diagram audit, and the documentation-consistency sweep — pointing to this decision log as the detailed record for every item. Validated against the original with the docx skill's schema validator before being accepted (`All validations PASSED!`, paragraph count 10→20, as expected for a 10-paragraph addition). Visual rendering via LibreOffice was not available in this environment and was not installed (a much heavier dependency than pandoc/lxml for a single verification); the pandoc round-trip read-back was used instead to confirm the inserted content and structure. Renamed `Generative AI Statement.docx` → `7030DATSCI-14_07_2026_Generative_AI_Statement.docx` to match the dissertation's dated-filename convention.

**Explicitly NOT done:** Revising `7030DATSCI-14_07_2026.docx`'s actual dissertation prose to reflect the changed RQ1 evidence base, GDELT integration, or RQ3 result. The instruction ("we will need to update based on the new update") was read as flagging this as known, upcoming work rather than a directive to do it now — that edit requires editorial judgement about which specific claims change and how, which is a Project Director decision, not a mechanical documentation fix. Confirm scope before it is attempted.

**Files updated:** `notebooks/03_event_detection.ipynb` (renamed from `_revised`); `notebooks/archive/03_event_detection.ipynb` (new, gitignored, not tracked); `README.md`, `notebooks/README.md`, `src/README.md`, `data/raw/README.md`, `data/processed/README.md`, `scripts/README.md`, `docs/research_bible/{05_data_dictionary.md, 14_project_dashboard.md, future_improvements.md}`; `DATSCI7030_Repository_Audit_Report.ipynb` (new §10d); `reports/dissertation/` (5 files moved to `archive/pre_2026-07-14_cleanup/`, 1 renamed, 1 revised-and-renamed, `README.md` rewritten).

**Explicitly NOT changed:** Any other notebook or source file; the dissertation's actual prose content; any data or model artefact.

**Reversible:** Notebook rename and every documentation edit: yes, fully (git history for the tracked files; the archived notebook itself was never git-relevant to begin with, matching its siblings). `reports/dissertation/` cleanup: the five archived files are recoverable from `archive/pre_2026-07-14_cleanup/` on disk, but — unlike the rest of this session's work — **not** recoverable via git, since the folder was never tracked; this is a real, if small, exposure the Project Director should be aware of (e.g. a `git add`/commit of `reports/dissertation/` going forward would close it).

---

### 2026-07-14 — Feature-matrix validation `FAIL` accepted as a narrow, SHA-256-bound known exception

**Decision:** On explicit Project Director instruction, accept the rebuilt FES v1.0 feature matrix under disposition `ACCEPTED_WITH_KNOWN_EXCEPTION`. The original `feature_matrix_validation.json` remains unchanged at `validation_status: FAIL`; it correctly identifies two constant-zero columns, `labour` and `labour_event_day`. Approval is recorded separately in `data/processed/feature_matrix_validation_disposition.json`, which binds the exact matrix and validation files by SHA-256 hash and names the permitted exception, controls, impact, approver, and date.

**Reasoning:** All 103 labour-tagged presidential documents in the rebuilt catalogue received neutral FinBERT sentiment, making the aggregated `labour` signal and its contract-defined `sentiment_category != 0.0` occurrence proxy constant zero across all 2,477 rows. This is a genuine data characteristic under the frozen FES v1.0 formulas, not missing data, target leakage, a duplicate feature, or a notebook execution failure. `Baseline_LASSO` cannot read either column because both sit outside the 27 Market-category features. Notebook 07 reads the full matrix but explicitly handles zero-standard-deviation scaling safely; constant columns cannot contribute information to LASSO or tree splits. The current model comparison and RQ3 conclusion therefore do not depend on either column.

**Scope and safeguards:** This is not a validation `PASS`, a general waiver for constant features, or permission to ignore future failures. It applies only to the named columns and the exact artefact hashes recorded in the disposition. Any changed hash, additional constant column, constant Market feature, leakage mismatch, duplicate feature, missing value, or non-finite scaled value remains blocking. Changing the affected formulas, redefining `labour_event_day` from direct event presence, or removing either column would require a new FES version and full Notebook 05–08 re-validation.

**Alternatives considered:** (a) Rewrite `FAIL` as `PASS` — rejected because it would erase valid evidence. (b) Drop the two columns or redefine `labour_event_day` now — rejected because that would create FES v1.1, require downstream retraining/figure regeneration, and offer no predictive benefit for the already-frozen result. (c) Leave the conflict unresolved while continuing to cite a contract that says every `FAIL` blocks training — rejected as internally inconsistent. The adopted exception preserves both the evidence and the frozen research result transparently.

**Research/dissertation impact:** No data value, model, statistic, figure, or RQ1–RQ3 conclusion changes. The dissertation should describe the two zero-variance features as a detected and controlled limitation, not claim that the rebuilt feature matrix received an unconditional validation pass.

**Files updated:** New `data/processed/feature_matrix_validation_disposition.json`; `docs/research_bible/feature_contract.md`, `11_limitations.md`, `13_validation_checklist.md`, `14_project_dashboard.md`, `15_traceability_matrix.md`; `data/processed/README.md`. `feature_matrix_validation.json` and `feature_matrix.parquet` deliberately unchanged.

**Reversible:** Yes. Removing the disposition restores the prior blocking state. The original validation evidence and frozen analytical artefacts have not been modified.

---

### 2026-07-14 — Dataset v1.2 / FES v1.1 migration started: Notebook 03 explicit occurrence counts completed

**Decision:** On Project Director instruction, begin the feature-occurrence correction as a controlled, notebook-by-notebook version migration rather than continuing indefinitely under the FES v1.0 known exception. Notebook 03 is the first completed boundary. Its daily aggregation now persists three direct catalogue-row counts: `n_health_catalogue_events`, `n_labour_catalogue_events`, and `n_other_catalogue_events`.

**Reasoning:** FES v1.0 defines `health_event_day`, `labour_event_day`, and `other_event_day` using `sentiment_category != 0.0`. That formula confounds event occurrence with sentiment polarity: a real event scored neutral by FinBERT is treated as no event. This is especially visible for labour, where all 103 catalogue events have neutral sentiment and the derived occurrence proxy is therefore constant zero. Direct row counts preserve neutral events and give the future FES v1.1 event-day flags an auditable source variable.

**Implementation and validation:** Notebook 03 version 2.2 now builds a daily occurrence pivot alongside the existing sentiment pivot, keeps the counts as non-negative `int64`, and fails before saving if their totals do not reconcile to the unified catalogue. The notebook was executed end-to-end: 17/17 code cells have execution counts, no saved error outputs exist, and `daily_sentiment.parquet` changed from 739 × 15 to 739 × 18. Verified totals are health 14, labour 103, and other 427 on both sides of the reconciliation. The 1,005-row event catalogue, 344-row high-impact subset, and 4,018-row GDELT series are otherwise unchanged. The repository test result remains 8 passed.

**Architecture and documentation impact:** `data_lineage.svg` v2.4 now labels `daily_sentiment.parquet` as carrying sentiment plus explicit occurrence counts. No connector topology changed because the migration adds columns, not a new processing stage. Notebook 03's methodology, data dictionary, folder READMEs, root status, and dashboard were updated with verified current values.

**Migration boundary:** This entry does not claim Dataset v1.2 or FES v1.1 is frozen. `master_dataset.parquet` is still v1.1 and does not yet promote the three new fields; `feature_matrix.parquet` remains the exact FES v1.0 artefact covered by the existing known-exception disposition. The next controlled step is `scripts/build_master_dataset.py` plus Dataset v1.2 validation/freeze, followed by Notebook 05/FES v1.1 and full downstream regeneration.

**Files updated:** `notebooks/03_event_detection.ipynb`; regenerated `data/processed/{events_tagged.parquet,daily_sentiment.parquet,high_impact_events.parquet,gdelt_daily_risk.parquet}`; `README.md`; `notebooks/README.md`; `data/processed/README.md`; `docs/research_bible/{03_methodology.md,05_data_dictionary.md,10_decision_log.md,14_project_dashboard.md}`; `docs/architecture/{data_lineage.svg,README.md}`. Pre-migration Notebook 03 outputs were copied to `data/processed/archive/pre_dataset_v1_2_notebook03_2026-07-14/` before execution.

**Reversible:** Yes. The prior Notebook 03 output set is locally archived, and tracked source/document changes remain recoverable through version control once committed.

---

### 2026-07-14 — Dataset v1.2 frozen: explicit occurrence counts promoted to the SPY trading-day panel

**Decision:** Complete the second boundary of the controlled FES v1.1 migration by freezing `master_dataset.parquet` as Dataset v1.2. The dedicated builder now promotes `n_health_catalogue_events`, `n_labour_catalogue_events`, and `n_other_catalogue_events` from Notebook 03 and writes a machine-readable, hash-bound validation report.

**Verified change:** Dataset v1.1 contained 2,765 rows × 31 columns. Dataset v1.2 contains 2,765 × 34 and adds exactly the three intended integer count columns; no column was removed, and every v1.1 column is value-for-value identical in the rebuilt dataframe. Train/test counts remain 2,014/751 and the date range remains 2015-01-02 through 2025-12-30.

**Validation:** `master_dataset_validation.json` records `validation_status: PASS`, zero duplicate dates, monotonic dates, zero unexpected missing values, zero forward-target mismatches, the required final-row target `NaN`, valid non-negative integer occurrence fields, and zero trading-day reconciliation mismatches. The frozen parquet SHA-256 is `142145722faff37156c6606801ae82d56843118b486942b050629b0472647819`. Eight repository tests pass.

**Trading-calendar policy:** Notebook 03 all-calendar totals are health 14, labour 103, other 427. Dataset v1.2 uses the existing same-calendar-date SPY trading-day alignment, yielding 12/96/395. The excluded 2/7/32 events occurred on weekends or market holidays and are explicitly recorded in validation rather than silently moved to the next session. A next-session sensitivity analysis would change causal timing and therefore requires a later dataset/SAP amendment; it is logged as limitation L17.

**Architecture and governance:** Dataset v1.2 is now the approved input boundary for FES v1.1. `data_lineage.svg` v2.5 identifies the frozen 34-column dataset and validation `PASS`. `feature_matrix.parquet` remains FES v1.0 and is unchanged, so its existing accepted-known-exception disposition remains valid until Notebook 05 generates a replacement matrix and validation report.

**Files updated:** `scripts/build_master_dataset.py`; regenerated `data/processed/master_dataset.parquet`; new `data/processed/master_dataset_validation.json`; `README.md`; `scripts/README.md`; `data/processed/README.md`; `docs/research_bible/{03_methodology.md,05_data_dictionary.md,dataset_contract.md,dataset_version.md,10_decision_log.md,11_limitations.md,13_validation_checklist.md,14_project_dashboard.md}`; `docs/architecture/{data_lineage.svg,README.md}`. Dataset v1.1 was archived locally as `data/processed/archive/pre_dataset_v1_2_master_2026-07-14/master_dataset_v1_1.parquet` before overwrite.

**Reversible:** Yes. Dataset v1.1 is archived locally, the v1.2 build is deterministic from hash-recorded inputs, and source/document changes remain recoverable through version control once committed.

---

### 2026-07-14 — FES v1.1 frozen: direct occurrence flags and binding training-variance enforcement

**Decision:** Complete the third boundary of the controlled migration by rebuilding `feature_matrix.parquet` as FES v1.1 in Notebook 05. The new matrix is the only current feature input. The old FES v1.0 matrix, profile, failed validation, and accepted-known-exception disposition are historical artefacts retained under `data/processed/archive/pre_fes_v1_1_2026-07-14/`.

**Why the change was required:** FES v1.0 defined `health_event_day`, `labour_event_day`, and `other_event_day` as `sentiment_category != 0.0`, which treats a real neutral-sentiment event as no event. Dataset v1.2 supplies explicit catalogue counts, so FES v1.1 now defines each flag as `n_<category>_catalogue_events > 0`. This separates occurrence from polarity and resolves the cause of the zero `labour_event_day` column.

**Variance-screening finding:** The first controlled execution surfaced a second governance issue before save: training-split variance identified three features below the frozen 1e-8 threshold — `energy`, `labour`, and `monetary_x_rate_cut`. Allowing them through would contradict the SAP/FES screening rule and produce non-finite VIF calculations. They were therefore removed, changing the planned 94-feature intermediate design to the final 92-feature FES v1.1. Correlation and VIF findings remain informational flags, not automatic-removal criteria.

**Exact change surface:** Relative to archived FES v1.0, exactly three features are removed and none added. Exactly three retained features are redefined: `health_event_day` changes on 7 rows, `labour_event_day` on 90 rows, and `other_event_day` on 299 rows. The 2,477-row date/split index is unchanged. Category counts are Market 27, Macro & VIX 16, Sentiment 23, Event 14, Temporal 5, and Interaction 7.

**Validation and reproducibility:** Notebook 05 executed top-to-bottom with 16/16 code cells counted and zero saved errors. A deliberate repeat execution exposed and fixed a migration-QA bug: the comparison cell initially read the now-current `feature_matrix.parquet` as though it were the v1.0 baseline. It now prefers the dated v1.0 archive (or the current path only when the three legacy columns prove it is genuinely v1.0), so repeated runs retain the exact historical delta check; a clean rebuild with no historical matrix records the comparison as unavailable rather than fabricating one. The corrected notebook was rerun successfully. `feature_matrix_validation.json` reports `PASS`: no duplicates, missing values, constants, near-zero-training-variance features, target-passthrough mismatches, non-anticipative-lag failures, or occurrence-source failures. Seven |r|>0.90 pairs and 31 VIF>10 features are retained as documented interpretability flags. The saved matrix is 2,477 × 95 including date/split/target (1,727 train, 750 test) and has SHA-256 `127a6dbe4b83e59c873dfdf7502060aab115037732bbb723f9d489c6b85dc383`. The matrix/profile/validation files bind both Dataset v1.2 and `car_results.parquet` by hash, and post-save verification succeeded. Eight repository tests pass.

**Governance consequence:** The FES v1.0 known exception is retired for current use; there is no current `feature_matrix_validation_disposition.json` because FES v1.1 passes unconditionally. SAP v1.0 and MCP v1.0 do not change. Existing Baseline_LASSO, Event_LASSO, XGBoost, LightGBM, Random Forest importance, SHAP, statistical-test, figure, and dissertation outputs were produced from FES v1.0 and are now stale relative to the feature contract. They remain valid historical evidence but must not be relabelled as FES v1.1.

**Next controlled boundary:** Execute Notebook 06 against FES v1.1, then Notebook 07, then Notebook 08; compare regenerated results to the archived/current FES v1.0 evidence and update all result-bearing dissertation material only after the downstream validation chain passes.

**Files updated:** `notebooks/05_feature_engineering.ipynb`; regenerated `data/processed/{feature_matrix.parquet,feature_profile.json,feature_matrix_validation.json}`; regenerated `reports/figures/05_learning_outcome.png`; FES/current-state Research Bible documents, repository READMEs, and canonical architecture diagrams. Superseded artefacts and the prior learning figure were archived before execution.

**Reversible:** Yes. The exact FES v1.0 artefacts and figure are retained in dated local archives; the new matrix is deterministically reproducible from hash-bound inputs.

---

### 2026-07-14 — `Baseline_LASSO` v1.1 frozen on FES v1.1; historical result reproduced exactly

**Decision:** Complete the fourth controlled FES v1.1 boundary by executing Notebook 06 against the validated matrix, versioning the result as `Baseline_LASSO` v1.1, and replacing the canonical baseline paths only after the FES, Market-only feature, finite-input, and historical-reproduction checks pass. The prior model, metadata, predictions, metrics, and learning figure were retained in dated archives before replacement.

**Why the numerical result should not change:** FES v1.1 changes three non-Market occurrence features and removes three non-Market/interaction features, but leaves all 27 Market values, the target, dates, and split unchanged. Notebook 06 verifies those invariants directly against `data/processed/archive/pre_fes_v1_1_2026-07-14/feature_matrix.parquet`. Alpha, intercept, all-zero coefficient state, and every train/test RMSE/MAE/R²/directional-accuracy value match the dated FES v1.0 baseline within 1e-6; `REPRODUCTION_EXACT = True`.

**Current result:** The model uses 1,727 training and 750 test rows. `LassoCV(TimeSeriesSplit(5), seed=42)` selects alpha `0.0018533887501907256`; all 27 coefficients are zero and the intercept is `0.00046584536225195564`. Test RMSE is `0.009631061816812675`, MAE `0.006547931743181517`, R² `-0.0015064059786111805`, and directional accuracy `0.5746666666666667`. The full SAP suite now includes fold diagnostics, classification framing, residual diagnostics, empirical prediction intervals, seeded block-bootstrap/Wilson intervals, and random/persistence/mean context baselines.

**Validation and reproducibility:** All 10 code cells executed with non-null counts and zero saved errors. `reports/baseline/baseline_model_validation.json` reports `PASS`, binds FES v1.1 matrix SHA-256 `127a6dbe4b83e59c873dfdf7502060aab115037732bbb723f9d489c6b85dc383`, records exact historical reproduction, and stores SHA-256 digests for the model, metadata, predictions, and metrics. Every saved artefact was reloaded and checked. The learning figure was regenerated and visually inspected.

**Governance consequence:** Mission 06 is current FES v1.1 evidence. Mission 07 is now the migration boundary: Event_LASSO, XGBoost, LightGBM, Random Forest importance, SHAP outputs, DM/z-tests, and their dissertation/figure consumers remain historical FES v1.0 evidence until regenerated. The Research Bible is source governance documentation and is exposed to Git; staging/pushing remains a separate reviewed action.

**Archives:** `models/archive/pre_fes_v1_1_notebook06_2026-07-14/`, `reports/baseline/archive/pre_fes_v1_1_notebook06_2026-07-14/`, and `reports/figures/archive/pre_fes_v1_1_notebook06_2026-07-14/06_learning_outcome.png`.

**Reversible:** Yes. The dated v1.0 artefacts remain available, and Notebook 06 deterministically regenerates v1.1 from hash-bound FES inputs.

---

### 2026-07-14 — Notebook 07 migrated to FES v1.1; RF reproducibility restored; RQ2/RQ3 re-confirmed

**Decision:** Complete the fifth controlled FES v1.1 boundary by rebuilding Notebook 07 against the validated 92-feature matrix and frozen `Baseline_LASSO` v1.1. Retain the approved model roster and decision rules unchanged; restore Random Forest as an explicit executable RQ2 step; replace canonical paths only after validation passes.

**Implementation:** Notebook 07 validates feature-matrix and baseline hashes before training, requires finite non-zero training standard deviations, and checks baseline predictions row-for-row. It trains Event_LASSO/XGBoost/LightGBM on all 92 features, trains the RQ2-only fixed Random Forest (`n_estimators=500`, seed 42), computes held-out SHAP with exact reconstruction checks, applies the frozen one-sided DM and directional z tests at Bonferroni α=0.0167, and adds the full SAP diagnostic suite. A zero-variance loss differential is an undefined DM result (`null`), not a fabricated statistic.

**Issues found and fixed:** (1) strict JSON correctly rejected an archived FES v1.0 `NaN` DM value, so historical non-finite values are normalised to `null`; (2) a partial first run upgraded the metadata schema and exposed a repeat-run `source` lookup assumption, so parameter provenance is now backward/forward compatible. Both were fixed before the accepted run.

**Validated result:** 16/16 code cells executed with zero errors. Validation is `PASS`, SHAP reconstruction maximum absolute differences are 0, 2.33×10⁻⁹, and 8.67×10⁻¹⁸, and 13 primary artefacts are SHA-256-bound. A full repeat execution reproduced every hash exactly.

**Research consequence:** RQ2's joint top-decile rule is not met: macro/VIX features appear in the RF top 10 but no event feature does (`mean_car` #20), so H0 is not rejected. LightGBM SHAP ranks `mean_car` fifth as model-specific nuance. For RQ3, Event_LASSO exactly matches the baseline while XGBoost and LightGBM are numerically worse; none clears either corrected significance leg, so H0₃ is not rejected.

**Archives:** prior event models/metadata under `models/archive/pre_fes_v1_1_notebook07_2026-07-14/`; comparison outputs under `reports/model_comparison/archive/pre_fes_v1_1_notebook07_2026-07-14/`; learning figure under `reports/figures/archive/pre_fes_v1_1_notebook07_2026-07-14/`.

**Next boundary:** Notebook 08 must regenerate downstream RQ2/RQ3 figures before they are called FES v1.1. No contract, hypothesis, or alpha threshold changed.

**Reversible:** Yes. Dated FES v1.0 artefacts remain locally archived; Notebook 07 deterministically regenerates current outputs from hash-bound inputs and frozen parameters.

---

### 2026-07-14 — Final evidence-consumer synchronization and architecture v2.9

**Decision:** Treat the executed Notebook 01–08 outputs and their `PASS` validation artefacts as the sole current numerical source, then synchronize every active Markdown, canonical SVG and current Word consumer without changing SAP v1.0, FES v1.1 or MCP v1.0.

**Implementation:** Notebook 08 was regenerated against FES v1.1. Figure 08b and Figure 04c now name the current combined APP + FOMC treatment; the persisted `sentiment_method=lexicon` field remains an explicit accepted legacy-label exception rather than being silently rewritten. Six canonical architecture diagrams were updated to v2.9 and checked both as XML and rendered images. The dissertation draft, project summary and AI statement are generated reproducibly by `scripts/sync_current_documents.py`, with pre-sync copies retained under `reports/dissertation/archive/pre_fes_v1_1_doc_sync_2026-07-14/` and mandatory page-render review applied.

**Historical reporting boundary (superseded 2026-07-15):** At this checkpoint, RQ2 and RQ3 were final under their frozen rules, while RQ1 still awaited insertion of Benjamini–Hochberg-adjusted event-type results and standardised effect sizes. The following RQ1 reporting-freeze entry records completion without another model run.

**AI disclosure:** The final statement explicitly records Claude and OpenAI Codex assistance with bounded coding, debugging, documentation synchronization, diagrams and formatting. The researcher retains responsibility for design choices, verification, interpretation and submission wording.

**Reversible:** Yes. Source Markdown/SVG changes are version-controlled once committed, the document script is repeatable, and the original Word files were preserved before synchronization.

---

### 2026-07-15 — RQ1 reporting freeze v1.0

**Decision:** Close the pre-specified RQ1 reporting gate by applying BH-FDR across the five event-type mean-CAR tests and reporting a two-sided 95% t interval plus Cohen's d for each type. Preserve the underlying `car_results.parquet` schema and hash so the validated FES/model chain is not needlessly invalidated.

**Implementation boundary:** Notebook 04 writes the new `event_type_statistics.parquet` and hash-bound `rq1_reporting_validation.json`. It leaves the 264-row CAR artefact, causal estimator, SAP v1.0, FES v1.1 and MCP v1.0 unchanged. Notebook 08 consumes the reporting table and advances its figure validation to visualisation v1.2.

**Outcome:** Validation is `PASS`; 0/5 event-type nulls are rejected, minimum q=0.5810 and maximum |d|=0.2389. RQ1 is final but qualified: event-type abnormal-return evidence is null, while the separate pooled DoWhy estimate remains positive subject to observational assumptions.

**Why this approach:** A separate group-level artefact makes the inferential unit explicit and prevents BH-adjusted event-type results from being confused with the existing individual-window diagnostic flags. It also avoids rewriting a frozen input solely to add repeated group statistics.

**Reversible:** Yes. The added reporting artefact can be regenerated deterministically from the unchanged CAR file; downstream feature/model results remain hash-valid.

---

### 2026-07-15 — Notebook 01 GDELT reproducibility/traceability correction

**Decision:** Narrowly fix two independent defects surfaced by a documentation-traceability audit of the GDELT section of `notebooks/01_data_collection.ipynb`, without touching research questions, statistics, causal methods, feature engineering, or any frozen output.

**What was found (inspection, before any edit):**
1. **Live cell 19 ("Fetch Historical GDELT Data") was stale, not re-executed in the notebook's final run.** Its `execution_count=14` sits out of sequence between neighbouring cells executed at 35 and 36 — every other cell in the notebook ran in one continuous session; this one didn't. Its saved output (577,135,447 raw rows, a parquet-engine-failure warning that fell back to CSV) is inherited from an earlier, separate kernel session.
2. **The raw GDELT files this cell's saved output describes no longer exist.** `data/raw/gdelt_events.csv` (26GB) and `data/raw/gdelt_events.parquet` (201MB) were deliberately deleted 2026-07-13 with Project Director confirmation once `gdelt_daily_summary.parquet` was verified as their sole downstream consumer (see the 2026-07-13 "repository cleanup" entry above). Only `data/raw/gdelt_daily_summary.parquet` (4,018 rows, 157KB) remains, and it is confirmed valid: no nulls, no duplicate dates, non-negative event counts, finite Goldstein/tone values, date range exactly 2015-01-01→2025-12-31.
3. **Consequently, cell 21's calendar-build logic was a live landmine, not just a stale artefact.** Its branch order was `if csv_path.exists(): <rescan 26GB> elif parquet_path.exists(): <but then reads gdelt_daily_summary.parquet, a different file than the one just tested> else: <fill all GDELT columns with 0>`. Since neither raw file exists today, re-running this cell as saved would silently fall through to the all-zero branch, corrupting `daily_modelling_calendar_v1.parquet`'s GDELT columns without any warning — while a normal run should simply load the already-frozen summary.
4. **The daily summary is genuinely load-bearing.** `data/processed/master_dataset.parquet`'s `gdelt_risk_score`/`gdelt_mean_tone`/`gdelt_n_events` columns (built via `notebooks/03_event_detection.ipynb` §5 → `data/processed/gdelt_daily_risk.parquet` → `scripts/build_master_dataset.py`) trace directly back to this exact file and carry real, non-zero variance — confirmed by direct inspection, not by trusting prior documentation.

**Implementation:** Added `REFRESH_GDELT_RAW`/`REBUILD_GDELT_DAILY_SUMMARY` flags (both default `False`) plus `validate_gdelt_daily_summary()` and `write_gdelt_manifest()` helpers to cell 5 (Section 1 config). Cell 19's live fetch is now wrapped in `if REFRESH_GDELT_RAW: ... else: <report and skip, no network call>`, refuses to overwrite an existing raw file, and writes/updates `data/raw/gdelt_manifest.json` on success. Cell 21's aggregation logic now branches on `summary_path.exists()` (not the raw-parquet path it previously mismatched): (A) default — load and strictly validate the frozen summary, no CSV/raw-parquet scan; (B) `REBUILD_GDELT_DAILY_SUMMARY=True` — rebuild from a raw CSV or raw parquet using the original 500,000-row chunked algorithm (parquet now via `pyarrow.iter_batches`, never loading the full file into memory), then validate and save; (C) neither available — raise `FileNotFoundError` explaining exactly what to set, replacing the previous silent zero-fill. Section 7/8 markdown and the Implementation Note were updated to describe the A/B/C separation; no other section of the notebook was touched. Only cells 5, 18, 19, 21, 22 changed (confirmed by a full before/after cell diff); all 28 cells still compile and the notebook remains valid nbformat 4.5 JSON.

**Validation performed:** the new default-path logic was independently re-implemented in a standalone, read-only script (pandas/numpy are unavailable in this environment — `ModuleNotFoundError: numpy` — so `pyarrow` was used instead) and run against the real `data/raw/gdelt_daily_summary.parquet`: it confirms 4,018 rows, the three required columns, no nulls, no duplicate dates, non-negative counts, finite values, the exact 2015-01-01→2025-12-31 range, and that neither `gdelt_events.csv` nor `gdelt_events.parquet` was read. The notebook's own cells 5/19/21 were not re-executed in a live kernel (none is available in this environment, and doing so would require running unrelated network-dependent Sections 0–6) — their stale saved outputs were cleared (`execution_count=None`, `outputs=[]`) rather than left showing behaviour the new code no longer performs. `REFRESH_GDELT_RAW`/`REBUILD_GDELT_DAILY_SUMMARY` were never set to `True` at any point during this task.

**Explicitly not done:** modifying `src/data_collector.py`'s `GDELTCollector` to support a caller-supplied versioned output path (it always writes to a fixed `gdelt_events.parquet`) — the notebook-side guard instead hard-refuses to overwrite an existing raw file, which satisfies "do not silently overwrite" without a broader collector-class refactor outside the approved notebook-01-only scope. Also not done: editing the dissertation, and rewriting any statistic, dataset, or frozen artefact.

**GDELT scope audit (requested alongside this fix):** classification **B, narrowly** — full-history GDELT (4,018 daily rows, `master_dataset.parquet`'s `gdelt_risk_score`/`gdelt_mean_tone`/`gdelt_n_events`) is the official, current pipeline state, and it is *not* a broad documentation-staleness problem as initially framed: direct inspection of the current dissertation docx (`reports/dissertation/7030DATSCI-14_07_2026.docx`) found zero "five-day"/"5-day" GDELT mentions — every occurrence already correctly states "4,018 daily rows/observations, 2015–2025... excluded from the frozen causal DAG and FES v1.1." A repo-wide grep for "5-day"/"five-day" GDELT text confirms every other current-state document (`README.md`, `14_project_dashboard.md`, `future_improvements.md`, `11_limitations.md`, `08_figures_plan.md`, `dataset_version.md`) was already corrected in the 2026-07-13 documentation-consistency pass; the one and only stale artefact was the `09_results_log.md` zero-variance claim withdrawn above. GDELT does not affect RQ1, RQ2, RQ3, the causal DAG, event classification, or `feature_matrix.parquet` — it is a retained candidate contextual control in `master_dataset.parquet` only, per the existing 2026-07-13 GDELT-continuous-confounder decision (above).

**Reversible:** Yes. All notebook edits are source-level and version-controlled; no dataset, model, or reported statistic changed; the deleted raw GDELT files remain unreachable without a genuine external re-fetch, which is unchanged by this entry (still gated behind `REFRESH_GDELT_RAW=True`, never invoked here).

---

### 2026-07-15 — Full eight-notebook traceability audit: closing four findings, adding a permanent documentation-governance rule

**Decision:** Following a full inspection-only audit of all eight notebooks, close the four findings the audit surfaced as needing a narrow correction (rather than a documentation note only), and add a permanent, repository-wide documentation-impact and date-maintenance rule so future technical changes don't leave a stale doc trail the way the GDELT entry above did for two days.

**1. Notebook 08's pooled DoWhy estimate had no persisted source file.** Inspection found `data/processed/causal_estimates.parquet` contains only the five per-event-type causal estimates — a different estimand — never a pooled/overall row. The pooled figure (+0.005601, 95% CI [+0.002295, +0.008907], p=0.0009) existed only as Notebook 04's own printed cell output (cell 12, variable `lexicon_estimate`/`lexicon_ci` — stale name, no lexicon involved) and as a hand-copied literal in `notebooks/08_results_visualisation.ipynb` cell 3. Per Project Director direction, a new dedicated artefact was added rather than extending or overwriting `causal_estimates.parquet`: `data/processed/causal_overall_estimate.json`. A new cell was inserted into `04_causal_analysis.ipynb` (directly after the cell that computes `lexicon_estimate`/`lexicon_ci`, before the refutation-test cell) that persists the already-computed values verbatim — no recalculation, no methodology change — with an assertion that the effect lies within its own confidence interval and that `n_obs` (2,762, matching `causal_df`) is positive. `08_results_visualisation.ipynb` cell 3 was rewritten to read this file instead of hardcoding the dict, with schema/finiteness/CI-ordering/positive-`n_obs`/single-row validation, while preserving the `rq1_overall_dowhy` variable name and shape exactly so downstream code (the cell-7 caption, the cell-15 validation JSON) is unaffected. The new artefact's SHA-256 was added to cell 15's `input_sha256` provenance dict. **Verified: the reconstructed `rq1_overall_dowhy` dict is byte-identical to the previous hardcoded one** (`{'effect': 0.005601, 'ci_lower': 0.002295, 'ci_upper': 0.008907, 'p_value': 0.0009}`).

**Execution caveat, disclosed rather than papered over:** no live Jupyter kernel is available in this environment (confirmed: `numpy`/`pandas`/`scipy`/`pytest` are all missing from the Python installation used for this task). `data/processed/causal_overall_estimate.json` was therefore populated directly, not by executing the new notebook cell — its `provenance_note` field states this explicitly and records exactly which saved Notebook 04 output lines it was read from (cell 12: "Estimated effect : 0.005601", "95% CI : [0.002295, 0.008907]"; cell 11: "Causal dataframe: 2,762 rows"; DoWhy's own printed "p-value: [0.00090645]"). The two edited notebook cells (`04_causal_analysis.ipynb`'s new cell, `08_results_visualisation.ipynb`'s cells 3 and 15) have their saved outputs cleared (`execution_count: null`) rather than fabricated, for the same reason already established in the 2026-07-15 GDELT entry above. Re-running both notebooks in a real kernel will regenerate `causal_overall_estimate.json` and reproduce these exact figures; this is a known, disclosed limitation of this pass, not a silent gap.

**2. The only event-study unit test validated dead code.** `tests/test_event_study.py` tested `src/causal_engine.py::EventStudy`, an OLS market-model regression — a different methodology from Notebook 04's actual inline constant-mean-return `compute_car` (estimation window (-252,-21), event window (-5,+10), simple mean/std, no regression), which `03_methodology.md` itself already flags as the active method, with `causal_engine.py` unused. Per the approved conservative sequence: (a) a new dependency-free module, `src/event_study_reference.py`, reproduces Notebook 04's exact calculation; (b) it was validated against a hand-independently-computed synthetic example (alternating-constant estimation window, distinct-value event window, expected mean/std/CAR/t-stat computed via plain textbook formulas, not via the function under test) plus seven further edge-case tests (insufficient estimation history at the series start, below-60-observation threshold, missing values dropped not zero-filled, event window outside available data, non-trading-day event-date resolution, determinism, no-look-ahead-leakage via a perturbation test); (c) it was then cross-validated against **every one of the 264 rows** in the frozen `data/processed/car_results.parquet` (reconstructing the exact `spy_return = close.pct_change()` series from `data/raw/spy_ohlcv.parquet`), matching to floating-point precision (max abs diff ≈1e-13, pure numerical noise). (d) **Notebook 04 was deliberately left unchanged** — exact numerical equivalence earns the *option* to refactor it to import the helper, but there is no functional need, and the notebook is dissertation-referenced, so per the conservative instruction it was not touched. The old test file was renamed to `tests/test_legacy_causal_engine.py` (not deleted — `EventStudy` is a genuinely independent, self-consistent implementation of the classical OLS market-model event study, useful as an independent reference), with a module docstring making its legacy/unused status explicit. A new `tests/test_event_study.py` was created testing the new reference module. `tests/README.md` and `src/README.md` were updated to describe both files' actual roles.

**3. Mission 05A EDA dataset-scope citation.** Added a dated clarification within the existing 2026-07-05 Mission 05A entry in `09_results_log.md` (not a new top-level entry, since it's a citation correction on that one entry, distinct from the GDELT withdrawal already recorded there): the entry's heading/Source line name `master_dataset.parquet`, but `02_eda.ipynb`'s own `master_df` variable actually loads `data/raw/daily_modelling_calendar_v1.parquet` (2,743 rows), a Notebook 01 precursor, not the later canonical 2,765-row file. States explicitly that this does not invalidate the entry's EDA findings or any later frozen result, and does not reopen the already-withdrawn GDELT zero-variance claim.

**4. Four lower-severity findings recorded as future work, not actioned:** `future_improvements.md` items 33–36 — non-monotonic-from-1 execution sessions in Notebooks 05/06/07 (reproducibility-only, no wrong output); Notebook 07's cold-start dependency on persisted XGBoost/LightGBM hyperparameters (already a deliberate, previously-logged choice); the FinBERT sentiment cache's lack of a sidecar provenance manifest (recommended fix specified: a manifest recording model identifier/revision, scored field, preprocessing config, creation date, pipeline version, checksum — explicitly not a cache rewrite, and explicitly not claiming fine-tuning, since no labelled dataset/training procedure/checkpoint/evaluation artefact exists anywhere in this repository); and Notebook 04's stale "lexicon" variable/column names (self-disclosed in-notebook already, cosmetic-only, deferred to a formal version bump per `00_project_freeze.md` Section 7).

**Documentation-impact scan (per the new Section 10 below):** checked and updated where genuinely affected — `09_results_log.md`, `future_improvements.md`, `tests/README.md`, `src/README.md`, `docs/stakeholder_report/README.md`, `docs/stakeholder_report/01_data_collection.md` (re-review date added, no substantive claim changed), notebooks 04/08. Checked and found to need **no change**: `dataset_contract.md`, `feature_contract.md`, `model_contract.md`, `05_data_dictionary.md`, `06_feature_dictionary.md`, `13_validation_checklist.md`, `15_traceability_matrix.md`, `11_limitations.md`, the dissertation, all frozen `.parquet`/`.joblib` artefacts, all figures. A permanent **Section 10 — Documentation Impact and Date Maintenance** was added to `00_project_freeze.md`, codifying the created/historical-decision/last-modified/last-verified/generated/status date distinction and the "technically complete, documentation incomplete" reporting category used in this entry's own completion report.

**RQ1–RQ3 impact:** None. No dataset, feature matrix, model artefact, or reported statistic changed; `rq1_overall_dowhy`'s value is unchanged (verified byte-identical); `car_results.parquet` and `causal_estimates.parquet` were not modified; FES v1.1 remains 92 predictors/2,477 rows/1,727 train/750 test (unchanged, unread by any file touched in this entry).

**Reversible:** Yes. All changes are source-level (notebook cells, a new `src/` module, a new small JSON artefact, test/doc files) and version-controlled; no frozen dataset, model, or figure was touched.

---

### 2026-07-15 — Live execution of the causal-lineage/event-study corrections: environment built, both notebooks clean-executed, all results confirmed unchanged

**Decision:** Close the "disclosed limitation" from the entry above — no live kernel had been available — by building a working project environment and actually executing both edited notebooks from clean kernels, rather than continuing to rely on inspection-based reasoning.

**Environment:** The default `python3` on this machine (`/opt/homebrew/bin/python3.14`) has a broken `numpy` install (confirmed: `ModuleNotFoundError`), which is why the prior entry's validation used a dependency-free reference module and `pyarrow`-only reads. A second, working Python 3.14.6 installation was found at `/usr/local/bin/python3` with `numpy 2.4.6`, `pandas 3.0.3`, `scipy 1.18.0`, `pyarrow 25.0.0`, `jupyter`, `nbclient 0.11.0`, `nbformat`, `matplotlib`, `seaborn`, `scikit-learn`, `xgboost`, `lightgbm`, `shap`, `transformers`, `torch` already present, but missing `dowhy`, `statsmodels`, and `pytest`. All three were installed via `pip install` at the exact versions pinned in `requirements-lock.txt` (`dowhy==0.8`, `statsmodels==0.14.6`, `pytest==9.1.1`), plus `ipykernel` (registered as the `python3` Jupyter kernel) since none was previously registered. This is not a project-local virtual environment — it is a pre-existing system Python installation with most of the stack already present; a `requirements-lock.txt`-based venv was not built from scratch since a materially complete environment already existed.

**Notebook 04 — clean execution:** Executed via `nbclient.NotebookClient` with a fresh `python3` kernel, all 19 code cells run in physical order, saved only after full success. Execution counts: monotonic 1→19, zero errors, zero cells left unexecuted. The live-computed pooled estimate (cell 12's own DoWhy output): `Mean value: 0.005600854084540827`, `p-value: [0.00090645]`, `95% CI: [0.0022946, 0.0089071]` — identical to the rounded values already reported everywhere (`+0.005601`, `[+0.002295, +0.008907]`, `p=0.0009`), and identical to what the earlier inspection-based bootstrap had recorded. The new persistence cell (13) executed successfully, printing `causal_overall_estimate.json saved effect=+0.005601 CI=[+0.002295, +0.008907] p=0.0009 n=2,762`.

**`causal_overall_estimate.json` — notebook-generated vs. bootstrap:** Every scientific/methodology field is identical: `causal_effect`, `ci_lower`, `ci_upper`, `p_value`, `n_obs` (2,762), `treatment`, `outcome`, `confounders`, `method`, `description`. The only difference is that the bootstrap file's `provenance_note` field (which explained it had been populated by inspection rather than execution) is now absent, because the real notebook code never wrote such a field — no claim of manual extraction survives now that the notebook has actually generated it. New authoritative SHA-256: `9633bc1474cc95617414e941ca7478744d139b4de89cef8499074133a390361d`.

**Notebook 08 — clean execution:** Executed the same way, all 8 code cells, execution counts monotonic 1→8, zero errors. Cell 3's assertions (schema/finiteness/CI-ordering/positive-`n_obs`/not-a-list) all passed silently, confirming the read-from-artefact path works end-to-end; printed verdicts unchanged (`RQ1 BH-FDR rejections=0/5`, `RQ2: H0 NOT REJECTED`, `RQ3: H0(3) NOT REJECTED`). The regenerated `reports/figures/results_visualisation_validation.json` shows `rq1_overall_dowhy` byte-identical to the pre-correction hardcoded value, and its `input_sha256.causal_overall_estimate` field now correctly binds to the new artefact's hash above.

**Figure drift check:** all four figures (`08a_event_landscape.png`, `08b_causal_evidence.png`, `08c_predictive_pipeline.png`, `08d_full_dashboard.png`) were re-generated by this live run and their SHA-256 hashes compared byte-for-byte against the pre-execution copies — **all four are identical, zero drift.** (Matplotlib's PNG output is deterministic here because no run-generated timestamp is embedded in the pixel data and no upstream value changed.)

**Test execution (real `pytest`, not the standalone runner used previously):** `python3 -m pytest tests/test_event_study.py tests/test_legacy_causal_engine.py -v` → **14 collected, 14 passed, 0 failed, 0 warnings, 0.90s.** The 10 active-methodology tests cover all of: default windows matching Notebook 04 (-252,-21)/(-5,+10); hand-independent CAR/t-stat computation; the real default-window/60-observation-threshold path; insufficient history at the series start; below-60-observation rejection; missing-value dropping (not zero-filling); event window past the end of the data; non-trading-day date resolution; determinism; and no-look-ahead-leakage via a direct perturbation test.

**Full 264-row CAR equivalence (repeated with the real pandas/numpy/scipy stack, not the earlier `pyarrow`-only check):** reconstructed `spy_return = close.pct_change()` from `data/raw/spy_ohlcv.parquet` exactly as Notebook 04 does, resolved each of the 264 `car_results.parquet` event dates to its nearest trading day, and re-ran `compute_car_reference`. **264/264 rows matched** within 1e-6 absolute tolerance on `car`, `mean_ar`, `t_stat`, and `p_value`, with `n_event_days`/`n_est_days` matching exactly; max absolute differences ranged from 1.7e-18 to 1.1e-13 (floating-point noise, not a real discrepancy); zero mismatched rows; zero rows where the reference returned `None` unexpectedly; row count and date ordering both confirmed.

**Frozen-artefact hash comparison (before vs. after this entire live-execution pass):** SHA-256 recorded before and after for `car_results.parquet`, `causal_estimates.parquet`, `feature_matrix.parquet`, `feature_profile.json`, `event_type_statistics.parquet`, `master_dataset.parquet`, all five model `.joblib` files (`baseline_lasso`, `event_lasso`, `xgboost`, `lightgbm`, `random_forest_importance_tool`), `model_comparison.parquet`, `feature_importance.parquet`, all three SHAP parquet files, `statistical_tests.json`, `baseline_predictions.parquet`, and all six `04`/`08` figures. **Every single one is byte-identical before and after.** The only file whose hash changed is `causal_overall_estimate.json` — the one artefact this correction was always expected to produce.

**Documentation updated as a result:** `docs/stakeholder_report/README.md` Stage 8 status changed from "🟠 Updated but awaiting final verification" to "🟡 Notebook and technical stage verified" (the plain-language page still doesn't exist, so it is not marked "written"). No other stakeholder page, research-bible document, or the dissertation required a change — no numerical or methodological difference was found anywhere in this pass.

**RQ1–RQ3 impact:** None. Confirmed by direct re-execution, not merely by inspection this time.

**Reversible:** Yes. `causal_overall_estimate.json` can be regenerated deterministically by re-running Notebook 04; no frozen dataset, model, or figure was touched.
