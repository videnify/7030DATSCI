# reports/

**Purpose:** All generated, non-code outputs of the pipeline — figures, tables, per-model statistics summaries, and the dissertation itself.
**Added:** 2026-07-06 (repository hygiene pass — this folder previously had no top-level README; its subfolders are self-explanatory by name but were not indexed anywhere in one place). **Updated 2026-07-13** — `statistics/` and `event/` no longer exist (archived in the 2026-07-13 repository cleanup, see below); `baseline/` and `model_comparison/` were regenerated by the same day's pipeline rebuild.

## Contents (verified against disk, 2026-07-13)

| Subfolder | Purpose |
|-----------|---------|
| `dissertation/` | The dissertation document itself (tracked; the only `reports/` content meant to be a final, citable deliverable) — see `reports/dissertation/README.md` for the current multi-draft situation |
| `figures/` | All chart/plot PNGs (300 dpi where publication-quality) regenerated by the numbered notebooks — gitignored, reproducible |
| `tables/` | CSV exports of statistical/descriptive tables (correlation matrices, stationarity tests, metrics) referenced by the dissertation |
| `baseline/` | `Baseline_LASSO` (MCP v1.0) outputs: `baseline_metrics.json`, `baseline_predictions.parquet` — regenerated 2026-07-13 |
| `model_comparison/` | The primary, frozen RQ3 comparison (MCP v1.0): `model_comparison.parquet`, `statistical_tests.json`, `feature_importance.parquet`, `shap_values_*.parquet`, `event_model_predictions.parquet` — SHAP/prediction outputs rebuilt 2026-07-13 (no current notebook produced them before that pass, see `docs/research_bible/10_decision_log.md`) |

## No longer present (archived 2026-07-13, not deleted)

- **`statistics/`** (per-mission Markdown summaries, e.g. `07_event_models_summary.md`) and **`event/`** (isolated Mission 07A/07B/07C single-model re-run outputs) — moved to `models/archive/pre_2026-07-13_pipeline_rebuild/reports_statistics/` and `reports_event/` respectively during the full-pipeline consistency scan and cleanup, since neither had a current producer and both predated the same day's rebuild. `07_event_models_summary.md` specifically is still cited as the documented methodology source for this session's rebuilt SHAP computation in `07_model_evaluation.ipynb` — its citation paths were updated to the new archive location.

## Legacy content (not part of the current pipeline)

- `08_05_2026_DATSCI7030_Data_Collection_Report.docx` / `.pdf` — an early, standalone data-collection report predating the Research Bible and the frozen contracts. Superseded in scope by `docs/research_bible/05_data_dictionary.md` and the dashboard/decision-log documents. Retained for historical reference, not cited as current.

## Dependencies

Populated by `notebooks/02_eda.ipynb` through `08_results_visualisation.ipynb`; consumed by `reports/dissertation/`.
