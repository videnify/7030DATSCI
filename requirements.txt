# ============================================================
# DATSCI7030 — Requirements
# Causal Event-Driven Market Impact Modelling
# ============================================================
# Install all: pip install -r requirements.txt
# Quick check: python -c "import pyarrow, yfinance, pandas; print('OK')"

# --- Core Data Science ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
statsmodels>=0.14.0

# --- Parquet / Storage (REQUIRED — pandas uses pyarrow by default) ---
pyarrow>=20.0.0               # .parquet engine — requires pandas>=2.3 (fixes PeriodType clash)
fastparquet>=2024.2.0         # fallback parquet engine

# --- Market Data ---
yfinance>=0.2.36
fredapi>=0.5.1                # FRED macro data — works on Python 3.13
# pandas-datareader is BROKEN on Python 3.13 (deprecate_kwarg TypeError) — do not use

# --- Web Scraping (American Presidency Project) ---
beautifulsoup4>=4.12.0        # HTML parsing for APPCollector
lxml>=5.0.0                   # Fast XML/HTML parser backend for BeautifulSoup

# --- NLP & Event Detection ---
transformers>=4.40.0          # FinBERT (ProsusAI/finbert)
torch>=2.0.0                  # PyTorch backend for transformers
spacy>=3.7.0                  # Optional NLP utilities
newsapi-python>=0.2.7         # NewsAPI headlines (legacy — superseded by APP for 2015–2025)

# --- Causal Inference ---
dowhy>=0.11.1                 # DoWhy structural causal model
causalml>=0.15.0              # Causal ML meta-learners

# --- Machine Learning ---
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.3.0
shap>=0.45.0                  # SHAP explainability

# --- Visualisation ---
matplotlib>=3.10.9
seaborn>=0.13.0
plotly>=5.20.0
kaleido>=0.2.1                # Static plotly image export

# --- Notebooks & Kernel ---
jupyter>=1.0.0
jupyterlab>=4.0.0
ipykernel>=6.29.0             # Jupyter kernel — ensures correct Python registered
ipywidgets>=8.0.0
nbformat>=5.9.0

# --- Utilities ---
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.66.0
requests>=2.31.0
joblib>=1.3.0
certifi>=2024.0.0             # SSL certificates — fixes macOS Python 3.13 CERTIFICATE_VERIFY_FAILED

# --- Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0
